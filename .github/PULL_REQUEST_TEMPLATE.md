# Pull Request

## 📝 Description
<!-- อธิบายสิ่งที่เปลี่ยนแปลงในครั้งนี้ -->

### What changed?
<!-- สรุปการเปลี่ยนแปลงหลักๆ -->
- 

### Why?
<!-- เหตุผลที่ต้องทำการเปลี่ยนแปลงนี้ -->
- 

### Related Issue
<!-- เชื่อมโยงกับ Issue ที่เกี่ยวข้อง (ถ้ามี) -->
Closes #

---

## 🔍 Type of Change
<!-- เลือกประเภทของการเปลี่ยนแปลง (ใส่ x ใน [ ]) -->

- [ ] ✨ New feature (เพิ่มความสามารถใหม่)
- [ ] 🐛 Bug fix (แก้ไขปัญหา)
- [ ] 🔨 Refactoring (ปรับปรุงโครงสร้างโค้ด)
- [ ] 📚 Documentation (เอกสาร)
- [ ] 🎨 UI/UX (ปรับปรุง UI)
- [ ] ⚡ Performance (เพิ่มประสิทธิภาพ)
- [ ] 🔒 Security (ความปลอดภัย)
- [ ] 🧪 Testing (เพิ่ม tests)

---

## ✅ Checklist

### Code Quality
- [ ] โค้ดตรวจสอบด้วย linter แล้ว (`flutter analyze`)
- [ ] โค้ดจัดรูปแบบแล้ว (`dart format .`)
- [ ] ไม่มี warning หรือ error
- [ ] ลบโค้ดที่ไม่ใช้แล้ว (commented code, unused imports)
- [ ] เพิ่ม comments ในส่วนที่ซับซ้อน (ถ้าจำเป็น)

### Testing
- [ ] Unit tests ผ่านทั้งหมด
- [ ] ทดสอบ manual แล้ว (ระบุผลลัพธ์ด้านล่าง)
- [ ] ทดสอบ edge cases แล้ว (empty state, error state, loading state)
- [ ] ทดสอบบน Android
- [ ] ทดสอบบน iOS (ถ้าเกี่ยวข้อง)

### Backend (ถ้ามี)
- [ ] API endpoint มี server-side validation
- [ ] มี error handling ที่เหมาะสม
- [ ] มี logging สำหรับ debug
- [ ] มี rate limiting (ถ้าจำเป็น)
- [ ] Transaction เป็น atomic (ไม่มี race condition)

### Frontend (ถ้ามี)
- [ ] มี loading state
- [ ] มี error state (แสดง error message ที่เข้าใจได้)
- [ ] มี empty state
- [ ] รองรับ offline mode (ถ้าเกี่ยวข้อง)
- [ ] UI ตรงตาม design spec

### Database (ถ้ามี)
- [ ] Firestore indexes สร้างแล้ว (ถ้าใช้ compound queries)
- [ ] Firestore security rules อัปเดตแล้ว
- [ ] Migration script เตรียมไว้แล้ว (ถ้าเปลี่ยน schema)

### IAP/Monetization (ถ้ามี)
- [ ] มี duplicate purchase prevention
- [ ] มี retry mechanism สำหรับ failed transactions
- [ ] Receipt validation ทำงานถูกต้อง
- [ ] ทดสอบกับ sandbox account แล้ว

### Quest Bar Specific (ถ้าเกี่ยวข้อง)
- [ ] Countdown timer แสดงเวลาถูกต้อง
- [ ] Offer priority logic ทำงานถูกต้อง
- [ ] Dismissed state เก็บไว้ใน local
- [ ] API ใช้ server time (ไม่ trust client time)

### Security
- [ ] Input validation (sanitize ทุก input จาก client)
- [ ] Rate limiting (ป้องกัน abuse)
- [ ] ไม่มี sensitive data ใน logs
- [ ] ไม่มี hardcoded secrets

### Localization (ถ้ามี)
- [ ] เพิ่ม keys ใน `app_en.arb`
- [ ] เพิ่ม keys ใน `app_th.arb`
- [ ] ทดสอบทั้งภาษาอังกฤษและไทย

---

## 🧪 Testing

### Manual Testing Results
<!-- อธิบายวิธีการทดสอบและผลลัพธ์ -->

**Test Scenarios:**
1. 
2. 
3. 

**Test Results:**
- ✅ Scenario 1: [ผลลัพธ์]
- ✅ Scenario 2: [ผลลัพธ์]
- ✅ Scenario 3: [ผลลัพธ์]

### Edge Cases Tested
<!-- edge cases ที่ทดสอบแล้ว -->
- [ ] Empty state (ไม่มีข้อมูล)
- [ ] Error state (API error, network error)
- [ ] Loading state (แสดง loading indicator)
- [ ] Offline mode (ทำงานโดยไม่มี internet)
- [ ] ข้อมูลเยอะ (pagination, infinite scroll)
- [ ] ข้อมูลน้อย (1-2 items)

---

## 📸 Screenshots
<!-- เพิ่ม screenshots ถ้าเปลี่ยน UI (Before/After) -->

### Before
<!-- Screenshot ก่อนเปลี่ยนแปลง (ถ้ามี) -->

### After
<!-- Screenshot หลังเปลี่ยนแปลง -->

---

## 🚀 Deployment Notes
<!-- สิ่งที่ต้องทำก่อน/หลัง deploy (ถ้ามี) -->

### Before Deploy
- [ ] 

### After Deploy
- [ ] 

---

## 📝 Additional Notes
<!-- ข้อมูลเพิ่มเติมที่ reviewer ควรรู้ -->

---

## 👥 Reviewers
<!-- tag คนที่ต้องการให้ review -->

@<!-- username -->
