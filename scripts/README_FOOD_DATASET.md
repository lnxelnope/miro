# การใช้งาน MM-Food-100K Dataset

## 📋 ภาพรวม

สคริปต์นี้ใช้สำหรับดาวน์โหลดและประมวลผล MM-Food-100K dataset จาก Hugging Face เพื่อให้ local AI สามารถเรียนรู้และค้นหาอาหารทั่วโลกได้

## 🚀 การใช้งาน

### 1. ติดตั้ง Dependencies

```bash
pip install datasets huggingface_hub
```

### 2. Login เข้า Hugging Face

```bash
huggingface-cli login
```

กรอก token ที่ได้จาก https://huggingface.co/settings/tokens

### 3. รัน Script

```bash
python scripts/process_food_dataset.py
```

### 4. ผลลัพธ์

Script จะสร้างไฟล์ต่อไปนี้ใน `assets/data/`:

- `global_food_database.json` - ข้อมูลอาหารทั้งหมด (100K+ รายการ)
- `global_food_index.json` - Index สำหรับค้นหาที่เร็วขึ้น
- `global_food_summary.json` - สรุปข้อมูล

## 📁 โครงสร้างข้อมูล

### GlobalFoodData

```json
{
  "id": 0,
  "name": "ชื่ออาหาร (ภาษาไทย)",
  "name_en": "Food Name (English)",
  "calories": 250.0,
  "protein": 15.0,
  "carbs": 30.0,
  "fat": 10.0,
  "fiber": 5.0,
  "sugar": 8.0,
  "sodium": 500.0,
  "serving_size": 100.0,
  "serving_unit": "g",
  "category": "หมวดหมู่",
  "cuisine": "อาหารประจำชาติ",
  "image_url": "URL รูปภาพ"
}
```

## 🔍 การใช้งานใน Flutter/Dart

### โหลดข้อมูล (ทำอัตโนมัติใน main.dart)

```dart
await GlobalFoodDatabase.load();
```

### ค้นหาอาหาร

```dart
// ค้นหาแบบ exact match
final food = GlobalFoodDatabase.lookup('ข้าวผัด');

// ค้นหาแบบ fuzzy search
final results = GlobalFoodDatabase.search('pizza', limit: 10);

// ค้นหาตามหมวดหมู่
final desserts = GlobalFoodDatabase.findByCategory('dessert');

// ค้นหาตามอาหารประจำชาติ
final thaiFoods = GlobalFoodDatabase.findByCuisine('thai');
```

### ใช้ร่วมกับ ThaiFoodDatabase

ระบบจะค้นหาจาก ThaiFoodDatabase ก่อน (เพราะมีอาหารไทยที่เฉพาะเจาะจง) แล้วจึงค้นหาจาก GlobalFoodDatabase ถ้าไม่พบ

## ⚙️ การทำงาน

1. **ThaiFoodDatabase** - อาหารไทยที่เฉพาะเจาะจง (exact match)
2. **GlobalFoodDatabase** - อาหารทั่วโลก (exact match + fuzzy search)
3. **Fallback** - ถ้าไม่พบจะใช้ค่าประมาณ

## 📝 หมายเหตุ

- Dataset มีขนาดใหญ่ (~100K รายการ) อาจใช้เวลาในการโหลด
- ไฟล์ JSON จะถูก bundle ไว้ใน app (เพิ่มขนาด app)
- ถ้าต้องการลดขนาด สามารถกรองเฉพาะอาหารที่ใช้บ่อยได้

## 🔄 อัปเดตข้อมูล

รัน script ใหม่เพื่ออัปเดตข้อมูลจาก dataset ล่าสุด:

```bash
python scripts/process_food_dataset.py
```

## 🐛 Troubleshooting

### ไม่พบ dataset
- ตรวจสอบว่า login Hugging Face แล้ว
- ตรวจสอบว่า dataset "Codatta/MM-Food-100K" มีอยู่จริง

### ไฟล์ JSON ไม่ถูกสร้าง
- ตรวจสอบว่า folder `assets/data/` มีอยู่
- ตรวจสอบ permission ของ folder

### Flutter ไม่พบ assets
- ตรวจสอบว่าเพิ่ม `assets/data/` ใน `pubspec.yaml` แล้ว
- รัน `flutter pub get` ใหม่
- รัน `flutter clean` และ build ใหม่
