# 🚀 Quick Test Guide for Miro Hybrid

## 🛠️ Setup ก่อนเริ่ม (สำคัญ!)

### ⚠️ ถ้าเห็น "Flutter NOT found in PATH":

**Option 1: Auto-detect Flutter (แนะนำ)**
```
setup_flutter_path.bat
```

**Option 2: ติดตั้ง Flutter (ถ้ายังไม่มี)**
- ดูคู่มือ: `INSTALL_FLUTTER_GUIDE.md`
- หรือ: https://flutter.dev/docs/get-started/install/windows

**Option 3: ระบุ Path เอง (ถ้า Flutter ติดตั้งแล้วแต่ไม่ได้ Add PATH)**
```cmd
set PATH=C:\flutter\bin;%PATH%
check_setup.bat
```

---

### ถ้า Flutter พร้อมแล้ว:

```
check_setup.bat        (ตรวจสอบ Environment)
setup_project.bat      (Setup โครงสร้าง Flutter)
```

---

## วิธีทดสอบแอป (เรียงตามความง่าย)

---

### Option 1: 🌐 Web Preview (เร็วที่สุด)
**ใช้สำหรับ:** ดู UI เบื้องต้น (ML Kit/Isar จะไม่ทำงาน)

```
run_web.bat
```

1. ดับเบิ้ลคลิก `run_web.bat`
2. รอ Flutter เปิด Chrome
3. ดู UI ได้เลย (ข้อมูลจริงจะไม่ทำงาน)

---

### Option 2: 📱 Build APK + Transfer Manual
**ใช้สำหรับ:** ทดสอบจริงบนมือถือ

```
build_apk.bat
```

1. ดับเบิ้ลคลิก `build_apk.bat`
2. รอ Build (ครั้งแรกอาจนาน 3-5 นาที)
3. โฟลเดอร์ `build\app\outputs\flutter-apk\` จะเปิดขึ้น
4. ส่งไฟล์ `app-debug.apk` ไปมือถือ (USB/Drive/Line)
5. ติดตั้งบนมือถือ

---

### Option 3: 📡 Local Server + Download via Browser
**ใช้สำหรับ:** ไม่อยากต่อสาย USB

```
build_apk.bat        (Build ก่อน)
serve_apk_qrcode.bat (เปิด Server)
```

1. รัน `build_apk.bat` ก่อน
2. รัน `serve_apk_qrcode.bat`
3. ต่อมือถือกับ Wi-Fi เดียวกับคอม
4. เปิด Browser บนมือถือ ไปที่ `http://<IP ที่แสดง>:8080`
5. กด Download แล้วติดตั้ง

---

### Option 4: 📶 Wireless ADB (สำหรับ Dev)
**ใช้สำหรับ:** ติดตั้งผ่าน Wi-Fi โดยไม่ต้องเปิด Browser

```
build_apk.bat
install_via_adb_wifi.bat
```

**ข้อกำหนด:**
- เปิด Developer Options บนมือถือ
- เปิด Wireless Debugging
- ใช้ Pairing Code จากมือถือ

---

## ⚠️ หมายเหตุ

- **Web Version:** ใช้ดู UI อย่างเดียว ไม่สามารถทดสอบ:
  - การสแกนรูปจาก Gallery
  - ML Kit (OCR, Image Labeling)
  - Isar Database
  - Google Calendar Sync

- **ครั้งแรกที่ Build:** อาจใช้เวลานาน เพราะต้อง Download Gradle และ Dependencies

- **ถ้า Build Error:** ให้รัน `flutter doctor` ดูว่ามี Environment ครบไหม
