---
phase: 07-camera-stream
plan: 02
type: execute
wave: 1
depends_on:
  - "06-02"
  - "07-01"
files_modified:
  - lib/features/arscan/application/camera_stream_controller.dart
autonomous: true
requirements:
  - ARSCAN-03
user_setup: []
must_haves:
  truths:
    - "มี controller สำหรับจัดการ camera stream แยกจาก UI"
    - "สามารถเปิด/ปิด stream และส่ง frame ต่อเนื่องให้ consumer ภายหลังได้"
  artifacts:
    - path: "lib/features/arscan/application/camera_stream_controller.dart"
      provides: "การควบคุม camera stream และการ expose frame stream"
  key_links: []
---

<objective>
สร้างเลเยอร์ควบคุม camera stream แยกจาก UI เพื่อรองรับการประมวลผล frame แบบต่อเนื่องใน phase detection ถัดไป โดยเน้นความเสถียรและความง่ายต่อการทดสอบ

Purpose: แยก concerns ระหว่าง UI กับการจัดการกล้อง/stream
Output: Camera stream controller ที่พร้อมเชื่อมกับ preview และ detection
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ออกแบบ interface สำหรับ camera stream controller</name>
  <files>lib/features/arscan/application/camera_stream_controller.dart</files>
  <action>กำหนด interface/คลาสสำหรับเปิด/ปิดกล้อง, start/stop image stream, expose stream ของ frame และ callback lifecycle ที่จำเป็น โดยไม่ผูกกับ widget framework โดยตรงเกินไป</action>
  <verify>รันการคอมไพล์โปรเจกต์ให้ผ่านและไม่มี warning สำคัญเกี่ยวกับ controller</verify>
  <done>interface ชัดเจนและพร้อมให้ UI/detection เรียกใช้</done>
</task>

<task type="auto">
  <name>Task 2: เชื่อม controller เข้ากับ ARscan screen/preview</name>
  <files>lib/features/arscan/application/camera_stream_controller.dart, lib/features/arscan/presentation/arscan_screen.dart</files>
  <action>ผูก controller เข้ากับ ARscan screen และ widget preview ที่สร้างในแผน 07-01 เพื่อให้เมื่อเปิดหน้าจอแล้ว stream ถูกเริ่ม/หยุดอย่างเหมาะสม โดยยังไม่ทำ detection</action>
  <verify>ทดสอบเข้า/ออก ARscan screen หลายครั้งและสังเกต log/fps เบื้องต้นว่าไม่มี crash หรือ resource leak ชัดเจน</verify>
  <done>ARscan screen ใช้ controller ใหม่ในการจัดการ stream แทนการใช้โค้ดกล้องใน widget โดยตรง</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] controller ทำงานร่วมกับ ARscan screen ได้โดยไม่มี error runtime
- [ ] ไม่มี behavior แปลกจากการเปิด/ปิดกล้องซ้ำ ๆ ในการทดสอบช่วงสั้น ๆ
</verification>

<success_criteria>

- Camera stream ถูกจัดการผ่าน controller เลเยอร์กลาง
- โค้ดพร้อมให้ detection เข้ามา subscribe frame stream ใน Phase 8
  </success_criteria>

<output>
After completion, create `.planning/phases/07-camera-stream/07-02-SUMMARY.md`
</output>

