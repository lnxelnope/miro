---
phase: 07-camera-stream
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - lib/features/arscan/application/camera_stream_diagnostics.dart
autonomous: true
requirements:
  - ARSCAN-03
user_setup: []
must_haves:
  truths:
    - "สามารถตรวจวัด frame rate โดยประมาณของ camera stream ได้"
    - "มีวิธีทดสอบ long-run stability (อย่างน้อย 5 นาที) ของ stream"
  artifacts:
    - path: "lib/features/arscan/application/camera_stream_diagnostics.dart"
      provides: "ยูทิลิตี้/เครื่องมือวัด fps และ long-run diagnostics สำหรับ stream"
  key_links: []
---

<objective>
เพิ่มเครื่องมือวัด performance และ stability ของ camera stream เพื่อตรวจว่า requirement ARSCAN-03 เรื่อง frame rate และความเสถียรถูกตอบโจทย์

Purpose: ทำให้สามารถตรวจสอบและดีบัก performance ของ stream ได้อย่างเป็นระบบ
Output: diagnostics utility และแนวทางทดสอบ long-run
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: สร้าง diagnostics utility สำหรับนับ frame และคำนวณ fps</name>
  <files>lib/features/arscan/application/camera_stream_diagnostics.dart</files>
  <action>สร้างคลาส/ฟังก์ชันที่สามารถรับ callback จาก stream ของ frame แล้วนับจำนวน frame ต่อวินาที แสดงผลเป็น log หรือ overlay สำหรับ dev build</action>
  <verify>รัน ARscan screen แล้วสังเกต log/overlay ว่ามีค่า fps แสดงเมื่อ stream ทำงาน</verify>
  <done>fps วัดได้และใช้ในการประเมิน performance เบื้องต้น</done>
</task>

<task type="auto">
  <name>Task 2: นิยามขั้นตอนทดสอบ long-run stability</name>
  <files>lib/features/arscan/application/camera_stream_diagnostics.dart</files>
  <action>เพิ่มโค้ด/คำอธิบายสำหรับการรัน camera stream ต่อเนื่องอย่างน้อย 5 นาทีใน dev mode พร้อม log memory/exception ที่สำคัญ เพื่อใช้เป็น checklist ทดสอบ long-run stability</action>
  <verify>รันการทดสอบ long-run บนอย่างน้อย 1 อุปกรณ์ตามขั้นตอนที่กำหนดและยืนยันว่าไม่มี crash</verify>
  <done>มีวิธีทดสอบ long-run stability ที่นำไปใช้ซ้ำได้</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] มีวิธีการเก็บข้อมูล fps ของ camera stream บน dev build
- [ ] ผ่านการทดสอบ long-run อย่างน้อย 5 นาทีโดยไม่มี crash หรือ memory leak ชัดเจน
</verification>

<success_criteria>

- ARSCAN-03 สามารถตรวจวัดและยืนยันได้เชิงเทคนิค ไม่ใช่แค่ความรู้สึก
- ทีม dev มีเครื่องมือและขั้นตอนสำหรับตรวจ performance ของ stream
  </success_criteria>

<output>
After completion, create `.planning/phases/07-camera-stream/07-03-SUMMARY.md`
</output>

