---
phase: 07-camera-stream
plan: 01
type: execute
wave: 1
depends_on:
  - "06-02"
files_modified:
  - lib/features/arscan/presentation/arscan_screen.dart
  - lib/features/arscan/presentation/widgets/camera_preview_view.dart
autonomous: true
requirements:
  - ARSCAN-03
user_setup: []
must_haves:
  truths:
    - "ผู้ใช้เห็น live camera preview แบบเต็มหน้าจอใน ARscan screen"
    - "preview มี frame rate เสถียรในระดับที่รองรับ detection phase ถัดไป"
  artifacts:
    - path: "lib/features/arscan/presentation/arscan_screen.dart"
      provides: "ARscan screen ที่เป็น entry point ของ camera stream"
    - path: "lib/features/arscan/presentation/widgets/camera_preview_view.dart"
      provides: "widget สำหรับแสดง camera preview"
  key_links:
    - from: "lib/features/arscan/presentation/arscan_screen.dart"
      to: "lib/features/arscan/presentation/widgets/camera_preview_view.dart"
      via: "การประกอบ UI"
      pattern: "CameraPreviewView"
---

<objective>
สร้าง ARscan screen พร้อม live camera preview แบบเต็มหน้าจอที่ใช้กล้องเวอร์ชันใหม่จาก Phase 6 และเตรียมโครงสร้างให้พร้อมสำหรับการเสียบ frame processing ภายหลัง

Purpose: วางรากฐาน UI/กล้องสำหรับ camera stream ก่อนเพิ่ม detection
Output: ARscan screen และ widget preview ที่ทำงานได้จริงบนอุปกรณ์
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ออกแบบโครงสร้าง ARscan screen และ route</name>
  <files>lib/features/arscan/presentation/arscan_screen.dart</files>
  <action>สร้าง ARscan screen ใหม่หรือตรวจสอบของเดิม กำหนด route/entry point ที่สอดคล้องกับ navigation ปัจจุบัน โดยยังไม่เพิ่ม detection หรือตัวควบคุมอื่น ๆ</action>
  <verify>รันแอป dev และนำทางไปยัง ARscan screen ได้โดยไม่มี crash</verify>
  <done>มี ARscan screen ที่เป็นจุดรวมของ camera stream ชัดเจน</done>
</task>

<task type="auto">
  <name>Task 2: สร้าง widget camera preview ที่ใช้ camera plugin ใหม่</name>
  <files>lib/features/arscan/presentation/widgets/camera_preview_view.dart</files>
  <action>สร้าง widget สำหรับแสดง live camera preview โดยใช้กล้องเวอร์ชันที่อัปเดตใน Phase 6 รองรับ orientation ที่เหมาะสม และเตรียม interface สำหรับรับ controller/frame stream ภายหลัง</action>
  <verify>ทดสอบบนอุปกรณ์/จำลองอย่างน้อย 1 เครื่อง ให้ preview แสดงผลได้เต็มหน้าจอโดยไม่มี error</verify>
  <done>preview แสดงผลได้ดีและประกอบเข้ากับ ARscan screen แล้ว</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ARscan screen เปิดได้จาก navigation จริงของแอป
- [ ] กล้องแสดง live preview ได้บนอุปกรณ์อย่างน้อย 1 เครื่อง
- [ ] ไม่มี error lifecycle เบื้องต้นเมื่อเข้า/ออก ARscan screen
</verification>

<success_criteria>

- ARscan screen พร้อม widget preview ทำงานได้
- โครงสร้างพร้อมสำหรับการต่อยอด frame processing ใน phase ถัดไป
  </success_criteria>

<output>
After completion, create `.planning/phases/07-camera-stream/07-01-SUMMARY.md`
</output>

