---
phase: 17-merge-media
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01-PLAN.md
files_modified:
  - lib/core/widgets/ingredient_thumb.dart
  - lib/core/nutrition/ingredients_codec.dart
  - lib/core/nutrition/ingredients_models.dart
  - lib/features/health/widgets/add_food_bottom_sheet.dart
  - lib/features/health/widgets/edit_food_bottom_sheet.dart
  - lib/features/health/widgets/gemini_analysis_sheet.dart
  - lib/features/health/widgets/create_meal_sheet.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_th.arb
autonomous: true
requirements:
  - ARC2-THUMB-01
user_setup:
  - "flutter gen-l10n หลังแก้ ARB"
must_haves:
  truths:
    - "IngredientThumb แสดงรูปเมื่อมี path และไม่แสดงอะไรเมื่อไม่มี (ไม่มี placeholder)"
    - "โมเดล v2 รองรับ optional imagePath บนโหนด ingredient โดย backward compatible"
    - "แตะ thumb เปิดดูรูปเต็มจอ พร้อม overlay bbox เมื่อมีข้อมูล"
  artifacts:
    - path: "lib/core/widgets/ingredient_thumb.dart"
      provides: "reusable widget สำหรับ thumbnail ingredient"
    - path: "lib/core/nutrition/ingredients_models.dart"
      provides: "optional imagePath + bbox fields บนโหนด"
  key_links:
    - from: "ingredient_thumb.dart"
      to: "add_food_bottom_sheet / edit_food_bottom_sheet / gemini_analysis_sheet / create_meal_sheet"
      via: "import + ใช้ในแถว ingredient"
      pattern: "IngredientThumb(imagePath: node.imagePath)"
---

<objective>
แสดง **micro thumbnail** ข้างชื่อวัตถุดิบในแถว ingredient ที่มี path รูป + ขยาย schema v2 ให้รองรับ optional `imagePath`/bbox — ตาม `17-CONTEXT` D-10–D-14

Purpose: ARC2-THUMB-01
Output: widget `IngredientThumb` ใช้ได้ในแถว ingredient ทุกจุด, schema v2 backward compatible
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-merge-media/17-CONTEXT.md (D-10–D-14)
@.planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md
@lib/core/theme/app_tokens.dart (AppSpacing)
@lib/core/theme/app_colors.dart (AppColors)
</context>

<tasks>

<task type="auto">
  <name>Task 17-02-01: ขยาย schema v2 — optional imagePath + bbox</name>
  <read_first>
    - lib/core/nutrition/ingredients_models.dart (โครงสร้าง main/sub node — ดูทุกฟิลด์)
    - lib/core/nutrition/ingredients_codec.dart (parse / serialize — ดูว่า map key ชื่ออะไร)
    - lib/core/database/app_database.dart (ค้นหา "arBoundingBox" — ดูว่า FoodEntry เก็บ bbox อย่างไร)
  </read_first>
  <action>
1. เพิ่มฟิลด์ optional ในโมเดลโหนด ingredient:
   - `String? imagePath` — path รูปย่อ
   - `String? arBoundingBox` — JSON string ของ bbox (เช่น `{"x":0.1,"y":0.2,"w":0.3,"h":0.4}`)
   - `int? arImageWidth`, `int? arImageHeight` — ขนาดภาพต้นฉบับ
2. อัปเดต codec parse: อ่าน `imagePath`/bbox จาก JSON ถ้ามี — ถ้าไม่มีเป็น null (backward compatible)
3. อัปเดต codec serialize: เขียน `imagePath`/bbox ลง JSON เฉพาะเมื่อมีค่า (ไม่ใส่ null key)
4. ตรวจ: parse JSON เก่า (ไม่มีฟิลด์ใหม่) ต้องไม่ throw
  </action>
  <acceptance_criteria>
    - `rg "imagePath" lib/core/nutrition/ingredients_models.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "imagePath" lib/core/nutrition/ingredients_codec.dart` พบอย่างน้อย 2 บรรทัด (parse + serialize)
    - `flutter analyze lib/core/nutrition/` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 17-02-02: IngredientThumb widget</name>
  <read_first>
    - lib/core/theme/app_tokens.dart (AppSpacing ค่าที่ใช้ เช่น sm, md)
    - lib/core/theme/app_colors.dart (AppColors สำหรับ border/overlay)
  </read_first>
  <action>
สร้าง `lib/core/widgets/ingredient_thumb.dart`:

```dart
class IngredientThumb extends StatelessWidget {
  final String? imagePath;
  final double size; // default ~28 (ระหว่าง 24–36 ตาม D-11)
  final VoidCallback? onTap;
  // ...
}
```

Rules:
1. ถ้า `imagePath == null || imagePath!.isEmpty` → return `SizedBox.shrink()` (**ไม่มี placeholder** ตาม D-10)
2. ถ้ามี path → `GestureDetector` + `ClipRRect(borderRadius: AppRadius.xs)` + `Image.file(File(imagePath!), width: size, height: size, fit: BoxFit.cover)`
3. `errorBuilder` → `SizedBox.shrink()` (ไม่แสดงกล่องเทาถ้าไฟล์ไม่อยู่)
4. `onTap` ถ้าไม่ null → trigger full-screen preview (งาน 17-02-03)
5. ใช้สีจาก `AppColors` / theme เท่านั้น — ไม่ hardcode สี
  </action>
  <acceptance_criteria>
    - ไฟล์ `lib/core/widgets/ingredient_thumb.dart` มีอยู่
    - `rg "SizedBox.shrink" lib/core/widgets/ingredient_thumb.dart` พบอย่างน้อย 1 บรรทัด (กรณีไม่มีรูป)
    - `rg "Image.file\|Image\.file" lib/core/widgets/ingredient_thumb.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "errorBuilder" lib/core/widgets/ingredient_thumb.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/core/widgets/ingredient_thumb.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 17-02-03: Full-screen preview + bbox overlay</name>
  <read_first>
    - lib/core/database/app_database.dart (ค้นหา "BoundingBoxPainter" หรือ "arBoundingBox" — ดูว่ามี painter อยู่แล้วหรือไม่)
    - lib/features/sharing/widgets/share_card_food_item.dart (ค้นหา "BoundingBoxPainter" — ดู pattern overlay ที่มี)
    - lib/core/widgets/ingredient_thumb.dart (จากงาน 17-02-02)
  </read_first>
  <action>
1. สร้างฟังก์ชัน / static method เช่น `IngredientThumb.showFullScreen(context, imagePath, {arBoundingBox, arImageWidth, arImageHeight})` หรือ navigation ไป dialog:
   - แสดง `Dialog` / `MaterialPageRoute` fullscreen ด้วย `InteractiveViewer` + `Image.file`
   - ถ้ามี `arBoundingBox` + ขนาดภาพ → ใช้ `CustomPaint` หรือ `BoundingBoxPainter` ที่โปรเจกต์มีอยู่แล้ว (reuse จาก share_card_food_item ถ้าเป็นไปได้) วาด overlay
   - ถ้าไม่มี bbox → แสดงแค่รูป
2. ปุ่มปิด / gesture back
3. **ไม่** hardcode ข้อความ — ใช้ L10n สำหรับ tooltip/label ถ้ามี (เช่น "Close" → `l10n.close`)
  </action>
  <acceptance_criteria>
    - `rg "showFullScreen\|FullScreenImage\|_showImagePreview" lib/core/widgets/ingredient_thumb.dart` พบอย่างน้อย 1 บรรทัด (method ที่เปิดรูปเต็มจอ)
    - `rg "BoundingBox\|CustomPaint\|Rect" lib/core/widgets/ingredient_thumb.dart` พบอย่างน้อย 1 บรรทัด (overlay logic)
    - `flutter analyze lib/core/widgets/ingredient_thumb.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 17-02-04: ผูก IngredientThumb ในแถว ingredient sheets</name>
  <read_first>
    - lib/features/health/widgets/add_food_bottom_sheet.dart (ค้นหา "_EditableIngredient" — ดูว่าแถว ingredient แสดง UI อย่างไร, ชื่อ/ปริมาณ/kcal อยู่ตรงไหนในแถว)
    - lib/features/health/widgets/edit_food_bottom_sheet.dart (แถว ingredient เดียวกัน)
    - lib/features/health/widgets/gemini_analysis_sheet.dart (แถว ingredient)
    - lib/features/health/widgets/create_meal_sheet.dart (บรรทัด 825–970 แถว sub — ดู Row แรกของแต่ละ sub)
    - lib/core/widgets/ingredient_thumb.dart
  </read_first>
  <action>
ในแต่ละ sheet ที่แสดงแถว ingredient:
1. import `ingredient_thumb.dart`
2. เมื่อโหนด ingredient มี `imagePath` ไม่ null → แสดง `IngredientThumb(imagePath: node.imagePath, onTap: () => IngredientThumb.showFullScreen(context, node.imagePath, arBoundingBox: node.arBoundingBox, ...))` **ก่อนชื่อ** ในแถว (เช่น ใน Row ที่มี Autocomplete/TextField ชื่อ)
3. เมื่อไม่มี imagePath → `IngredientThumb` คืน `SizedBox.shrink()` — layout ไม่เปลี่ยน
4. สำหรับ `create_meal_sheet.dart`: แสดงใน Row แรกของ sub (บรรทัด ~920 ก่อน `Expanded(child: Autocomplete)`) — **ไม่ทำลาย** `_lookupSubIngredient` (D-17)
  </action>
  <acceptance_criteria>
    - `rg "IngredientThumb" lib/features/health/widgets/add_food_bottom_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "IngredientThumb" lib/features/health/widgets/edit_food_bottom_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "IngredientThumb" lib/features/health/widgets/gemini_analysis_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "IngredientThumb" lib/features/health/widgets/create_meal_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/health/widgets/add_food_bottom_sheet.dart lib/features/health/widgets/edit_food_bottom_sheet.dart lib/features/health/widgets/gemini_analysis_sheet.dart lib/features/health/widgets/create_meal_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze lib/core/widgets/ingredient_thumb.dart lib/core/nutrition/` exit code 0
- [ ] `flutter analyze` บน 4 sheet ที่แก้ exit code 0
- [ ] `flutter test test/core/nutrition/ingredients_codec_test.dart` ยังผ่าน (regression — schema change ต้อง backward compatible)
</verification>

<success_criteria>
- JSON v2 รองรับ optional imagePath + bbox บนโหนด, backward compatible
- IngredientThumb แสดงเมื่อมีรูป, ไม่แสดงเมื่อไม่มี
- แตะ thumb → เต็มจอ + overlay bbox เมื่อมีข้อมูล
- Layout ไม่แตกบนจอเล็ก
</success_criteria>

<output>
After completion, create `.planning/phases/17-merge-media/17-02-SUMMARY.md`
</output>
