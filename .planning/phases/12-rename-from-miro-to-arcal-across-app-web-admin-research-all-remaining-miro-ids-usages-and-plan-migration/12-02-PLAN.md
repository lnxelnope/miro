---
phase: 12-rename-from-miro-to-arcal-across-app-web-admin-research-all-remaining-miro-ids-usages-and-plan-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - lib/
  - web/
  - ios/
  - android/
  - assets/
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "ผู้ใช้ไม่เห็นคำว่า \"Miro\" บน UI ของ mobile app / web app / web splash / app icon name อีกต่อไป (แทนที่ด้วย ArCal หรือ copy ใหม่ตาม branding)"
    - "การเปลี่ยนชื่อทั้งหมดใน plan นี้จำกัดเฉพาะ user-facing surfaces เท่านั้น โดยไม่แตะ internal IDs/slug/namespace (เก็บไว้ให้ Plan 12-03 ตัดสินใจ)"
    - "มี test script หรือคำสั่งค้นหา (เช่น rg/grep) ที่ใช้ยืนยันว่าไม่มี user-facing string ของ \"Miro\" เหลืออยู่ใน codebase ที่ deploy จริง"
  artifacts:
    - path: "lib/"
      provides: "UI copy, ข้อความในแอป, title, hint, banner ที่เปลี่ยนเป็น ArCal ครบ"
    - path: "web/"
      provides: "web splash + meta + title ปรับ branding เป็น ArCal"
    - path: "ios/, android/"
      provides: "display name และ permission / privacy text user-facing ที่ยังเป็น Miro ถูกเปลี่ยน"
  key_links:
    - from: ".planning/phases/11-app-rebrand/11-02-PLAN.md"
      to: ".planning/phases/12-rename-from-miro-to-arcal-across-app-web-admin-research-all-remaining-miro-ids-usages-and-plan-migration/12-RESEARCH.md"
      via: "ต่อยอดจาก rebrand เดิมที่เน้น iOS/Android system-level มาสู่ทุก UI surface"
---

<objective>
เปลี่ยนทุก user-facing surface จาก "Miro" เป็น ArCal (หรือ copy ใหม่ตาม branding) ให้ครบ ทั้งใน mobile app, web, web splash และข้อความ permission/privacy โดยไม่แตะ internal IDs/slug

Purpose: ให้ผู้ใช้ไม่เห็นชื่อเดิมหลงเหลืออยู่เลยในประสบการณ์ใช้งานปัจจุบัน
Output: code และ assets ที่ deploy ได้ โดย UI ทั้งหมดแสดง ArCal แทน Miro และมี script ตรวจสอบเบื้องต้น
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-rename-from-miro-to-arcal-across-app-web-admin-research-all-remaining-miro-ids-usages-and-plan-migration/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ใช้ inventory จาก 12-RESEARCH.md แยก user-facing vs internal</name>
  <files>.planning/phases/12-rename-from-miro-to-arcal-across-app-web-admin-research-all-remaining-miro-ids-usages-and-plan-migration/12-RESEARCH.md</files>
  <action>
อ่าน 12-RESEARCH.md แล้วสร้าง section ย่อย:

## User-facing items for rename

ดึง row จากตารางที่ type = user-facing หรือ docs (ที่ผู้ใช้/ลูกค้าเห็น) แล้วคัดลอกมาเป็น checklist รายการ:
- [ ] {location} — {snippet} — {system}

อย่าใส่ internal ID/slug/namespace หรือ analytics key ใน section นี้ (เก็บไป Plan 12-03)
  </action>
  <verify>มีรายการ checklist user-facing ชัดเจนครอบคลุมทุกจุดที่ผู้ใช้เห็นคำว่า \"Miro\"</verify>
  <done>รู้ scope ชัดเจนว่าจะเปลี่ยนตรงไหนบ้างโดยไม่ไปโดน internal</done>
</task>

<task type="auto">
  <name>Task 2: อัปเดต UI copy ใน Flutter (lib/)</name>
  <files>lib/</files>
  <action>
สำหรับทุกจุดใน lib/ ที่ user-facing และยังมี \"Miro\":
- ปรับข้อความให้ใช้ ArCal หรือ \"ArCal - AI Calorie Counter\" ตาม context
- ตรวจว่าตาม rules i18n ของโปรเจกต์ (ARB + L10n) การเปลี่ยน string ทำในไฟล์ .arb + regenerate localization เท่านั้น (ห้าม hardcode ใน widget ใหม่)

ตัวอย่าง:
- Title / tagline / description ที่เคยกล่าวถึง Miro → ปรับเป็น ArCal พร้อม messaging ตาม branding ล่าสุด
- In-app help text / empty state / CTA ที่อ้างถึงชื่อแอป → เปลี่ยนเป็น ArCal
  </action>
  <verify>ค้นหาใน lib/ แล้วไม่พบ user-facing string \"Miro\" ที่ผู้ใช้จะเห็นใน UI จริง</verify>
  <done>UI ฝั่ง Flutter เปลี่ยน branding เสร็จสมบูรณ์</done>
</task>

<task type="auto">
  <name>Task 3: อัปเดต web splash + web UI</name>
  <files>web/</files>
  <action>
ตรวจไฟล์ใน web/:
- web/index.html
- web/splash/ และ asset ที่เกี่ยวข้อง

สำหรับทุกที่ที่แสดงชื่อแอป/คำอธิบาย:
- ปรับ title, meta, alt text, และข้อความใน splash/page ให้เป็น ArCal แทน Miro
- ถ้ามีโลโก้หรือข้อความในรูป (asset) ยังเป็น Miro ให้จดใน 12-RESEARCH.md และสร้าง task แยกสำหรับรีดีไซน์ asset (อาจอยู่นอก Phase 12 ถ้าใช้ดีไซน์เนอร์)
  </action>
  <verify>เปิด web build หรือดู index.html แล้วไม่เห็น \"Miro\" ใน title/meta/splash อีก</verify>
  <done>ฝั่ง web ที่ผู้ใช้เห็นถูก rebrand เป็น ArCal แล้ว</done>
</task>

<task type="auto">
  <name>Task 4: ทบทวน iOS/Android user-facing text ที่ยังเหลือ</name>
  <files>ios/, android/</files>
  <action>
ต่อยอดจาก Phase 11:
- ตรวจ Info.plist, InfoPlist.strings, Localizable.strings ฝั่ง iOS
- ตรวจ AndroidManifest.xml, strings.xml, resource อื่น ๆ ฝั่ง Android

ค้นหา \"Miro\":
- ถ้าเป็นชื่อ dialog, permission rationale, privacy description ที่ผู้ใช้เห็น → เปลี่ยนเป็น ArCal หรือข้อความใหม่ที่เหมาะสม
- ถ้าเป็น internal bundle id, package name, deep link host ฯลฯ ให้จดกลับไปที่ 12-RESEARCH.md (type = internal-id) แล้วไม่แตะใน plan นี้
  </action>
  <verify>บน device จริง permission/privacy dialog ทั้งหมดพูดถึง ArCal ไม่ใช่ Miro</verify>
  <done>system-level user-facing text ถูก sync กับ branding ใหม่ครบ</done>
</task>

<task type="auto">
  <name>Task 5: สร้าง script/คำสั่งตรวจ user-facing \"Miro\" ก่อน release</name>
  <files>.planning/phases/12-rename-from-miro-to-arcal-across-app-web-admin-research-all-remaining-miro-ids-usages-and-plan-migration/12-RESEARCH.md</files>
  <action>
ใน 12-RESEARCH.md หรือใน docs เพิ่ม section:

## Verification commands for user-facing rename

ใส่คำสั่งตัวอย่างที่ทีมใช้รันก่อนปล่อย release เช่น:
- `rg "Miro" lib/ ios/ android/ web/ -n`

และอธิบายว่า:
- ผลลัพธ์ที่เหลืออยู่ควรเป็น internal-only (เช่น package name, legacy ID) ซึ่งจะถูกจัดการใน Plan 12-03
- ถ้ามีผลลัพธ์ที่เป็น user-facing ให้ถือว่า plan นี้ยังไม่ complete
  </action>
  <verify>มีคำสั่งชัดเจนที่ใคร ๆ ก็รันเพื่อตรวจว่าไม่มี user-facing \"Miro\" เหลืออยู่</verify>
  <done>สามารถใช้เป็น gate manual/CI ก่อน release ได้</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ค้นหา \"Miro\" ใน lib/, web/, ios/, android/ แล้วผลลัพธ์ที่เหลือทั้งหมดเป็น internal-only ตามที่ 12-RESEARCH.md ระบุ
- [ ] บน device จริง (iOS/Android) ไม่มีข้อความบนหน้าจอใดแสดงชื่อ Miro กับผู้ใช้อีกต่อไป
- [ ] web/splash/title/meta ไม่มีคำว่า Miro
- [ ] มีคำสั่ง verification ที่สามารถรันซ้ำได้และเขียนไว้ใน docs/12-RESEARCH.md
</verification>

<success_criteria>
- ผู้ใช้ทุก touchpoint ปัจจุบันมองเห็นเฉพาะ ArCal ไม่ใช่ Miro
- ทีมสามารถมั่นใจได้ว่าการเปลี่ยน user-facing branding เสร็จสิ้น โดยยังไม่ไปแตะ internal ID/slug ใด ๆ
</success_criteria>

<output>
After completion, update `.planning/STATE.md` และ/หรือ SUMMARY ของ Phase 12 เพื่อบันทึกว่า user-facing rename เสร็จแล้ว เหลือ internal IDs/slug สำหรับ Plan 12-03
</output>

