---
phase: 11-app-rebrand
plan: 01
type: execute
wave: 1
depends_on:
  - "10-03"
files_modified:
  - pubspec.yaml
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
autonomous: true
requirements:
  - BRAND-01
user_setup: []
must_haves:
  truths:
    - "ชื่อแอปที่แสดงบน home screen ของทั้ง Android และ iOS เปลี่ยนเป็น ArCal - AI Calorie Counter"
  artifacts:
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "android:label สำหรับชื่อแอป ArCal - AI Calorie Counter"
    - path: "ios/Runner/Info.plist"
      provides: "CFBundleDisplayName เป็น ArCal - AI Calorie Counter"
  key_links: []
---

<objective>
เปลี่ยนชื่อแอปที่แสดงบน home screen จาก Miro/MIRO เป็น ArCal - AI Calorie Counter บนทั้ง Android และ iOS โดยไม่เปลี่ยน package name

Purpose: ให้ผู้ใช้เห็น branding ใหม่บนอุปกรณ์โดยไม่กระทบการอัปเดตจาก store
Output: config ชื่อแอปใหม่ใน pubspec/AndroidManifest/Info.plist
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ค้นหาการใช้งานชื่อแอปปัจจุบันในโปรเจกต์</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>ค้นหาข้อความ Miro/MIRO/ชื่อแอปเดิมในโปรเจกต์เพื่อระบุจุดที่ใช้เป็น display name เท่านั้น แยกจากส่วนที่เกี่ยวข้องกับ package id หรือ key อื่นที่ไม่ควรแตะ</action>
  <verify>ตรวจสอบผลค้นหาและยืนยันว่าจะไม่แตะ identifier ที่เกี่ยวข้องกับ package name/bundle id</verify>
  <done>มีรายการตำแหน่งที่ต้องเปลี่ยน display name ชัดเจน</done>
</task>

<task type="auto">
  <name>Task 2: ปรับชื่อแอปใน AndroidManifest และ Info.plist</name>
  <files>android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>แก้ไข android:label และ CFBundleDisplayName (หรือตัวแปรที่ใช้) ให้เป็น ArCal - AI Calorie Counter โดยตรวจสอบ encoding/locale ให้ถูกต้อง</action>
  <verify>build แอปและติดตั้งบนอุปกรณ์/จำลองอย่างน้อย 1 เครื่องต่อแพลตฟอร์ม เพื่อตรวจว่าชื่อบน home screen เปลี่ยนถูกต้อง</verify>
  <done>ชื่อแอปบน home screen เปลี่ยนเป็น ArCal - AI Calorie Counter บนทั้งสองแพลตฟอร์ม</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] แอปที่ติดตั้งใหม่มีชื่อ ArCal - AI Calorie Counter บน Android
- [ ] แอปที่ติดตั้งใหม่มีชื่อ ArCal - AI Calorie Counter บน iOS
</verification>

<success_criteria>

- Requirement BRAND-01 ถูกตอบโจทย์เต็มรูปแบบ
- ไม่มีการเปลี่ยน package name/bundle id
  </success_criteria>

<output>
After completion, create `.planning/phases/11-app-rebrand/11-01-SUMMARY.md`
</output>

