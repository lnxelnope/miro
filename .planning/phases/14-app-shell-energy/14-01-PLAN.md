---
phase: 14-app-shell-energy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/providers/app_mode_provider.dart
  - lib/features/home/presentation/home_screen.dart
  - lib/features/home/presentation/basic_mode_tab.dart
  - lib/features/health/presentation/health_timeline_tab.dart
autonomous: true
requirements:
  - ARC2-SHELL-01
  - ARC2-SHELL-02
  - ARC2-SHELL-03
  - ARC2-SHELL-04
  - ARC2-SHELL-05
user_setup: []
must_haves:
  truths:
    - "ผู้ใช้ไม่สามารถเห็น ModeToggle บน Home AppBar"
    - "แอปบังคับ AppMode.basic หลัง bootstrap แม้ prefs เคยเป็น pro"
    - "QuestBar ไม่อยู่ใน tree ของ BasicModeTab และ HealthTimelineTab ตามแผน"
    - "ช่องแชทด้านล่างใน BasicModeTab ถูกซ่อน (maintainState ได้ตาม CONTEXT)"
  artifacts:
    - path: "lib/core/providers/app_mode_provider.dart"
      provides: "migration / lock ไม่ให้โหลด pro เป็นค่าที่ใช้วาด shell"
    - path: "lib/features/home/presentation/home_screen.dart"
      provides: "ปุ่ม My Meal + ไม่มี ModeToggle"
  key_links:
    - from: "lib/features/home/presentation/home_screen.dart"
      to: "lib/features/health/presentation/health_my_meal_tab.dart"
      via: "MaterialPageRoute + Scaffold"
      pattern: "HealthMyMealTab"
---

<objective>
ล็อก shell เป็น Basic เท่านั้น: migrate `app_mode` prefs, เอา `ModeToggle` ออก, เพิ่มทางเข้า My Meal จาก AppBar (L10n), ซ่อน QuestBar บน basic home / timeline tab, ซ่อน chat input ใน `BasicModeTab` — ไม่ลบไฟล์ Chat/Pro

Purpose: ครอบคลุม ARC2-SHELL-01 ถึง ARC2-SHELL-05
Output: analyzer ผ่านสำหรับไฟล์ที่แตะ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-app-shell-energy/14-CONTEXT.md
@.planning/phases/14-app-shell-energy/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 14-01-01: บังคับ AppMode.basic ที่ provider</name>
  <read_first>
    - lib/core/providers/app_mode_provider.dart
    - .planning/phases/14-app-shell-energy/14-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
ใน `AppModeNotifier`:
1. ใน `_load()` ถ้าอ่านค่าได้เป็น `pro` ให้ตั้ง `state = AppMode.basic` และ `await prefs.setString(_key, 'basic')` (migrate ครั้งโหลด)
2. ป้องกันการสลับกลับไป pro: ทำให้ `toggle()` ไม่ตั้ง state เป็น `AppMode.pro` (เช่น no-op หรือบังคับ basic เสมอ); `setMode(AppMode.pro)` ไม่เปลี่ยนเป็น pro — บันทึก prefs เป็น `basic` เท่านั้นเมื่อมีการเรียก
3. คง `enum AppMode` และชื่อคลาสไว้เพื่อ dead code Pro
  </action>
  <acceptance_criteria>
    - `grep -n "AppMode.pro" lib/core/providers/app_mode_provider.dart` ยังพบได้ (enum) แต่ต้องมีตรรกะที่ทำให้ `state` ไม่ค้างเป็น pro หลัง `_load` — ตรวจด้วยการอ่านไฟล์ว่ามีการ set `basic` เมื่อ prefs เป็น pro
    - `flutter analyze lib/core/providers/app_mode_provider.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14-01-02: HomeScreen — เอา ModeToggle, เพิ่ม My Meal</name>
  <read_first>
    - lib/features/home/presentation/home_screen.dart
    - lib/features/health/presentation/health_my_meal_tab.dart (signature constructor)
    - lib/l10n/app_localizations.dart หรือ ARB: `navMyMeals`, `appBarMyMeals`
  </read_first>
  <action>
1. ลบ `const ModeToggle()` ออกจาก `actions` และลบ import `mode_toggle.dart` ถ้าไม่ใช้แล้ว
2. เพิ่ม `IconButton` (แนะนำ `Icons.restaurant_menu`) **ก่อน** ปุ่ม Profile — `onPressed`: `Navigator.push` ไป route ที่มี `Scaffold` + `AppBar` title = `L10n.of(context)!.appBarMyMeals` (หรือ `navMyMeals`), `leading` ปุ่มกลับ, `body: const HealthMyMealTab()` (หรือ wrap ตามที่ต้องการ Provider scope)
3. `tooltip` ใช้ L10n เดียวกับ title หรือ `navMyMeals` — ห้าม hardcode ภาษาไทย/อังกฤษ
4. (ทางเลือก) ทำความสะอาด `_buildAppBar` ให้ title ไม่พึ่งสาขา Pro ของ `_currentIndex` เมื่อโหมดถูกบังคับเป็น basic — อย่างน้อยต้องคอมไพล์ได้และแสดง title สำหรับวันนี้ได้
  </action>
  <acceptance_criteria>
    - `grep -n "ModeToggle" lib/features/home/presentation/home_screen.dart` ไม่พบผลลัพธ์
    - `grep -n "HealthMyMealTab" lib/features/home/presentation/home_screen.dart` พบอย่างน้อย 1 บรรทัด
    - `grep -n "restaurant_menu\\|MyMeal" lib/features/home/presentation/home_screen.dart` พบอย่างน้อย 1 บรรทัดที่เกี่ยวกับปุ่มหรือ route
    - `flutter analyze lib/features/home/presentation/home_screen.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14-01-03: BasicModeTab — ซ่อน QuestBar และ chat input</name>
  <read_first>
    - lib/features/home/presentation/basic_mode_tab.dart
    - .planning/phases/14-app-shell-energy/14-CONTEXT.md (D-07, D-11)
  </read_first>
  <action>
1. ลบหรือแทนที่ `SliverToBoxAdapter(child: QuestBar())` ด้วย `SliverToBoxAdapter(child: SizedBox.shrink())` หรือไม่ใส่ sliver นั้น — ห้ามแสดง `QuestBar` บน basic home
2. ซ่อน `_buildChatInput` — ใช้ `Visibility(visible: false, maintainState: true, child: _buildChatInput(l10n))` หรือ comment ออกจาก tree แต่เก็บเมธอด `_buildChatInput` ไว้ในไฟล์ตาม D-07
  </action>
  <acceptance_criteria>
    - `grep -n "QuestBar()" lib/features/home/presentation/basic_mode_tab.dart` ไม่พบผลลัพธ์
    - `flutter analyze lib/features/home/presentation/basic_mode_tab.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14-01-04: HealthTimelineTab — ซ่อน QuestBar</name>
  <read_first>
    - lib/features/health/presentation/health_timeline_tab.dart (บริเวณ QuestBar)
    - .planning/phases/14-app-shell-energy/14-CONTEXT.md (D-11)
  </read_first>
  <action>
ลบหรือแทนที่ widget `QuestBar()` ในไฟล์นี้ด้วย `SizedBox.shrink()` / ไม่ใส่ใน tree เพื่อไม่ให้แสดงเมื่อ widget ถูกใช้ในอนาคตหรือในโค้ดที่เหลือ
  </action>
  <acceptance_criteria>
    - `grep -n "QuestBar()" lib/features/health/presentation/health_timeline_tab.dart` ไม่พบผลลัพธ์
    - `flutter analyze lib/features/health/presentation/health_timeline_tab.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze lib/core/providers/app_mode_provider.dart lib/features/home/presentation/home_screen.dart lib/features/home/presentation/basic_mode_tab.dart lib/features/health/presentation/health_timeline_tab.dart` exit code 0
- [ ] `rg "ModeToggle" lib/features/home/presentation/home_screen.dart` ไม่มีผลลัพธ์
</verification>

<success_criteria>
- ตรงกับ ROADMAP success ข้อ 1–3 (ส่วน shell + QuestBar)
- ไม่ลบ `ChatScreen` / โฟลเดอร์ chat
</success_criteria>

<output>
After completion, create `.planning/phases/14-app-shell-energy/14-01-SUMMARY.md`
</output>
