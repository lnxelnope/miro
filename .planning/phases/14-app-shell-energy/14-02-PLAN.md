---
phase: 14-app-shell-energy
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - lib/features/energy/presentation/energy_store_screen.dart
  - .planning/phases/14-app-shell-energy/14-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ARC2-ENERGY-01
user_setup: []
must_haves:
  truths:
    - "ไม่มีการเรียก claimFreeEnergyEndpoint ใน lib/"
    - "ไม่มีเมธอด _claimFreeEnergy และ state _isClaimingFreeEnergy"
    - "_buildFreepassOfferCard / _claimFreepass ยังอยู่และคอมไพล์ได้"
  artifacts:
    - path: "lib/features/energy/presentation/energy_store_screen.dart"
      provides: "ถอด free_energy offer path ออกจาก Store"
    - path: ".planning/phases/14-app-shell-energy/14-MANUAL-CHECKLIST.md"
      provides: "D-17 manual verification"
  key_links:
    - from: "lib/features/energy/presentation/energy_store_screen.dart"
      to: "rewardType switch ใน _buildOfferCards"
      via: "ลบ case free_energy"
      pattern: "freepass"
---

<objective>
ถอด free energy legacy จาก Energy Store: ลบการ์ด/เมธอด/endpoint ที่เกี่ยวกับ `claimFreeEnergy` คง free pass และ offer อื่นที่ไม่ผูก legacy — พร้อมเอกสาร manual checklist ปิดเฟส

Purpose: ARC2-ENERGY-01 + ROADMAP success ข้อ 4
Output: grep `claimFreeEnergy` ใน lib → 0 รายการ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-app-shell-energy/14-CONTEXT.md (D-13–D-16)
@.planning/phases/14-app-shell-energy/14-RESEARCH.md (§5)
</context>

<tasks>

<task type="auto">
  <name>Task 14-02-01: ถอด free_energy จาก EnergyStoreScreen</name>
  <read_first>
    - lib/features/energy/presentation/energy_store_screen.dart (บรรทัด 450–900 โดยประมาณ)
    - .planning/phases/14-app-shell-energy/14-CONTEXT.md (D-13, D-14, D-15)
  </read_first>
  <action>
1. ใน `_buildOfferCards` ลบ `case 'free_energy':` และไม่สร้างการ์ดสำหรับ offer นั้น — หรือ `continue` เมื่อ `rewardType == 'free_energy'` ก่อน switch
2. ลบเมธอดทั้งก้อน `_buildFreeEnergyCard`
3. ลบ `_claimFreeEnergy` และตัวแปร state `_isClaimingFreeEnergy` และทุก reference
4. ลบ import `http` ถ้าใช้เฉพาะ free energy แล้วไม่เหลือการใช้ — **ถ้า** `http` ยังใช้ที่อื่นในไฟล์ ให้เก็บ import
5. ตรวจ `_inferRewardType` และส่วนอื่นว่าไม่สร้าง path ไป free energy โดยไม่ตั้งใจ
  </action>
  <acceptance_criteria>
    - `rg "claimFreeEnergy" lib/` ไม่มีผลลัพธ์ (รันในรากโปรเจกต์)
    - `rg "claimFreeEnergyEndpoint" lib/` ไม่มีผลลัพธ์
    - `rg "_buildFreeEnergyCard" lib/features/energy/presentation/energy_store_screen.dart` ไม่มีผลลัพธ์
    - `grep -n "_claimFreepass\\|_buildFreepassOfferCard" lib/features/energy/presentation/energy_store_screen.dart` พบอย่างน้อย 1 บรรทัดต่อชื่อ (ยืนยันยังมี free pass)
    - `flutter analyze lib/features/energy/presentation/energy_store_screen.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14-02-02: 14-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/14-app-shell-energy/14-CONTEXT.md (D-17)
  </read_first>
  <action>
สร้าง `.planning/phases/14-app-shell-energy/14-MANUAL-CHECKLIST.md` ภาษาไทย รายการตรวจมือ:
1. ไม่มี ModeToggle บน AppBar
2. แตะ My Meal → เปิดหน้า → กลับได้
3. หน้าหลัก basic ไม่มี QuestBar
4. Energy Store ไม่มีการรับ free energy legacy; free pass ยังทดสอบได้
5. ช่องแชทด้านล่างไม่ปรากฏบน basic home
  </action>
  <acceptance_criteria>
    - ไฟล์ `14-MANUAL-CHECKLIST.md` มีหัวข้อ "## Manual checklist" หรือเทียบเท่า
    - `grep -n "free pass\\|Free pass\\|freepass" .planning/phases/14-app-shell-energy/14-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
    - `grep -n "QuestBar\\|ModeToggle" .planning/phases/14-app-shell-energy/14-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัดรวม
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14-02-03: Regression — flutter test</name>
  <read_first>
    - test/integration/quest_bar_test.dart (บรรทัดต้น ๆ — package import)
  </read_first>
  <action>
รัน `flutter test` จากรากโปรเจกต์ ถ้ามีเทสต์ fail ที่เกี่ยวกับ QuestBar/Home ให้แก้เทสต์หรือ mock ให้สอดคล้องการซ่อน QuestBar จาก home (ไม่บังคับแก้ production ย้อนกลับ)
  </action>
  <acceptance_criteria>
    - บันทึกผลลัพธ์ `flutter test` ใน `14-02-SUMMARY.md` (pass หรือรายการ fail ที่ยอมรับชั่วคราว)
    - ถ้า full suite green: `flutter test` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rg "claimFreeEnergy" lib/` ว่าง
- [ ] `flutter analyze lib/features/energy/presentation/energy_store_screen.dart` ผ่าน
</verification>

<success_criteria>
- ROADMAP success ข้อ 4
- Free pass flow ยังมีโค้ด path ในไฟล์เดิม
</success_criteria>

<output>
After completion, create `.planning/phases/14-app-shell-energy/14-02-SUMMARY.md`
</output>
