---
phase: 18-share-promo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/sharing/models/share_card_config.dart
  - lib/features/sharing/services/card_capture_service.dart
  - lib/features/sharing/presentation/share_card_creator_screen.dart
  - lib/features/sharing/widgets/share_card_food_item.dart
  - lib/features/sharing/widgets/share_card_daily_summary.dart
  - lib/features/sharing/widgets/share_card_nutrition_summary.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_th.arb
autonomous: true
requirements:
  - ARC2-SHARE-01
user_setup:
  - "flutter gen-l10n หลังแก้ ARB"
must_haves:
  truths:
    - "ShareCardConfig มี ShareCardAspect enum (ratio16x9, ratio1x1) และ showServingOnImage field"
    - "ทั้งสาม widget การ์ดแสดงภายในกรอบ aspect ที่เลือก"
    - "toggle showServingOnImage แสดงเฉพาะ ShareCardType.foodItem"
    - "ไม่มี hardcode ข้อความผู้ใช้"
  artifacts:
    - path: "lib/features/sharing/models/share_card_config.dart"
      provides: "ShareCardAspect enum + showServingOnImage + copyWith"
    - path: "lib/features/sharing/presentation/share_card_creator_screen.dart"
      provides: "UI เลือก aspect + toggle (food only)"
  key_links:
    - from: "share_card_config.dart"
      to: "share_card_food_item.dart / daily_summary / nutrition_summary"
      via: "config.aspect"
      pattern: "AspectRatio หรือ SizedBox ตาม preset"
---

<objective>
เพิ่ม preset **16:9** / **1:1** สำหรับทุกประเภทการ์ด + toggle **แสดงปริมาณบนภาพ** เฉพาะ food item card — ตาม `18-CONTEXT` D-01–D-05

Purpose: ARC2-SHARE-01
Output: export 16:9/1:1 ได้ทุกประเภทการ์ด + food card มี toggle ปริมาณ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-share-promo/18-CONTEXT.md (D-01–D-05)
@lib/features/sharing/models/share_card_config.dart
@lib/features/sharing/presentation/share_card_creator_screen.dart
@.cursorrules (L10n, UnitConverter)
</context>

<tasks>

<task type="auto">
  <name>Task 18-01-01: Model — ShareCardAspect + showServingOnImage</name>
  <read_first>
    - lib/features/sharing/models/share_card_config.dart (ทั้งไฟล์ — enum ShareCardType, constructor, copyWith, fields ทั้งหมด)
  </read_first>
  <action>
1. เพิ่ม enum ก่อน class:
   ```dart
   enum ShareCardAspect { ratio16x9, ratio1x1 }
   ```
2. เพิ่มฟิลด์ใน `ShareCardConfig`:
   - `final ShareCardAspect aspect;` (default `ShareCardAspect.ratio16x9`)
   - `final bool showServingOnImage;` (default `false`)
3. อัปเดต constructor: `this.aspect = ShareCardAspect.ratio16x9`, `this.showServingOnImage = false`
4. อัปเดต `copyWith`: รองรับ `ShareCardAspect? aspect`, `bool? showServingOnImage`
5. เพิ่ม getter ช่วย:
   - `double get aspectRatio` → `ratio16x9` = 16/9, `ratio1x1` = 1.0
   - `Size get logicalSize` → `ratio16x9` = Size(360, 202.5), `ratio1x1` = Size(360, 360) (หรือขนาดที่สอดคล้องกับ capture คุณภาพสูง)
  </action>
  <acceptance_criteria>
    - `rg "ShareCardAspect" lib/features/sharing/models/share_card_config.dart` พบอย่างน้อย 3 บรรทัด (enum + field + copyWith)
    - `rg "showServingOnImage" lib/features/sharing/models/share_card_config.dart` พบอย่างน้อย 3 บรรทัด
    - `rg "aspectRatio\|logicalSize" lib/features/sharing/models/share_card_config.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/sharing/models/share_card_config.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 18-01-02: L10n keys</name>
  <read_first>
    - lib/l10n/app_en.arb (ค้นหา "share\|card" — ดู key ที่มี)
  </read_first>
  <action>
เพิ่ม keys:
- `aspectRatio16x9`: "16:9 Wide" / "16:9 กว้าง"
- `aspectRatio1x1`: "1:1 Square" / "1:1 สี่เหลี่ยม"
- `aspectRatioLabel`: "Aspect ratio" / "อัตราส่วนภาพ"
- `showServingToggle`: "Show serving size" / "แสดงปริมาณ"
- `servingDisplay`: "{size} {unit}" / "{size} {unit}" (สำหรับแสดงบนการ์ด)
- `noServingData`: "No serving data" / "ไม่มีข้อมูลปริมาณ" (disable toggle)
รัน `flutter gen-l10n`
  </action>
  <acceptance_criteria>
    - `rg "aspectRatio16x9\|showServingToggle" lib/l10n/app_en.arb` พบอย่างน้อย 2 บรรทัด
    - `flutter gen-l10n` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 18-01-03: Layout การ์ดสามแบบตาม aspect</name>
  <read_first>
    - lib/features/sharing/widgets/share_card_food_item.dart (ทั้งไฟล์ — ดูขนาดปัจจุบัน SizedBox 1080/3 × 1350/3, layout Stack)
    - lib/features/sharing/widgets/share_card_daily_summary.dart (โครงสร้างและขนาด)
    - lib/features/sharing/widgets/share_card_nutrition_summary.dart (โครงสร้างและขนาด)
  </read_first>
  <action>
ทั้ง 3 widget:
1. อ่าน `config.aspect` แล้วตั้ง `SizedBox` ขนาดตาม `config.logicalSize` (แทนที่ขนาดคงที่เดิม)
2. ปรับ layout ภายใน (gradient, text position, spacing) ให้ไม่ถูก crop ผิดทั้ง 16:9 และ 1:1
3. **`ShareCardFoodItem` เท่านั้น:** ถ้า `config.showServingOnImage == true` → แสดงบรรทัดปริมาณ:
   - ข้อความ: `"${entry.servingSize} ${UnitConverter.displayLabel(entry.servingUnit, context)}"` หรือ L10n `servingDisplay`
   - ตำแหน่ง: ใต้ชื่ออาหาร หรือตำแหน่งที่ไม่บังข้อมูลสำคัญ
   - สไตล์: สีขาวกับ shadow เพื่ออ่านบนรูป (ใช้ theme ที่มี)
  </action>
  <acceptance_criteria>
    - `rg "config.aspect\|logicalSize\|aspectRatio" lib/features/sharing/widgets/share_card_food_item.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "config.aspect\|logicalSize\|aspectRatio" lib/features/sharing/widgets/share_card_daily_summary.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "config.aspect\|logicalSize\|aspectRatio" lib/features/sharing/widgets/share_card_nutrition_summary.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "showServingOnImage" lib/features/sharing/widgets/share_card_food_item.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/sharing/widgets/` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 18-01-04: Creator screen — ตัวเลือก aspect + toggle</name>
  <read_first>
    - lib/features/sharing/presentation/share_card_creator_screen.dart (บรรทัด 16–46 state, _config, toggles ที่มี เช่น showCalories/showMacros — ดู UI pattern ที่ใช้)
  </read_first>
  <action>
1. เพิ่มส่วนเลือก aspect (เช่น `SegmentedButton` หรือ `ChoiceChip`):
   - แสดง 2 ตัวเลือก: 16:9 / 1:1
   - `onSelectionChanged` → `setState(() => _config = _config.copyWith(aspect: selected))`
   - ตำแหน่ง: **เหนือ** toggles โภชนาการที่มีอยู่
2. เพิ่ม toggle `showServingOnImage`:
   - **แสดงเฉพาะ** `_config.type == ShareCardType.foodItem`
   - ถ้า entry ไม่มี serving data (servingSize null/0) → disable + ข้อความ L10n `noServingData`
   - `onChanged` → `setState(() => _config = _config.copyWith(showServingOnImage: value))`
3. ป้าย/label ใช้ L10n ทั้งหมด
  </action>
  <acceptance_criteria>
    - `rg "ShareCardAspect\|SegmentedButton\|ChoiceChip" lib/features/sharing/presentation/share_card_creator_screen.dart` พบอย่างน้อย 1 บรรทัด
    - `rg "showServingOnImage" lib/features/sharing/presentation/share_card_creator_screen.dart` พบอย่างน้อย 2 บรรทัด (toggle + config update)
    - `rg "foodItem" lib/features/sharing/presentation/share_card_creator_screen.dart` พบการเช็ค type สำหรับ toggle
    - `flutter analyze lib/features/sharing/presentation/share_card_creator_screen.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze lib/features/sharing/` exit code 0
- [ ] `flutter gen-l10n` exit code 0
</verification>

<success_criteria>
- ทุกประเภทการ์ด export 16:9 และ 1:1 ได้
- food card มี toggle ปริมาณ on/off
- daily / nutrition ไม่มี toggle ปริมาณ
- ไม่มี hardcode ข้อความ
</success_criteria>

<output>
After completion, create `18-01-SUMMARY.md`
</output>
