# Phase 18 — Plan 01 summary (share card)

- เพิ่ม `ShareCardAspect` (16:9, 1:1), `showServingOnImage`, scaling ผ่าน `logicalSize` / `layoutScale` ใน `share_card_config.dart`
- ปรับ `share_card_food_item`, `share_card_daily_summary`, `share_card_nutrition_summary` และ `share_card_creator_screen` (SegmentedButton aspect, toggle serving สำหรับ food item, L10n)
- เพิ่ม/ใช้ keys ใน `app_en.arb` / `app_th.arb` สำหรับ aspect, serving, streak/goal toggles
