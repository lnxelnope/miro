---
phase: 13-ingredients-schema-recompute
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01-PLAN.md
files_modified:
  - test/core/nutrition/ingredients_codec_test.dart
  - .planning/phases/13-ingredients-schema-recompute/13-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ARC2-DATA-01
  - ARC2-DATA-02
  - ARC2-DATA-03
user_setup: []
must_haves:
  truths:
    - "flutter test test/core/nutrition/ingredients_codec_test.dart ผ่านทั้งหมด"
    - "มีเทสต์ flatten ความลึก ≥3 และ assert โครงสร้างเหลือ 2 ระดับ"
    - "มีเทสต์ recompute macro + เคส micro ไม่ครบคงค่าเดิม"
  artifacts:
    - path: "test/core/nutrition/ingredients_codec_test.dart"
      provides: "หลักฐานอัตโนมัติสำหรับ D-11 ใน 13-CONTEXT"
    - path: ".planning/phases/13-ingredients-schema-recompute/13-MANUAL-CHECKLIST.md"
      provides: "เช็กลิสต์มือสำหรับ UI path หลัง Phase 16"
  key_links:
    - from: "test/core/nutrition/ingredients_codec_test.dart"
      to: "lib/core/nutrition/ingredients_codec.dart"
      via: "import + เรียกฟังก์ชันสาธารณะ"
      pattern: "parseIngredientsJson, serializeIngredientsV2, flattenAiIngredientRoots, recompute"
---

<objective>
เพิ่ม unit tests ครอบคลุม legacy read, v2 round-trip, recompute ตัวเลข macro, flatten ลึก, และกฎ D-08b — พร้อมเอกสาร manual checklist สำหรับการยืนยันในแอปเมื่อ Phase 16 wire UI

Purpose: ปิด ROADMAP success criteria เฟส 13 ข้อ 1–3 ด้วยหลักฐานที่ตรวจได้
Output: `ingredients_codec_test.dart` green + `13-MANUAL-CHECKLIST.md`
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md (D-11)
@.planning/phases/13-ingredients-schema-recompute/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 13-02-01: สร้างไฟล์เทสต์ + legacy / v2 round-trip</name>
  <read_first>
    - lib/core/nutrition/ingredients_codec.dart
    - lib/core/nutrition/ingredients_models.dart
  </read_first>
  <action>
สร้าง `test/core/nutrition/ingredients_codec_test.dart` พร้อมเทสต์อย่างน้อย:
1. `parseIngredientsJson(null)` และ `''` → kind empty ไม่ throw
2. สตริง JSON ที่เป็น List หนึ่งรายการ (รูปแบบเดียวกับ legacy toMap) → parse ได้ และ serialize v2 แล้วได้ string ที่มี `"schemaVersion":2` (หรือ schemaVersion ไม่มี space ตาม jsonEncode)
3. parse สตริง v2 ที่ serialize ออกมา → round-trip ได้ค่า macro เดิมที่ระดับ entry roll-up หรือจำนวน main เท่าเดิม
  </action>
  <acceptance_criteria>
    - ไฟล์ `test/core/nutrition/ingredients_codec_test.dart` มีอยู่
    - `flutter test test/core/nutrition/ingredients_codec_test.dart` exit code 0
    - `grep -n "schemaVersion" test/core/nutrition/ingredients_codec_test.dart` พบอย่างน้อย 1 บรรทัด
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-02-02: เทสต์ recompute macro + D-08b micro preserve</name>
  <read_first>
    - lib/core/nutrition/ingredients_codec.dart (signature recompute / rollup)
    - .planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md (D-08b)
  </read_first>
  <action>
เพิ่มเทสต์:
- main หนึ่งรายการมี sub สองรายการด้วยค่า macro คงที่ → rollup main และ entry ตรงกับผลบวกมือ
- เคส fiber/sugar/sodium: บาง sub ไม่มีฟิลด์ micro → ตรวจว่าค่าที่คืนจาก recompute **เท่ากับ** entryFiber/entrySugar/entrySodium ที่ส่งเข้า (preserve)
- เคสทุก sub มี fiber → ผลรวมตรงกับผลบวก
  </action>
  <acceptance_criteria>
    - `grep -n "fiber\\|sodium\\|sugar" test/core/nutrition/ingredients_codec_test.dart` พบอย่างน้อย 3 บรรทัดรวม
    - `flutter test test/core/nutrition/ingredients_codec_test.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-02-03: เทสต์ flatten ความลึก ≥ 3</name>
  <read_first>
    - .planning/phases/13-ingredients-schema-recompute/13-RESEARCH.md (§4)
    - lib/core/nutrition/ingredients_codec.dart (flatten)
  </read_first>
  <action>
สร้าง `List<Map<String,dynamic>>` จำลอง AI: root → sub → sub (อย่างน้อย 3 ระดับ) เรียก `flattenAiIngredientRoots` แล้ว assert:
- ไม่มี sub ที่ยังมี sub ซ้อน (หรือข้อมูลถูก hoist ครบ)
- ผลรวม calories ของทุกโหนด “ใบไม้” ก่อนและหลัง flatten เท่ากันภายใน tolerance ที่กำหนดในเทสต์ (เช่น 0.01) **หรือ** ไม่เกินผลรวม root ต้นทาง (เลือก invariant หนึ่งที่สอดคล้อง implementation และเขียนใน assert)
  </action>
  <acceptance_criteria>
    - เทสต์ชื่อหรือกลุ่มเทสต์มีคำว่า `flatten` ในไฟล์: `grep -n "flatten" test/core/nutrition/ingredients_codec_test.dart` พบอย่างน้อย 2 บรรทัด
    - `flutter test test/core/nutrition/ingredients_codec_test.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-02-04: 13-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md (ขอบเขต Phase 16)
  </read_first>
  <action>
สร้าง `.planning/phases/13-ingredients-schema-recompute/13-MANUAL-CHECKLIST.md` รายการสั้น ๆ ภาษาไทย:
- หลัง Phase 16: เปิด entry ที่มี legacy ingredientsJson → ไม่ crash
- หลัง Phase 16: บันทึก entry ที่มี sub → entry macro ตรงผลรวม
- อ้างอิงคำสั่ง `flutter test test/core/nutrition/ingredients_codec_test.dart` สำหรับ regression
  </action>
  <acceptance_criteria>
    - ไฟล์ `13-MANUAL-CHECKLIST.md` มีหัวข้อ `## Manual checklist` หรือเทียบเท่า
    - `grep -n "Phase 16" .planning/phases/13-ingredients-schema-recompute/13-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
    - `grep -n "flutter test" .planning/phases/13-ingredients-schema-recompute/13-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter test test/core/nutrition/ingredients_codec_test.dart` exit code 0
- [ ] `flutter test` ไม่มี failure จากไฟล์ใหม่ (ถ้าโปรเจกต์มีเทสต์อื่น red อยู่แล้ว — บันทึกใน SUMMARY)
</verification>

<success_criteria>
- ครบ D-11 ใน 13-CONTEXT ผ่านเทสต์อัตโนมัติและ/หรือ checklist
- ARC2-DATA-01/02/03 มีหลักฐานใน repo
</success_criteria>

<output>
After completion, create `.planning/phases/13-ingredients-schema-recompute/13-02-SUMMARY.md`
</output>
