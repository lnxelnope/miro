---
phase: 13-ingredients-schema-recompute
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/nutrition/ingredients_codec.dart
  - lib/core/nutrition/ingredients_models.dart
autonomous: true
requirements:
  - ARC2-DATA-01
  - ARC2-DATA-02
  - ARC2-DATA-03
user_setup: []
must_haves:
  truths:
    - "parseIngredientsJson อ่าน legacy List และ v2 Map (schemaVersion==2) ได้โดยไม่ throw"
    - "serializeIngredientsV2 ส่งออก JSON ที่มีคีย์ schemaVersion และ mainIngredients"
    - "recomputeEntryTotals คำนวณ macro ระดับ entry จาก sub→main roll-up และคง micro ตามกฎ D-08b เมื่อข้อมูลไม่ครบ"
    - "flattenAiIngredients ลดความลึกเหลือ 2 ระดับและมีกลยุทธ์ไม่ double-count ตาม 13-RESEARCH.md"
  artifacts:
    - path: "lib/core/nutrition/ingredients_models.dart"
      provides: "immutable/plain types สำหรับ main/sub node และ IngredientsDocumentV2"
    - path: "lib/core/nutrition/ingredients_codec.dart"
      provides: "parse, serialize v2, flatten, recomputeEntryTotals/patch"
  key_links:
    - from: "lib/core/nutrition/ingredients_codec.dart"
      to: "lib/features/health/widgets/add_food_bottom_sheet.dart"
      via: "field names อ้างอิง toMap() legacy"
      pattern: "name, name_en, amount, unit, calories, protein, carbs, fat, sub_ingredients"
---

<objective>
สร้างเลเยอร์กลาง `lib/core/nutrition/` สำหรับ `ingredientsJson`: แยก legacy list vs v2 object, encode v2, flatten โครงสร้าง AI ลึกเกิน 2 ระดับ, และ recompute macro (micro ตาม D-08b) — **ไม่**แก้ Add/Edit/Gemini UI ในแผนนี้ (Phase 16)

Purpose: ครอบคลุม ARC2-DATA-01/02/03 ที่ระดับไลบรารี
Output: `ingredients_models.dart` + `ingredients_codec.dart` คอมไพล์ผ่าน analyzer
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md
@.planning/phases/13-ingredients-schema-recompute/13-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 13-01-01: โมเดลข้อมูล v2 + legacy view</name>
  <read_first>
    - lib/features/health/widgets/add_food_bottom_sheet.dart (บรรทัด 21–110 class _EditableIngredient และ toMap)
    - .planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md (D-10, D-06, D-09)
  </read_first>
  <action>
สร้าง `lib/core/nutrition/ingredients_models.dart` ที่มีอย่างน้อย:
- คลาส/typedef สำหรับโหนด ingredient (main/sub) ที่มีฟิลด์: name, nameEn, amount, unit, calories, protein, carbs, fat, fiber/sugar/sodium เป็น optional double? ถ้าต้องใช้ roll-up micro
- `IngredientsDocumentV2` ที่มี `int schemaVersion` (คงที่ 2), `List` ของ main nodes แต่ละตัวมี `List` sub nodes
- enum เช่น `IngredientsJsonKind { empty, legacyList, version2, invalidJson }` สำหรับผล parse
  </action>
  <acceptance_criteria>
    - ไฟล์ `lib/core/nutrition/ingredients_models.dart` มีอยู่จริง
    - `grep -n "schemaVersion" lib/core/nutrition/ingredients_models.dart` ต้องพบอย่างน้อย 1 บรรทัด หรือ schema ถูกกำหนดใน codec แต่ต้องมีคำว่า `IngredientsDocumentV2` ในไฟล์ models
    - `flutter analyze lib/core/nutrition/ingredients_models.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-01-02: parse + serialize + legacy→v2 conversion</name>
  <read_first>
    - lib/core/nutrition/ingredients_models.dart
    - .planning/phases/13-ingredients-schema-recompute/13-RESEARCH.md (§2 Current legacy shape)
  </read_first>
  <action>
สร้าง `lib/core/nutrition/ingredients_codec.dart` พร้อมฟังก์ชันสาธารณะอย่างน้อย:
- `IngredientsParseResult parseIngredientsJson(String? raw)` — `raw == null` หรือ empty → kind empty; `jsonDecode` fail → invalidJson ไม่ throw ออกนอก; `List` → legacyList พร้อมแปลงเป็น `IngredientsDocumentV2` ชั่วคราวหรือเก็บ list ดิบตามดีไซน์; `Map` ที่ `schemaVersion==2` → version2
- `String serializeIngredientsV2(IngredientsDocumentV2 doc)` — ผลลัพธ์ `jsonEncode` ต้องมี substring `"schemaVersion"` และ `"mainIngredients"` (camelCase ตามที่ตกลงใน RESEARCH)
- ฟังก์ชันช่วย `IngredientsDocumentV2 legacyListToV2(List&lt;dynamic&gt; list)` แปลงแต่ละ element map เป็น main โดย map `sub_ingredients` → sub list
  </action>
  <acceptance_criteria>
    - `lib/core/nutrition/ingredients_codec.dart` มี `parseIngredientsJson` และ `serializeIngredientsV2`
    - `grep -E "parseIngredientsJson|serializeIngredientsV2" lib/core/nutrition/ingredients_codec.dart` พบทั้งสองชื่อ
    - `flutter analyze lib/core/nutrition/ingredients_codec.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-01-03: flattenAiIngredients (depth ≤ 2, no double-count)</name>
  <read_first>
    - .planning/phases/13-ingredients-schema-recompute/13-RESEARCH.md (§4 Flatten)
    - lib/core/ai/gemini_service.dart (ค้นหา ingredients_detail / sub_ingredients ตัวอย่างโครงสร้าง — อ่านบริบทเท่านั้น)
  </read_first>
  <action>
ใน `ingredients_codec.dart` (หรือไฟล์แยก import ได้) ใส่ฟังก์ชันเช่น `IngredientsDocumentV2 flattenAiIngredientRoots(List&lt;Map&lt;String,dynamic&gt;&gt; roots)` ที่:
- แต่ละ root → หนึ่ง main
- ถ้า sub มี sub ซ้อน ให้ hoist ตามกลยุทธ์ใน RESEARCH (ชื่อ composite + แบ่ง macro ตามสัดส่วนแคลอรี่ลูก)
- ผลลัพธ์ทุก main มีเฉพาะ sub ระดับเดียว
  </action>
  <acceptance_criteria>
    - `grep -n "flattenAi" lib/core/nutrition/ingredients_codec.dart` พบอย่างน้อย 1 บรรทัด
    - คอมเมนต์สั้น ๆ ในโค้ดอธิบายกันซ้ำซ้อนของแคลอรี่ (1–3 ประโยค ภาษาไทยหรืออังกฤษ)
    - `flutter analyze lib/core/nutrition/` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13-01-04: recomputeEntryTotals + D-08b micro</name>
  <read_first>
    - .planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md (D-03, D-08, D-08b, D-04)
    - lib/core/database/app_database.dart (ค้นหา fiber, sugar, sodium บน FoodEntries)
  </read_first>
  <action>
เพิ่ม API เช่น `EntryNutritionRollup recomputeEntryRollup({required IngredientsDocumentV2 doc, double? entryFiber, double? entrySugar, double? entrySodium})` ที่คืนค่า:
- calories, protein, carbs, fat = ผลรวมจากทุก main หลัง roll-up sub → main (main ที่ไม่มี sub ใช้ค่าบน main)
- fiber/sugar/sodium: ถ้าทุกโหนดที่ร่วมคำนวณมีค่าฟิลด์นั้น → ผลรวม; ถ้าขาดอย่างใดอย่างหนึ่ง → คืนค่าเดิมจากพารามิเตอร์ entry* (preserve) ไม่บังคับ null
ส่งออก type ที่ executor ใช้ต่อกับ `FoodEntry` ได้ในภายหลัง (Phase 16)
  </action>
  <acceptance_criteria>
    - `grep -n "recompute" lib/core/nutrition/ingredients_codec.dart` พบอย่างน้อย 1 บรรทัดที่เป็นชื่อฟังก์ชันสาธารณะ (เช่น recomputeEntryRollup)
    - `grep -n "fiber\|sugar\|sodium" lib/core/nutrition/ingredients_codec.dart` พบอย่างน้อย 2 บรรทัดที่เกี่ยวกับการ preserve หรือ roll-up
    - `flutter analyze lib/core/nutrition/` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze lib/core/nutrition/` ไม่มี error
- [ ] ยังไม่บังกับรัน `flutter test` ใน wave 1 — รอแผน 02
- [ ] ไม่มี import จาก `lib/features/` เข้า `lib/core/nutrition/` (ตรวจด้วย `grep -r "features/" lib/core/nutrition/` → ควรว่าง)
</verification>

<success_criteria>
- มี API กลาง parse/serialize v2/flatten/recompute ตาม 13-CONTEXT และ 13-RESEARCH
- Legacy list อ่านได้โดยไม่ throw
</success_criteria>

<output>
After completion, create `.planning/phases/13-ingredients-schema-recompute/13-01-SUMMARY.md`
</output>
