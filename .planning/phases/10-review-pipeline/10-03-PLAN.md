---
phase: 10-review-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - lib/navigation/bottom_nav.dart
  - lib/features/arscan/presentation/arscan_entry_point.dart
autonomous: true
requirements:
  - ARSCAN-17
  - ARSCAN-18
user_setup: []
must_haves:
  truths:
    - "มีปุ่ม ARscan ที่เด่นชัดใน navigation bar มากกว่าปุ่มอื่น (ใหญ่กว่า / สีเด่นกว่า)"
    - "ผู้ใช้ยังสามารถเข้าถึง flow กล้อง/แกลเลอรี่เดิมได้ครบถ้วน"
  artifacts:
    - path: "lib/navigation/bottom_nav.dart"
      provides: "bottom nav ที่มี ARscan button เด่น"
    - path: "lib/features/arscan/presentation/arscan_entry_point.dart"
      provides: "entry point wrapper สำหรับ ARscan flow"
  key_links: []
---

<objective>
วาง ARscan ในตำแหน่งเด่นบน navigation bar โดยยังรักษาทางเข้ากล้อง/แกลเลอรี่เดิม

Purpose: ทำให้ ARscan เป็นฟีเจอร์หลักของ v2.0
Output: navigation ที่มี ARscan button โดดเด่น
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: วิเคราะห์ navigation ปัจจุบัน</name>
  <files>lib/navigation/, lib/main.dart</files>
  <action>
ค้นหาไฟล์ navigation หลักของแอป:
1. search "BottomNavigationBar" หรือ "NavigationBar" ทั้งโปรเจกต์
2. ระบุว่าปุ่มถ่ายรูป/สแกนอาหารปัจจุบันอยู่ตำแหน่งไหน (FAB? bottom nav item? speed dial?)
3. จดว่า flow กล้อง/แกลเลอรี่ปัจจุบันถูก trigger จากตรงไหน
4. สรุป: จะวาง ARscan ตำแหน่งไหนให้เด่นที่สุดโดยไม่ทำลาย UX เดิม
  </action>
  <verify>มีภาพรวม navigation ชัดเจน</verify>
  <done>รู้ว่าต้องปรับตรงไหน</done>
</task>

<task type="auto">
  <name>Task 2: เพิ่ม ARscan button ที่เด่นใน navigation</name>
  <files>lib/navigation/bottom_nav.dart, lib/features/arscan/presentation/arscan_entry_point.dart</files>
  <action>
**แนวทางแนะนำ (ปรับตาม navigation ที่มี):**

ถ้าแอปใช้ BottomNavigationBar:
- เปลี่ยนปุ่มตรงกลาง (หรือปุ่มถ่ายรูปเดิม) ให้เป็น ARscan โดย:
  - ไอคอน: `Icons.center_focus_strong` หรือ camera ที่มี badge "AR"
  - ขนาด: ใหญ่กว่า item อื่น (ใช้ FloatingActionButton กลางจอหรือ sized icon)
  - สี: ใช้ primary color / accent ที่ต่างจาก item อื่น
- ย้ายปุ่มกล้อง/แกลเลอรี่เดิมไป:
  - เป็น sub-menu ภายใน ARscan screen (ปุ่มสลับโหมด)
  - หรือเก็บไว้เป็น item ปกติใน nav (icon เล็กกว่า ARscan)

ถ้าแอปใช้ FAB:
- ทำ ARscan เป็น FAB หลัก (กลาง, ใหญ่)
- กล้อง/แกลเลอรี่ย้ายไปเป็น menu option

สร้าง `arscan_entry_point.dart`:
```dart
class ArScanEntryPoint extends StatelessWidget {
  // wrapper ที่ check permission (camera) แล้ว push ไป ArScanScreen
  // ถ้าไม่มี permission → ขอก่อน
}
```

**สำคัญ:** ห้ามลบทางเข้าของ flow เดิม (กล้อง/แกลเลอรี่) — ต้องเข้าถึงได้
  </action>
  <verify>
- เปิดแอป → เห็น ARscan button โดดเด่นกว่าปุ่มอื่นใน nav
- กด ARscan → เข้า ARscan screen
- กล้อง/แกลเลอรี่เดิมยังเข้าถึงได้
  </verify>
  <done>navigation ปรับเสร็จตาม ARSCAN-17 และ ARSCAN-18</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ARscan button เด่นกว่าปุ่มอื่นใน navigation (ใหญ่กว่า / สีเด่นกว่า)
- [ ] กด ARscan → เข้า ARscan screen ได้
- [ ] flow กล้องเดิม (ถ่ายรูป → Gemini) ยังทำงานได้
- [ ] flow แกลเลอรี่ (เลือกรูป → Gemini) ยังทำงานได้
</verification>

<success_criteria>

- ARSCAN-17 และ ARSCAN-18 ถูกตอบโจทย์
- ARscan เป็น primary action ใน navigation ของ v2.0
- ไม่มี regression กับ flow เดิม
  </success_criteria>

<output>
After completion, create `.planning/phases/10-review-pipeline/10-03-SUMMARY.md`
</output>
