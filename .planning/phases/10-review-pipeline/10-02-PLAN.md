---
phase: 10-review-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
  - "06-03"
files_modified:
  - lib/features/arscan/application/arscan_analysis_service.dart
  - lib/features/arscan/presentation/review/arscan_review_bottom_sheet.dart
autonomous: true
requirements:
  - ARSCAN-12
  - ARSCAN-13
user_setup: []
must_haves:
  truths:
    - "เมื่อผู้ใช้กด Analyse ใน review bottom sheet → ส่งรูป 3 มุม + metadata ไปยัง Gemini API แล้วได้ผล nutrition กลับมา"
    - "ผลลัพธ์ถูกสร้างเป็น FoodEntry ด้วย dataSource=arScan พร้อม supplementaryImagePath2/3 แล้วบันทึกผ่าน DatabaseService จาก Phase 6"
  artifacts:
    - path: "lib/features/arscan/application/arscan_analysis_service.dart"
      provides: "service สำหรับส่งหลายรูปไป Gemini และ parse ผลลัพธ์เป็น FoodEntry"
  key_links:
    - from: "lib/features/arscan/application/arscan_analysis_service.dart"
      to: "lib/core/database/database_service.dart"
      via: "บันทึก FoodEntry หลังวิเคราะห์เสร็จผ่าน DatabaseService"
      pattern: "DatabaseService.foodEntries"
---

<objective>
เชื่อม review bottom sheet เข้ากับ Gemini API สำหรับ multi-image nutritional analysis แล้วบันทึกผลเป็น FoodEntry

Purpose: ทำให้ flow ARscan จบครบ: ถ่ายรูป → review → วิเคราะห์ → บันทึก
Output: service ที่ส่ง 3 รูป + metadata ไป Gemini แล้ว save FoodEntry
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ศึกษา Gemini integration ปัจจุบันของแอป</name>
  <files>lib/features/scanner/services/, lib/services/</files>
  <action>
ค้นหาไฟล์ที่เรียก Gemini API ในโปรเจกต์ปัจจุบัน (search คำว่า "GenerativeModel", "generateContent", "gemini") แล้วสรุป:
1. ไฟล์ไหนเป็น service หลักที่ส่งรูปไป Gemini
2. ใช้ prompt อะไร (system prompt, user prompt)
3. parse ผลลัพธ์อย่างไร (JSON → FoodEntry fields)
4. จัดการ API key อย่างไร (BYOK — user ใส่ key เอง)

**ไฟล์ที่น่าจะเกี่ยว (ตรวจดู):**
- `lib/services/gemini_service.dart` หรือชื่อคล้ายกัน
- `lib/features/scanner/services/` directory
- search "generateContent" หรือ "GenerativeModel" ทั้งโปรเจกต์
  </action>
  <verify>สรุปได้ว่า Gemini integration อยู่ไฟล์ไหนและ pattern เป็นอย่างไร</verify>
  <done>เข้าใจ pattern การเรียก Gemini ปัจจุบัน</done>
</task>

<task type="auto">
  <name>Task 2: สร้าง ArScanAnalysisService สำหรับ multi-image</name>
  <files>lib/features/arscan/application/arscan_analysis_service.dart</files>
  <action>
สร้าง service ที่ **ใช้ pattern เดียวกันกับ Gemini service ปัจจุบัน** แต่ส่งหลายรูป:

```dart
class ArScanAnalysisService {
  Future<FoodEntry> analyseMultiAngle({
    required List<String> imagePaths,  // 3 paths
    required String? hiddenItems,
    required double? quantity,
    required String? unit,
  }) async {
    // 1. อ่านรูป 3 ไฟล์เป็น bytes
    // 2. สร้าง Gemini content ที่มี 3 images + text prompt
    // 3. prompt ให้บอก: "These are 3 angles of the same dish for better portion estimation..."
    // 4. Include hidden items / quantity / unit ใน prompt ถ้ามี
    // 5. Parse response → FoodEntry fields (calories, protein, carbs, fat, name)
    // 6. สร้าง FoodEntriesCompanion ด้วย source=DataSource.arScan.index, supplementaryImagePath2/3=imagePaths[1]/[2]
    // 7. บันทึกผ่าน DatabaseService.foodEntries
  }
}
```

**Gemini multi-image API (google_generative_ai):**
```dart
final content = Content.multi([
  TextPart(prompt),
  DataPart('image/jpeg', imageBytes1),
  DataPart('image/jpeg', imageBytes2),
  DataPart('image/jpeg', imageBytes3),
]);
final response = await model.generateContent([content]);
```

**สำคัญ:**
- ใช้ API key จากเดียวกับที่แอปใช้อยู่ (BYOK)
- ใช้ system prompt / response parsing เหมือน flow ถ่ายรูปเดิม ให้ได้ผลลัพธ์ format เดียวกัน
- เพิ่มในส่วน prompt: "3 images show top, diagonal, and side views for accurate portion estimation"
  </action>
  <verify>ส่ง 3 รูปอาหารไป Gemini → ได้ผลลัพธ์ nutrition กลับมา (แม้จะไม่ 100% accurate) โดยไม่ crash</verify>
  <done>service ส่ง 3 รูปแล้วได้ผลกลับ</done>
</task>

<task type="auto">
  <name>Task 3: เชื่อม service กับ review bottom sheet</name>
  <files>lib/features/arscan/presentation/review/arscan_review_bottom_sheet.dart, lib/features/arscan/application/arscan_analysis_service.dart</files>
  <action>
เชื่อมปุ่ม "Analyse" ใน review bottom sheet:

1. กด Analyse → แสดง loading indicator (CircularProgressIndicator)
2. เรียก `ArScanAnalysisService.analyseMultiAngle(...)` ด้วยข้อมูลจากฟอร์ม
3. เมื่อสำเร็จ → แสดงผลลัพธ์ (แคลอรี่, macro) เหมือน flow Gemini analysis เดิมของแอป
4. เมื่อ error → แสดง error message + ปุ่ม retry
5. หลังบันทึก FoodEntry สำเร็จ → `ref.invalidate()` providers ที่เกี่ยวข้อง (ดู pattern จาก flow เดิม) → pop back ไป home

**ดู flow เดิมจาก:** `gemini_analysis_sheet.dart` หรือ `image_analysis_preview_screen.dart` — ใช้ UX เดียวกัน
  </action>
  <verify>กด Analyse → loading → ได้ผล nutrition → บันทึก → กลับ home → เห็น entry ใหม่ในรายการ</verify>
  <done>flow end-to-end ทำงาน: ARscan → review → Gemini → save → home</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] กด Analyse ด้วยรูป 3 มุม → ได้ผล nutrition จาก Gemini (ไม่ crash)
- [ ] FoodEntry ถูกบันทึกด้วย source=DataSource.arScan + supplementaryImagePath2/3 (3 รูปรวม imagePath)
- [ ] entry ใหม่ปรากฏในรายการ timeline ของแอป
- [ ] error handling: ถ้า Gemini fail → แสดง error + retry ไม่ crash
</verification>

<success_criteria>

- ARSCAN-12 และ ARSCAN-13 ถูกตอบโจทย์: ส่ง 3 รูปไป Gemini ได้ multi-image analysis
- Flow ครบ end-to-end: ARscan capture → review → analyse → save → timeline
- ใช้ pattern เดียวกับ Gemini integration ที่มีอยู่ จึง maintain ง่าย
  </success_criteria>

<output>
After completion, create `.planning/phases/10-review-pipeline/10-02-SUMMARY.md`
</output>
