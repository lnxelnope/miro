---
phase: 16-ingredient-ui-ai
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02-PLAN.md
files_modified:
  - lib/core/ai/gemini_service.dart
  - lib/features/chat/providers/chat_provider.dart
  - lib/features/health/widgets/log_from_meal_sheet.dart
  - lib/features/health/widgets/food_detail_bottom_sheet.dart
  - lib/features/home/widgets/simple_food_detail_sheet.dart
  - lib/features/health/widgets/meal_section.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_th.arb
  - .planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md
autonomous: true
planning_note: "Wave ใหญ่ — แนะนำ execute แบบกลุ่มย่อย (Gemini / sheets / l10n / checklist) เพื่อลด context และ conflict"
requirements:
  - ARC2-ING-04
  - ARC2-AI-01
user_setup:
  - "flutter gen-l10n หลังแก้ ARB"
must_haves:
  truths:
    - "chat food_log บันทึก preliminaryIngredientsJson ผ่าน v2 หรือ parse→serialize เดียวกัน"
    - "gemini_service prompt/schema สอดคล้อง root=main, sub หนึ่งชั้น, ไม่ double-count"
    - "หน้าแสดงรายละเอียดอาหารอ่าน v2 ได้โดยไม่ crash"
  key_links:
    - from: "gemini_service.dart"
      to: "GeminiAnalysisSheet / food_preview"
      via: "parsed result shape"
      pattern: "ingredients_detail"
---

<objective>
ปิด **ARC2-ING-04** (chat, log meal, readers) และ **ARC2-AI-01** (prompt/validation ใน `gemini_service`); อัปเดตการแสดงผลรายละเอียด entry ให้รองรับ v2; เพิ่ม manual checklist

Purpose: ไม่เหลือ path หลักที่เขียน legacy List โดยไม่ผ่าน codec; prompt AI สอดคล้องสัญญา v2
Output: `16-MANUAL-CHECKLIST.md` + analyzer ผ่านบนไฟล์ที่แตะ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-10, D-12)
</context>

<tasks>

<task type="auto">
  <name>Task 16-03-01: gemini_service — prompt & validation</name>
  <read_first>
    - lib/core/ai/gemini_service.dart (ส่วน ingredients_detail, MANDATORY, CRITICAL)
    - .planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-12)
  </read_first>
  <action>
อัปเดตข้อความ prompt / คำอธิบาย JSON schema ให้ชัด: แต่ละ root = main; `sub_ingredients` หนึ่งชั้น; ห้ามซ้ำซ้อนแคลอรี่ระหว่าง parent กับ sub; สอดคล้องกับ flatten ใน codec — **ห้าม** แก้ business logic ที่ไม่เกี่ยวกับ ingredients โดยไม่จำเป็น
  </action>
  <acceptance_criteria>
    - `grep -n "sub_ingredients\\|ingredients_detail" lib/core/ai/gemini_service.dart` พบอย่างน้อย 5 บรรทัด (มีอยู่แล้ว) และมีการเพิ่ม/แก้ข้อความที่อธิบายความลึก 2 ระดับ (ค้นหา "two\\|2 level\\|flatten" หรือภาษาเทียบเท่าใน diff)
    - `flutter analyze lib/core/ai/gemini_service.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-02: chat_provider food_log</name>
  <read_first>
    - lib/features/chat/providers/chat_provider.dart (preliminaryIngredientsJson)
    - lib/core/nutrition/ingredients_codec.dart
  </read_first>
  <action>
แทนที่ `jsonEncode(preliminaryIngredients)` แบบ List ดิบด้วยการสร้าง document ผ่าน codec (flatten ถ้าโครงสร้างจาก AI ลึก) แล้ว `serializeIngredientsV2` หรือ helper เดียวกับแผน 16-02
  </action>
  <acceptance_criteria>
    - `grep -n "preliminaryIngredientsJson = jsonEncode" lib/features/chat/providers/chat_provider.dart` ไม่พบรูปแบบเดิมที่ encode list ตรงๆ โดยไม่ผ่าน codec — ต้องมีเรียก codec หรือ helper
    - `flutter analyze lib/features/chat/providers/chat_provider.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-03: log_from_meal_sheet</name>
  <read_first>
    - lib/features/health/widgets/log_from_meal_sheet.dart (`ingredientTreeToJsonMaps`, save)
  </read_first>
  <action>
แปลงผลจาก My Meal tree เป็น `IngredientsDocumentV2` แล้ว serialize v2 + recompute entry เมื่อบันทึก timeline entry — หลีกเลี่ยงสัญญาคนละแบบกับ add_food
  </action>
  <acceptance_criteria>
    - `grep -n "ingredientTreeToJsonMaps" lib/features/health/widgets/log_from_meal_sheet.dart` อาจยังมีแต่ต้องต่อกับ codec หรือแทนที่ด้วย path ที่ได้ v2
    - `flutter analyze lib/features/health/widgets/log_from_meal_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-04: Readers — food_detail, simple_food_detail, meal_section</name>
  <read_first>
    - lib/features/health/widgets/food_detail_bottom_sheet.dart
    - lib/features/home/widgets/simple_food_detail_sheet.dart
    - lib/features/health/widgets/meal_section.dart
  </read_first>
  <action>
แทนที่ `jsonDecode(...) as List` ด้วย `parseIngredientsJson` แล้วแปลงเป็นรายการสำหรับ UI (หรือแสดง v2 โครงสร้าง main/sub) — ต้องไม่ crash เมื่อ `ingredientsJson` เป็น object v2
  </action>
  <acceptance_criteria>
    - `grep -n "jsonDecode(entry.ingredientsJson" lib/features/health/widgets/food_detail_bottom_sheet.dart` — ถ้ายังมี ต้องไม่สมมติ List อย่างเดียว (ต้องแยกสาขา legacy/v2)
    - เงื่อนไขเดียวกันกับ `simple_food_detail_sheet.dart` และ `meal_section.dart` ในจุดที่อ่าน ingredientsJson
    - `flutter analyze` สามไฟล์ exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-05: saveIngredientsAndMeal / MyMeal path (ถ้าจำเป็น)</name>
  <read_first>
    - lib/features/health/providers/health_provider.dart (`saveIngredientsAndMeal`)
  </read_first>
  <action>
ถ้า MyMeal ยังเก็บโครงสร้างแยกจาก FoodEntry: ตรวจว่าการ import จาก Gemini ไม่ทำให้สัญญาขัดกับ v2 บน entry — ปรับให้ข้อมูลที่ส่งเข้า `ingredientsData` สอดคล้อง flatten หรือบันทึกสองที่ให้สอดคล้อง (บันทึกใน SUMMARY ถ้าไม่แก้ในรอบนี้)
  </action>
  <acceptance_criteria>
    - `16-03-SUMMARY.md` ระบุสถานะ MyMeal vs FoodEntry
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-06: 16-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 16 success criteria)
  </read_first>
  <action>
สร้าง checklist ภาษาไทย: (1) วิเคราะห์รูป → ยอดรวมสอดคล้อง subs (2) แก้ sub ใน Edit → entry อัปเดต (3) chat บันทึกอาหาร → เปิดรายละเอียดได้ (4) log จาก My Meal → v2 ไม่ crash
  </action>
  <acceptance_criteria>
    - ไฟล์ `.planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` มีอยู่
    - `grep -n "chat\\|แชท" .planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter test` (อย่างน้อยชุด nutrition จาก Phase 13 ถ้ามี) + `flutter analyze` ชุดใหญ่ที่เกี่ยว
</verification>

<success_criteria>
- ROADMAP เฟส 16 ข้อ 3 (IntentHandler/sheets สัญญาเดียว)
</success_criteria>

<output>
After completion, create `16-03-SUMMARY.md`
</output>
