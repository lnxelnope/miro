---
phase: 16-ingredient-ui-ai
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02-PLAN.md
files_modified:
  - lib/core/ai/gemini_service.dart
  - lib/features/chat/providers/chat_provider.dart
  - lib/features/health/widgets/log_from_meal_sheet.dart
autonomous: true
requirements:
  - ARC2-ING-04
  - ARC2-AI-01
user_setup:
  - "flutter gen-l10n หลังแก้ ARB ถ้ามี"
must_haves:
  truths:
    - "chat food_log บันทึก preliminaryIngredientsJson ผ่าน v2 หรือ parse→serialize เดียวกัน"
    - "gemini_service prompt/schema สอดคล้อง root=main, sub หนึ่งชั้น, ไม่ double-count"
  artifacts:
    - path: "lib/core/ai/gemini_service.dart"
      provides: "prompt สอดคล้อง v2 (root=main, sub 1 level)"
    - path: "lib/features/chat/providers/chat_provider.dart"
      provides: "food_log path ใช้ codec v2"
  key_links:
    - from: "gemini_service.dart"
      to: "GeminiAnalysisSheet / food_preview"
      via: "parsed result shape"
      pattern: "ingredients_detail"
---

<objective>
อัปเดต **AI write paths** — prompt/validation ใน `gemini_service.dart`, `chat_provider.dart` food_log, และ `log_from_meal_sheet.dart` — ให้ serialize ผ่าน codec v2 แทน legacy List ดิบ

Purpose: ปิด ARC2-ING-04 write paths + ARC2-AI-01 prompt
Output: analyzer ผ่านสำหรับไฟล์ที่แตะ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-10, D-12)
@lib/core/nutrition/ingredients_codec.dart
</context>

<tasks>

<task type="auto">
  <name>Task 16-03-01: gemini_service — prompt & validation</name>
  <read_first>
    - lib/core/ai/gemini_service.dart (ค้นหา "sub_ingredients" และ "ingredients_detail" — อ่าน context 40 บรรทัดรอบแต่ละจุด เน้น prompt text / schema description)
    - .planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-12)
  </read_first>
  <action>
อัปเดตข้อความ prompt / คำอธิบาย JSON schema ให้ชัด:
1. แต่ละ root ใน `ingredients_detail` = **main ingredient**
2. `sub_ingredients` มีได้ **หนึ่งชั้นเท่านั้น** (ไม่มี sub ของ sub)
3. **ห้ามซ้ำซ้อนแคลอรี่** ระหว่าง parent กับ sub — calories ของ parent = ผลรวมจาก subs (หรือระบุชัดว่า parent ไม่ซ้ำ)
4. สอดคล้องกับ flatten ใน codec
**ห้าม** แก้ business logic อื่นที่ไม่เกี่ยวกับ ingredients
  </action>
  <acceptance_criteria>
    - `rg "sub_ingredients" lib/core/ai/gemini_service.dart` พบอย่างน้อย 5 บรรทัด
    - มีข้อความใน prompt ที่อธิบาย "one level" หรือ "no nested" หรือเทียบเท่า (ตรวจด้วย `rg -i "one level\|no nested\|single level\|ชั้นเดียว\|2 level\|two level" lib/core/ai/gemini_service.dart` พบอย่างน้อย 1 บรรทัด)
    - `flutter analyze lib/core/ai/gemini_service.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-02: chat_provider food_log</name>
  <read_first>
    - lib/features/chat/providers/chat_provider.dart (ค้นหา "preliminaryIngredientsJson" — อ่าน context 30 บรรทัดรอบแต่ละจุด เพื่อเข้าใจ flow ที่สร้าง/บันทึก ingredients)
    - lib/core/nutrition/ingredients_codec.dart (signature ของ serializeIngredientsV2, flattenAiIngredientRoots)
  </read_first>
  <action>
แทนที่การ encode ingredients แบบ List ดิบ ด้วย pipeline:
1. ถ้ามี list จาก AI → `flattenAiIngredientRoots` (กัน sub-sub)
2. แปลงเป็น `IngredientsDocumentV2`
3. `serializeIngredientsV2(doc)` ได้ string v2
4. ใช้ string นี้เป็น `preliminaryIngredientsJson` แทน `jsonEncode(list)`
  </action>
  <acceptance_criteria>
    - `rg "preliminaryIngredientsJson = jsonEncode" lib/features/chat/providers/chat_provider.dart` ไม่พบ (ไม่มี jsonEncode List ตรงๆ)
    - `rg "ingredients_codec\|serializeIngredientsV2\|flattenAi" lib/features/chat/providers/chat_provider.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/chat/providers/chat_provider.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-03-03: log_from_meal_sheet</name>
  <read_first>
    - lib/features/health/widgets/log_from_meal_sheet.dart (ค้นหา "ingredientTreeToJsonMaps" และจุดที่สร้าง ingredientsJson — อ่าน context 30 บรรทัด)
    - lib/core/nutrition/ingredients_codec.dart (serializeIngredientsV2)
  </read_first>
  <action>
แปลงผลจาก My Meal tree เป็น `IngredientsDocumentV2` แล้ว serialize v2 + recompute entry เมื่อบันทึก timeline entry — `ingredientTreeToJsonMaps` อาจยังเรียกได้แต่ต้องต่อกับ codec สำหรับ serialize ขั้นสุดท้าย
  </action>
  <acceptance_criteria>
    - `rg "ingredients_codec\|serializeIngredientsV2\|parseIngredientsJson" lib/features/health/widgets/log_from_meal_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/health/widgets/log_from_meal_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze lib/core/ai/gemini_service.dart lib/features/chat/providers/chat_provider.dart lib/features/health/widgets/log_from_meal_sheet.dart` exit code 0
</verification>

<success_criteria>
- write paths ทั้ง 3 ไฟล์ใช้ v2 codec (D-08)
- prompt AI สอดคล้อง v2 (D-12)
</success_criteria>

<output>
After completion, create `16-03-SUMMARY.md`
</output>
