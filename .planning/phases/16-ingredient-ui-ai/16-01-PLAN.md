---
phase: 16-ingredient-ui-ai
plan: 01
type: execute
wave: 1
depends_on:
  - ../13-ingredients-schema-recompute/13-02-PLAN.md
files_modified:
  - lib/features/health/widgets/add_food_bottom_sheet.dart
  - lib/features/health/widgets/edit_food_bottom_sheet.dart
autonomous: true
requirements:
  - ARC2-ING-01
  - ARC2-ING-02
  - ARC2-ING-04
user_setup:
  - "Phase 13: มี lib/core/nutrition/ingredients_codec.dart ตามแผน 13-01"
must_haves:
  truths:
    - "บันทึกจาก Add/Edit เมื่อมี ingredients ผ่าน serialize v2 + recompute entry ตาม codec"
    - "โหลด ingredientsJson legacy หรือ v2 ผ่าน parse กลาง"
    - "แถว main ที่มี sub: ช่อง macro read-only และ derive จาก sub; เปลี่ยนปริมาณรวม main → สเกล sub ผ่านกฎเดียวกับ .cursorrules"
  artifacts:
    - path: "lib/features/health/widgets/add_food_bottom_sheet.dart"
    - path: "lib/features/health/widgets/edit_food_bottom_sheet.dart"
  key_links:
    - from: "ingredients_codec.dart"
      to: "add/edit sheets"
      via: "import"
      pattern: "parseIngredientsJson / serializeIngredientsV2 / recompute"
---

<objective>
ต่อ `ingredients_codec` เข้ากับ **AddFoodBottomSheet** และ **EditFoodBottomSheet**: parse ตอนโหลด, serialize v2 + recompute ตอนบันทึก, ล็อก main เมื่อมี sub, สเกล sub เมื่อเปลี่ยนปริมาณรวมของ main

Purpose: ARC2-ING-01, ARC2-ING-02, ส่วนใหญ่ของ ARC2-ING-04 สำหรับสอง sheet หลัก
Output: ไม่มี `jsonEncode(_ingredients.map((e) => e.toMap())` เป็นรูปแบบเดียวของการบันทึก ingredients เมื่อมีโครงสร้างจาก codec
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-01–D-05, D-08, D-09, D-14)
@.planning/phases/13-ingredients-schema-recompute/13-CONTEXT.md
@.cursorrules (base per unit, readOnly, UnitConverter)
</context>

<tasks>

<task type="auto">
  <name>Task 16-01-00: Gate — codec Phase 13</name>
  <read_first>
    - lib/core/nutrition/ingredients_codec.dart
    - .planning/phases/13-ingredients-schema-recompute/13-01-PLAN.md
  </read_first>
  <action>
ยืนยันว่าไฟล์ codec มีฟังก์ชันสาธารณะที่แผน 16 ต้องใช้; ถ้ายังไม่มีให้หยุดและ execute Phase 13 ก่อน หรือ merge branch ที่มี codec
  </action>
  <acceptance_criteria>
    - ไฟล์ `lib/core/nutrition/ingredients_codec.dart` มีอยู่จริงใน workspace ณ เวลา execute
    - `grep -n "serializeIngredientsV2\\|parseIngredientsJson" lib/core/nutrition/ingredients_codec.dart` พบอย่างน้อยหนึ่งชื่อ
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-01-01: AddFood — โหลด/บันทึกผ่าน codec</name>
  <read_first>
    - lib/features/health/widgets/add_food_bottom_sheet.dart (`_EditableIngredient`, `_save`, `_saveAndAnalyze`, toMap)
    - lib/core/nutrition/ingredients_codec.dart
  </read_first>
  <action>
1. เมื่อโหลดข้อมูลที่มี `ingredientsJson` ใช้ `parseIngredientsJson` แล้วแปลงเป็น `_EditableIngredient` tree (รองรับ legacy list และ v2)
2. ก่อนบันทึก: สร้าง `IngredientsDocumentV2` จาก state ปัจจุบัน → เรียก recompute กลาง → อัปเดตค่า calories/protein/carbs/fat (และ micro ตาม D-08b) บน `FoodEntry` ที่จะ save → `ingredientsJson = serializeIngredientsV2(...)`
3. Path `_saveAndAnalyze` ที่ส่ง entry ไปคิววิเคราะห์: เก็บ `ingredientsJson` ให้สอดคล้องกฎเดียวกันเมื่อมี ingredients
  </action>
  <acceptance_criteria>
    - `grep -n "jsonEncode(_ingredients.map" lib/features/health/widgets/add_food_bottom_sheet.dart` ไม่พบ **หรือ** เหลือเฉพาะ path ที่ dead code พร้อมคอมเมนต์อธิบาย — เป้าหมาย: เส้นทางบันทึกหลักใช้ codec
    - `grep -n "ingredients_codec\\|parseIngredientsJson\\|serializeIngredientsV2" lib/features/health/widgets/add_food_bottom_sheet.dart` พบอย่างน้อย 2 บรรทัดรวม
    - `flutter analyze lib/features/health/widgets/add_food_bottom_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-01-02: AddFood — lock main + scale (D-01, D-04)</name>
  <read_first>
    - lib/features/health/widgets/add_food_bottom_sheet.dart (UI แถว ingredient)
    - .cursorrules
  </read_first>
  <action>
1. เมื่อ `subIngredients.isNotEmpty` บนแถว main: ตั้ง `readOnly: true` (หรือ disabled) บนช่อง kcal/P/C/F ของแถวนั้น; ค่าแสดงผลมาจากผลรวม sub (ผ่าน `recalculate()` ที่อัปเดตจาก sub หรือผ่าน codec)
2. Listener บน `amountController` ของ main ที่มี sub: สเกล sub ตามสัดส่วน (ใช้สูตรเดียวกับ logic ปัจจุบันใน `_EditableIngredient.recalculate` แต่ต้องสอดคล้องกับการอัปเดต base ต่อหน่วยเมื่อจำเป็น)
3. ข้อความ UI ใหม่ → L10n
  </action>
  <acceptance_criteria>
    - `grep -n "readOnly.*true\\|readOnly: true" lib/features/health/widgets/add_food_bottom_sheet.dart` พบอย่างน้อย 1 บรรทัดที่เกี่ยวกับโภชนาการหรือช่องที่ล็อกเมื่อมี sub (หรือใช้ `IgnorePointer`/`AbsorbPointer` พร้อมคอมเมนต์)
    - `flutter analyze lib/features/health/widgets/add_food_bottom_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-01-03: EditFood — parse/serialize/lock/scale</name>
  <read_first>
    - lib/features/health/widgets/edit_food_bottom_sheet.dart (parse ingredients, save)
    - lib/core/nutrition/ingredients_codec.dart
  </read_first>
  <action>
ทำกฎเดียวกับ AddFood: parse กลาง, serialize v2, recompute entry ก่อนบันทึก, lock main เมื่อมี sub, สเกล sub เมื่อปริมาณรวม main เปลี่ยน
  </action>
  <acceptance_criteria>
    - `grep -n "jsonDecode(entry.ingredientsJson" lib/features/health/widgets/edit_food_bottom_sheet.dart` ไม่พบการสมมติเป็น `List` อย่างเดียวโดยไม่ผ่าน codec — ต้องมี parse กลางหรือ helper ที่เรียก codec
    - `grep -n "ingredients_codec\\|parseIngredientsJson\\|serializeIngredientsV2" lib/features/health/widgets/edit_food_bottom_sheet.dart` พบอย่างน้อย 2 บรรทัดรวม
    - `flutter analyze lib/features/health/widgets/edit_food_bottom_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze` สองไฟล์ sheet ผ่าน
- [ ] บันทึก fixture หรือ manual: string `ingredientsJson` ที่บันทึกใหม่มี `"schemaVersion"` เมื่อมี subs (ถ้าเทสต์อัตโนมัติยังไม่มี ให้บันทึกใน SUMMARY)
</verification>

<success_criteria>
- ROADMAP เฟส 16 ข้อ 2 (แก้ sub แล้ว entry อัปเดต; สเกลรวม) สำหรับ Add/Edit
</success_criteria>

<output>
After completion, create `16-01-SUMMARY.md`
</output>
