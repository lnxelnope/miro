---
phase: 16-ingredient-ui-ai
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01-PLAN.md
files_modified:
  - lib/features/health/widgets/gemini_analysis_sheet.dart
  - lib/features/health/presentation/food_preview_screen.dart
  - lib/core/utils/batch_analysis_helper.dart
autonomous: true
requirements:
  - ARC2-ING-03
  - ARC2-ING-04
  - ARC2-AI-01
user_setup: []
must_haves:
  truths:
    - "ผล ingredients จาก AI ผ่าน flatten → v2 ก่อนบันทึก"
    - "ยอด macro บน entry หลังวิเคราะห์สอดคล้องผลรวมจาก mains/subs (post-process / applyResult)"
    - "GeminiAnalysisSheet ล็อก main / สเกล sub เหมือน Add/Edit"
  key_links:
    - from: "ingredients_codec.dart"
      to: "gemini_analysis_sheet.dart"
      via: "flatten + serialize + recompute"
      pattern: "ingredients_detail"
---

<objective>
จัดแนว **GeminiAnalysisSheet**, **food_preview_screen** (บันทึกหลังวิเคราะห์), และ **batch_analysis_helper** ให้ใช้ flatten + v2 + recompute เดียวกับ Add/Edit; ล็อก main / สเกล sub บน sheet วิเคราะห์

Purpose: ARC2-ING-03, ส่วน ARC2-ING-04/ARC2-AI-01 สำหรับ flow วิเคราะห์รูป/แบทช์
Output: ไม่ `jsonEncode(ingredientsDetail)` เป็น List ดิบโดยไม่ผ่าน codec ในไฟล์เหล่านี้
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-06, D-11, D-13)
</context>

<tasks>

<task type="auto">
  <name>Task 16-02-01: Helper แปลง AI detail → v2 + rollup</name>
  <read_first>
    - lib/core/nutrition/ingredients_codec.dart
    - lib/core/ai/gemini_service.dart (โครงสร้าง ingredients_detail ตัวอย่าง)
  </read_first>
  <action>
ถ้ายังไม่มีใน codec: เพิ่มฟังก์ชันสาธารณะเช่น `encodeIngredientsFromAiDetail(List<Map<String,dynamic>> roots, {required FoodEntry entryForMicro})` หรือคืน `String?` v2 + patch macro — **หรือ** เรียก `flattenAiIngredientRoots` แล้ว `serializeIngredientsV2` + recompute ในลำดับเดียวจาก caller — รวม logic ไว้จุดเดียวเพื่อ food_preview และ batch ใช้ซ้ำ
  </action>
  <acceptance_criteria>
    - `grep -n "flattenAi\\|serializeIngredientsV2" lib/core/utils/batch_analysis_helper.dart` หรือ `lib/features/health/presentation/food_preview_screen.dart` หรือ codec พบอย่างน้อย 1 บรรทัดหลังทำงาน
    - `flutter analyze` บนไฟล์ที่แก้
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-02-02: GeminiAnalysisSheet</name>
  <read_first>
    - lib/features/health/widgets/gemini_analysis_sheet.dart
    - lib/features/health/widgets/add_food_bottom_sheet.dart (อ้างอิงพฤติกรรมหลัง 16-01)
  </read_first>
  <action>
1. โหลด/แก้ไข ingredients ผ่าน pipeline เดียวกับ AddFood (หรือ import shared widget ถ้า extract แล้ว)
2. flatten ผลดิบจาก AI ตอนรับเข้ามา (ถ้ายังไม่ flatten ใน service)
3. lock main + scale sub เหมือนแผน 16-01
4. บันทึก: serialize v2 + recompute entry totals ก่อนส่งต่อ notifier
  </action>
  <acceptance_criteria>
    - `grep -n "ingredients_codec\\|serializeIngredientsV2\\|parseIngredientsJson" lib/features/health/widgets/gemini_analysis_sheet.dart` พบอย่างน้อย 2 บรรทัดรวม
    - `flutter analyze lib/features/health/widgets/gemini_analysis_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-02-03: food_preview_screen _saveFood</name>
  <read_first>
    - lib/features/health/presentation/food_preview_screen.dart (`_saveFood`, ingredientsDetail)
  </read_first>
  <action>
แทนที่ `jsonEncode(_analysisResult!.ingredientsDetail)` ด้วย pipeline v2 + ปรับ calories/protein/carbs/fat บน entry ให้สอดคล้อง rollup (ARC2-AI-01) ก่อน `addFoodEntry`
  </action>
  <acceptance_criteria>
    - `grep -n "jsonEncode(_analysisResult!.ingredientsDetail)" lib/features/health/presentation/food_preview_screen.dart` ไม่พบ
    - `flutter analyze lib/features/health/presentation/food_preview_screen.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-02-04: batch_analysis_helper</name>
  <read_first>
    - lib/core/utils/batch_analysis_helper.dart
  </read_first>
  <action>
1. จุด `jsonDecode` เป็น `List` เท่านั้น: ใช้ `parseIngredientsJson` แทน
2. จุด `entry.ingredientsJson = jsonEncode(result.ingredientsDetail...)` เปลี่ยนเป็น v2 + recompute
3. Logic รวมแคลอรี่จาก root ต้องสอดคล้องกับโมเดล v2 (ไม่ double-count เมื่อมี sub)
  </action>
  <acceptance_criteria>
    - `grep -n "as List<dynamic>" lib/core/utils/batch_analysis_helper.dart` ไม่พบการ cast ตรงๆ ต่อ `jsonDecode` ของ ingredients โดยไม่ผ่าน codec — หรือมี helper หุ้ม
    - `flutter analyze lib/core/utils/batch_analysis_helper.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze` สามไฟล์หลักผ่าน
- [ ] วิเคราะห์รูปหนึ่งรายการแล้วบันทึก — `ingredientsJson` มี `schemaVersion` (manual หรือ test)
</verification>

<success_criteria>
- ROADMAP เฟส 16 ข้อ 1 (หลัง AI ชื่อจาน ตัวเลขรวมสอดคล้อง mains/subs) สำหรับ flow ที่แก้ในแผนนี้
</success_criteria>

<output>
After completion, create `16-02-SUMMARY.md`
</output>
