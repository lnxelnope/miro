---
phase: 16-ingredient-ui-ai
plan: 04
type: execute
wave: 4
depends_on:
  - 16-03-PLAN.md
files_modified:
  - lib/features/health/widgets/food_detail_bottom_sheet.dart
  - lib/features/home/widgets/simple_food_detail_sheet.dart
  - lib/features/health/widgets/meal_section.dart
  - lib/features/health/providers/health_provider.dart
  - .planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ARC2-ING-04
user_setup: []
must_haves:
  truths:
    - "หน้าแสดงรายละเอียดอาหารอ่าน ingredientsJson v2 ได้โดยไม่ crash"
    - "MyMeal path สอดคล้อง v2 หรือบันทึกสถานะใน SUMMARY"
  artifacts:
    - path: "lib/features/health/widgets/food_detail_bottom_sheet.dart"
      provides: "reader v2 compatible"
    - path: ".planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md"
      provides: "manual verification checklist ปิดเฟส 16"
  key_links:
    - from: "ingredients_codec.dart"
      to: "food_detail / simple_food_detail / meal_section"
      via: "parseIngredientsJson"
      pattern: "แทน jsonDecode as List"
---

<objective>
อัปเดต **reader paths** ทั้งหมด (food_detail, simple_food_detail, meal_section) ให้รองรับ ingredientsJson v2 ไม่ crash + ตรวจ MyMeal path + สร้าง manual checklist ปิดเฟส 16

Purpose: ปิด ARC2-ING-04 read paths + checklist
Output: ไม่มี `jsonDecode(...) as List` ที่ไม่ผ่าน codec ในไฟล์ reader
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-08, D-09)
@lib/core/nutrition/ingredients_codec.dart
</context>

<tasks>

<task type="auto">
  <name>Task 16-04-01: food_detail_bottom_sheet reader</name>
  <read_first>
    - lib/features/health/widgets/food_detail_bottom_sheet.dart (ค้นหา "ingredientsJson" และ "jsonDecode" — อ่าน 30 บรรทัดรอบแต่ละจุด)
    - lib/core/nutrition/ingredients_codec.dart (parseIngredientsJson signature + return type)
  </read_first>
  <action>
แทนที่ `jsonDecode(entry.ingredientsJson...) as List` ด้วย `parseIngredientsJson` แล้วแปลงเป็นรายการสำหรับ UI:
1. import codec
2. เรียก `parseIngredientsJson(entry.ingredientsJson)` 
3. แยกสาขา `kind` (legacy/v2/empty) แล้วแสดงผลให้ถูก — v2 แสดง main/sub, legacy แสดง flat list
4. ต้องไม่ crash เมื่อ `ingredientsJson` เป็น object v2
  </action>
  <acceptance_criteria>
    - `rg "as List" lib/features/health/widgets/food_detail_bottom_sheet.dart` ไม่พบใน context ที่เกี่ยวกับ ingredientsJson (อาจมี as List อื่นที่ไม่เกี่ยว — ตรวจจากบริบท)
    - `rg "ingredients_codec\|parseIngredientsJson" lib/features/health/widgets/food_detail_bottom_sheet.dart` พบอย่างน้อย 1 บรรทัด
    - `flutter analyze lib/features/health/widgets/food_detail_bottom_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-04-02: simple_food_detail_sheet reader</name>
  <read_first>
    - lib/features/home/widgets/simple_food_detail_sheet.dart (ค้นหา "ingredientsJson" และ "jsonDecode")
    - lib/core/nutrition/ingredients_codec.dart
  </read_first>
  <action>
เช่นเดียวกับ 16-04-01: ใช้ parse กลาง แทน `jsonDecode as List` สำหรับจุดที่อ่าน ingredientsJson
  </action>
  <acceptance_criteria>
    - `rg "ingredients_codec\|parseIngredientsJson" lib/features/home/widgets/simple_food_detail_sheet.dart` พบอย่างน้อย 1 บรรทัด หรือไม่มีจุดอ่าน ingredientsJson ในไฟล์นี้ (บันทึกใน SUMMARY)
    - `flutter analyze lib/features/home/widgets/simple_food_detail_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-04-03: meal_section reader</name>
  <read_first>
    - lib/features/health/widgets/meal_section.dart (ค้นหา "ingredientsJson" และ "jsonDecode" — อ่าน context)
  </read_first>
  <action>
แทนที่จุด `jsonDecode` ของ ingredientsJson ที่สมมติ List ด้วย `parseIngredientsJson` กลาง
  </action>
  <acceptance_criteria>
    - `rg "jsonDecode.*ingredientsJson" lib/features/health/widgets/meal_section.dart` ไม่พบการสมมติ List อย่างเดียวโดยไม่ผ่าน codec — ต้องมี parse กลาง หรือ helper
    - `flutter analyze lib/features/health/widgets/meal_section.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-04-04: saveIngredientsAndMeal / MyMeal path</name>
  <read_first>
    - lib/features/health/providers/health_provider.dart (ค้นหา "saveIngredientsAndMeal" — อ่าน 50 บรรทัดรอบๆ)
    - lib/features/health/widgets/log_from_meal_sheet.dart (ตรวจว่าแผน 16-03 ครอบคลุมแล้วหรือไม่)
  </read_first>
  <action>
ตรวจว่า MyMeal → FoodEntry path ไม่ทำให้สัญญาขัดกับ v2:
1. ถ้า `saveIngredientsAndMeal` ยังเก็บโครงสร้างแยกจาก entry → ปรับให้สอดคล้อง v2
2. ถ้าไม่จำเป็นต้องแก้ (MyMeal เก็บข้อมูลแยกจาก FoodEntry) → บันทึกใน SUMMARY พร้อมเหตุผล
  </action>
  <acceptance_criteria>
    - `16-04-SUMMARY.md` ระบุสถานะ MyMeal vs FoodEntry (ปรับแล้ว หรือ deferred พร้อมเหตุผล)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16-04-05: 16-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 16 success criteria)
    - .planning/phases/16-ingredient-ui-ai/16-CONTEXT.md (D-11 ถึง D-14)
  </read_first>
  <action>
สร้าง `.planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` ภาษาไทย:
1. วิเคราะห์รูป → ยอดรวม entry สอดคล้อง subs (ไม่ double-count)
2. แก้ sub ใน Edit sheet → entry macro อัปเดตทันที
3. เปลี่ยนปริมาณรวม main → subs สเกลตามสัดส่วน
4. chat บันทึกอาหาร → เปิดรายละเอียดได้ ไม่ crash
5. log จาก My Meal → v2 ไม่ crash
6. เปิด entry legacy (เก่า) → แสดงผลปกติ
7. `flutter test test/core/nutrition/ingredients_codec_test.dart` ผ่าน (regression)
  </action>
  <acceptance_criteria>
    - ไฟล์ `.planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` มีอยู่
    - `rg "chat\|แชท" .planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
    - `rg "flutter test" .planning/phases/16-ingredient-ui-ai/16-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze` ทุกไฟล์ที่แก้ exit code 0
- [ ] `flutter test test/core/nutrition/ingredients_codec_test.dart` ยังผ่าน (regression จาก Phase 13)
</verification>

<success_criteria>
- ROADMAP เฟส 16 ข้อ 3 (IntentHandler/sheets สัญญาเดียว) ปิดครบ
- reader ทุกจุดอ่าน v2 ได้
</success_criteria>

<output>
After completion, create `16-04-SUMMARY.md`
</output>
