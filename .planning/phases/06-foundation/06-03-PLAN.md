---
phase: 06-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - lib/core/database/database_service.dart
  - lib/core/database/drift_extensions.dart
autonomous: true
requirements:
  - ARSCAN-14
  - ARSCAN-16
user_setup: []
must_haves:
  truths:
    - "สามารถสร้าง FoodEntry จาก ARscan โดยมี dataSource=arScan และ image paths สูงสุด 3 รูปถูกบันทึกผ่าน DatabaseService"
    - "การสร้าง FoodEntry จากแหล่งอื่น (กล้องปกติ/แกลเลอรี่) ยังทำงานได้เหมือนเดิม"
  artifacts:
    - path: "lib/core/database/database_service.dart"
      provides: "เมธอดใน DatabaseService สำหรับบันทึกและอ่าน FoodEntry รวม multi-image และ dataSource=arScan"
    - path: "lib/core/database/drift_extensions.dart"
      provides: "Drift query extensions ที่รองรับ filter ตาม DataSource.arScan"
  key_links:
    - from: "lib/core/database/database_service.dart"
      to: "lib/core/database/app_database.dart"
      via: "เข้าถึง FoodEntries table ผ่าน AppDatabase instance"
      pattern: "DatabaseService.foodEntries"
---

<objective>
เชื่อม Drift schema ใหม่ (multi-image + DataSource.arScan) เข้ากับ DatabaseService เพื่อให้ ARscan สามารถสร้าง FoodEntry ที่มี multi-image และ dataSource ได้จริง โดยไม่ทำให้ flow การบันทึกอาหารเดิมพัง

Purpose: ทำให้ความสามารถ multi-image + dataSource ถูกใช้งานได้ผ่าน DatabaseService ชั้นเดียว
Output: DatabaseService ที่รองรับการบันทึก/อ่าน FoodEntry สำหรับ ARscan
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ตรวจสอบ DatabaseService และ drift_extensions ปัจจุบัน</name>
  <files>lib/core/database/database_service.dart, lib/core/database/drift_extensions.dart</files>
  <action>ค้นหาโค้ดที่ทำหน้าที่สร้างและบันทึก FoodEntry ผ่าน DatabaseService สรุปว่าปัจจุบันมี API/เมธอดใดบ้าง (เช่น put, putAll, findAll, filter) และตรวจว่า DataSource enum ปัจจุบันถูกใช้อย่างไรในการ insert</action>
  <verify>ค้นหา "DatabaseService" และ "foodEntries" ในโปรเจกต์แล้วรวบรวมรายการไฟล์ที่เรียกใช้ครบ</verify>
  <done>มีภาพรวม API ของ DatabaseService สำหรับ FoodEntry ชัดเจน</done>
</task>

<task type="auto">
  <name>Task 2: เพิ่ม/ปรับ API ใน DatabaseService สำหรับ ARscan multi-image</name>
  <files>lib/core/database/database_service.dart, lib/core/database/drift_extensions.dart</files>
  <action>
1. ตรวจว่า insert/update flow ของ FoodEntry ผ่าน DatabaseService รองรับคอลัมน์ใหม่ (supplementaryImagePath2, supplementaryImagePath3) แล้ว — Drift generated code ควรรองรับอัตโนมัติ
2. เพิ่ม helper method (ถ้าจำเป็น) เช่น:
   - `Future<int> insertArScanEntry(FoodEntriesCompanion entry)` — ตั้ง source = DataSource.arScan.index โดย default
   - filter extension ใน drift_extensions สำหรับ `whereArScan()` — query เฉพาะ ARscan entries
3. ตรวจว่า backup/sync service ยังทำงานถูกต้องกับคอลัมน์ใหม่ (search "supplementary" / "imagePath" ใน backup_service.dart)
  </action>
  <verify>รันชุดทดสอบที่มีอยู่ (ถ้ามี) หรือเขียน test เล็ก ๆ สำหรับ insert FoodEntry จาก ARscan ด้วย 3 image paths แล้วอ่านกลับมาตรวจค่า</verify>
  <done>DatabaseService รองรับ ARscan entries ครบ</done>
</task>

<task type="auto">
  <name>Task 3: ตรวจ regression flow การบันทึกอาหารเดิม</name>
  <files>lib/core/database/database_service.dart</files>
  <action>ตรวจ flow การบันทึกอาหารด้วยกล้องปกติ/แกลเลอรี่ — ตรวจว่า Companion insert ที่ไม่ระบุ supplementaryImagePath2/3 ยังทำงานได้ (Drift ใส่ null ให้อัตโนมัติสำหรับ nullable columns)</action>
  <verify>flutter analyze ผ่าน + เขียน test สร้าง FoodEntry แบบเดิม (ไม่มี multi-image) ให้ insert สำเร็จ</verify>
  <done>ยืนยันว่าไม่มี regression ในการบันทึกอาหารจากแหล่งเดิม</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DatabaseService insert FoodEntry จาก ARscan (3 images + DataSource.arScan) ทำงานได้
- [ ] Flow การบันทึกอาหารจากกล้องปกติ/แกลเลอรี่ยังทำงานได้
- [ ] ไม่มี error runtime เกี่ยวกับคอลัมน์ multi-image หรือ DataSource
</verification>

<success_criteria>

- DatabaseService รองรับ multi-image + DataSource.arScan สำหรับ ARscan
- Flow เดิมยังคงทำงานโดยไม่ต้องปรับ UI ทันที
- Phase 6 พร้อมให้ phases ถัดไปมา plug-in ARscan ได้
  </success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-03-SUMMARY.md`
</output>
