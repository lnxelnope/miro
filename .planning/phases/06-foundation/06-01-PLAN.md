---
phase: 06-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/database/app_database.dart
  - lib/core/database/app_database.g.dart
  - lib/core/database/model_extensions.dart
autonomous: true
requirements:
  - ARSCAN-14
  - ARSCAN-16
user_setup: []
must_haves:
  truths:
    - "FoodEntry สามารถเก็บ path รูปเสริมได้สูงสุด 3 รูปต่อ entry (supplementaryImagePaths)"
    - "FoodEntry มีค่า DataSource.arScan ใน enum DataSource แยก ARscan ออกจากกล้อง/แกลเลอรี่เดิม"
  artifacts:
    - path: "lib/core/database/app_database.dart"
      provides: "Drift table FoodEntries ที่มีคอลัมน์ใหม่สำหรับ multi-image + DataSource.arScan"
    - path: "lib/core/database/model_extensions.dart"
      provides: "extensions/helpers สำหรับ FoodEntryData ที่รองรับ multi-image"
  key_links:
    - from: "lib/core/database/app_database.dart"
      to: "lib/core/database/model_extensions.dart"
      via: "table → extension mapping"
      pattern: "FoodEntryData"
---

<objective>
ขยาย Drift database schema ให้รองรับ multi-image (สูงสุด 3 รูป) และเพิ่ม DataSource.arScan ใน enum สำหรับ entries จาก ARscan โดยไม่ทำให้ข้อมูลเดิมเสียหาย

Purpose: เตรียมฐานข้อมูลและโมเดลให้พร้อมสำหรับ flow ARscan หลายมุมมอง
Output: Drift table + migration ที่รองรับฟิลด์ multi-image + DataSource.arScan
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: สำรวจ Drift schema ปัจจุบันของ FoodEntries table</name>
  <files>lib/core/database/app_database.dart, lib/core/database/model_extensions.dart</files>
  <action>อ่านโค้ด Drift table `FoodEntries` ใน app_database.dart ดูคอลัมน์ปัจจุบัน, enum DataSource ที่มี, การเก็บ imagePath, และ schema version ล่าสุด สรุปข้อจำกัดและแผนเพิ่มฟิลด์</action>
  <verify>ค้นหาคำว่า "FoodEntries" และ "DataSource" ในโค้ดแล้วเข้าใจโครงสร้างครบ</verify>
  <done>เข้าใจ Drift schema ปัจจุบันชัดเจนและมีรายการที่ต้องแก้</done>
</task>

<task type="auto">
  <name>Task 2: เพิ่มคอลัมน์ multi-image และ DataSource.arScan ใน Drift table</name>
  <files>lib/core/database/app_database.dart</files>
  <action>
1. เพิ่มคอลัมน์ nullable text ใน FoodEntries table:
   - `supplementaryImagePath2` (text nullable) — path รูปมุมที่ 2
   - `supplementaryImagePath3` (text nullable) — path รูปมุมที่ 3
   (imagePath เดิมเก็บรูปหลัก/มุมที่ 1 อยู่แล้ว)
2. เพิ่ม `arScan` ใน enum DataSource (ค่า int ถัดจากค่าล่าสุด)
3. เพิ่ม schema migration: เพิ่ม version → onUpgrade เพิ่มคอลัมน์ใหม่ด้วย `m.addColumn()`
4. ตรวจให้แน่ใจว่าค่า default เป็น null (backward-compatible กับ entry เก่า)
  </action>
  <verify>รัน `dart run build_runner build` แล้ว generated code ไม่มี error</verify>
  <done>Drift table มีคอลัมน์ใหม่และ DataSource.arScan พร้อมใช้</done>
</task>

<task type="auto">
  <name>Task 3: เพิ่ม helper extensions สำหรับ multi-image</name>
  <files>lib/core/database/model_extensions.dart</files>
  <action>เพิ่ม extensions บน FoodEntryData (alias FoodEntry) สำหรับ:
- `List<String> get allImagePaths` — รวม imagePath + supplementaryImagePath2 + supplementaryImagePath3 (กรอง null ออก)
- `bool get isArScan` — ตรวจว่า source == DataSource.arScan.index
- `bool get hasMultipleImages` — ตรวจว่ามีรูปมากกว่า 1 รูป
  </action>
  <verify>คอมไพล์ผ่าน, เขียน test เล็ก ๆ สร้าง FoodEntryData ที่มีรูป 3 มุมแล้วเรียก allImagePaths ได้ 3 items</verify>
  <done>extensions ใช้งานได้และ type-safe</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] โปรเจกต์คอมไพล์ผ่านโดยไม่มี error ที่เกี่ยวข้องกับ FoodEntries table หรือ Drift migration
- [ ] สามารถสร้าง FoodEntryData ใหม่ที่มีรูป 3 path และ source=DataSource.arScan ได้
- [ ] ข้อมูล FoodEntry เดิมยังถูกอ่านได้โดยไม่ crash (คอลัมน์ใหม่เป็น nullable)
</verification>

<success_criteria>

- Drift schema รองรับ multi-image สูงสุด 3 รูปต่อ entry
- DataSource enum มีค่า arScan แยกจากแหล่งอื่นได้ชัดเจน
- ไม่มี regression กับข้อมูลเดิมหรือ flow กล้อง/แกลเลอรี่ปัจจุบัน
  </success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-01-SUMMARY.md`
</output>
