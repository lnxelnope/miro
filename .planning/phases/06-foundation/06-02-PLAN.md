---
phase: 06-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/build.gradle.kts
  - ios/Podfile
  - ios/Runner.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - ARSCAN-03
user_setup: []
must_haves:
  truths:
    - "โปรเจกต์ build ผ่านบนทั้ง Android และ iOS หลังติดตั้ง dependency ที่จำเป็นสำหรับ ARscan"
    - "ไม่มี feature ปัจจุบัน (กล้อง, บาร์โค้ด, แกลเลอรี่, แชท) พังจากการอัปเดต dependency"
  artifacts:
    - path: "pubspec.yaml"
      provides: "รายการ dependency สำหรับ ML Kit, sensors, กล้องเวอร์ชันที่รองรับ ARscan"
    - path: "android/app/build.gradle.kts"
      provides: "การตั้งค่า Android ให้รองรับ dependency ใหม่ (minSdk, kotlin, compileOptions)"
    - path: "ios/Podfile"
      provides: "การตั้งค่า CocoaPods สำหรับ ML Kit และกล้อง"
  key_links:
    - from: "pubspec.yaml"
      to: "android/app/build.gradle.kts"
      via: "เวอร์ชันกล้อง/MLKit ที่สอดคล้องกัน"
      pattern: "camera"
---

<objective>
ติดตั้งและอัปเดต dependency ที่จำเป็นสำหรับ ARscan (กล้อง, ML Kit object detection, sensors) และยืนยันว่าโปรเจกต์ build ผ่านบนทั้ง Android และ iOS โดยไม่มี regression กับฟีเจอร์เดิม

Purpose: สร้างฐาน dependency ที่เสถียรสำหรับ phases ถัดไปที่ต้องใช้ camera stream, detection และ sensors
Output: pubspec และ native build config ที่พร้อมสำหรับ ARscan

**หมายเหตุ:** โปรเจกต์ใช้ **Drift** (SQLite) เป็น database — ตรวจให้แน่ใจว่า dependency ใหม่ไม่ conflict กับ drift/drift_sqflite/sqlite3_flutter_libs ที่มีอยู่
</objective>

<execution_context>
@/Users/nu_mac_studio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nu_mac_studio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ทบทวน dependency ปัจจุบันที่เกี่ยวกับกล้อง/ภาพและ sensors</name>
  <files>pubspec.yaml, android/app/build.gradle.kts, ios/Podfile</files>
  <action>สำรวจเวอร์ชันกล้อง, image processing, sensors ที่ใช้อยู่ รวมถึง Drift database deps (drift, drift_sqflite, sqlite3_flutter_libs) และข้อจำกัด platform (minSdk, iOS version) จดรายการ constraint ที่ต้องระวังเมื่ออัปเดต</action>
  <verify>ตรวจสอบว่าไม่มี dependency ซ้ำซ้อนหรือ conflict ที่เห็นได้ชัดใน pubspec.yaml</verify>
  <done>เข้าใจสถานะปัจจุบันของ dependency ที่เกี่ยวกับ ARscan</done>
</task>

<task type="auto">
  <name>Task 2: เพิ่ม/อัปเดต dependency สำหรับ ML Kit object detection และ sensors</name>
  <files>pubspec.yaml</files>
  <action>เพิ่มแพ็กเกจที่จำเป็น:
- `google_mlkit_object_detection` — สำหรับ on-device food detection
- `sensors_plus` — สำหรับ accelerometer (ใช้ใน Phase 9 multi-angle capture)
- อัปเดต camera plugin ไปยังเวอร์ชันที่เหมาะสม รองรับ image stream
ตรวจให้แน่ใจว่าไม่ conflict กับ drift/drift_sqflite ที่มีอยู่</action>
  <verify>รัน `flutter pub get` แล้วสำเร็จโดยไม่มี error</verify>
  <done>pubspec.yaml สะท้อน dependency ที่จำเป็นสำหรับ ARscan ครบถ้วน</done>
</task>

<task type="auto">
  <name>Task 3: ปรับ native build config สำหรับ Android/iOS ให้รองรับ dependency ใหม่</name>
  <files>android/app/build.gradle.kts, ios/Podfile, ios/Runner.xcodeproj/project.pbxproj</files>
  <action>อัปเดต minSdk, compileOptions, kotlin/gradle plugin (ถ้าจำเป็น) และ Podfile/Pod configuration ให้รองรับ ML Kit และกล้องเวอร์ชันใหม่ โดยไม่ทำให้แอปอื่น ๆ พัง ตรวจสอบ permission ที่เกี่ยวข้องถ้ามี</action>
  <verify>รัน `flutter build apk --debug` และ `flutter build ios --simulator` หรือคำสั่ง build ที่โปรเจกต์ใช้อยู่ให้ผ่านอย่างน้อย 1 รอบบนแต่ละแพลตฟอร์ม</verify>
  <done>โปรเจกต์ build ผ่านบนทั้ง Android และ iOS หลังอัปเดต dependency</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter pub get สำเร็จ
- [ ] build Android สำเร็จ (debug หรือ profile ตามที่โปรเจกต์ใช้)
- [ ] build iOS สำเร็จ (simulator หรือ device ตามที่โปรเจกต์ใช้)
- [ ] แอปยังเปิดและใช้งานฟีเจอร์เดิมได้โดยไม่มี crash จาก dependency ใหม่
</verification>

<success_criteria>

- Dependency สำหรับ ARscan ถูกเพิ่ม/อัปเดตครบ (ML Kit + sensors_plus)
- โปรเจกต์ build ผ่านบนทั้งสองแพลตฟอร์ม
- ไม่มี regression ต่อฟีเจอร์กล้อง/บาร์โค้ด/แกลเลอรี่/chat ปัจจุบัน
- Drift database deps (drift, drift_sqflite, sqlite3_flutter_libs) ยังทำงานปกติ
  </success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-02-SUMMARY.md`
</output>
