---
phase: 15-sandbox-timeline
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - lib/features/health/widgets/change_meal_bottom_sheet.dart
  - lib/features/health/widgets/meal_section.dart
  - lib/features/home/widgets/food_sandbox.dart
  - lib/features/home/presentation/basic_mode_tab.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_th.arb
autonomous: true
requirements:
  - ARC2-TIMELINE-03
user_setup:
  - "รัน flutter gen-l10n หลังแก้ ARB"
must_haves:
  truths:
    - "ChangeMealBottomSheet เป็น public widget ในไฟล์แยก import ได้จาก meal_section และ food_sandbox"
    - "เลือกหลายรายการใน FoodSandbox แล้วย้ายมื้อได้; mealType อัปเดตใน DB; timestamp คงเดิม (D-12)"
    - "Picker วันใน sheet (โหมดเดี่ยวจาก MealSection) ใช้ DatePlanningLimits เดียวกับแผน 01"
  artifacts:
    - path: "lib/features/health/widgets/change_meal_bottom_sheet.dart"
      provides: "UI เลือกมื้อ (+ วันเมื่ออนุญาต)"
  key_links:
    - from: "food_sandbox.dart"
      to: "basic_mode_tab.dart"
      via: "onChangeMealForSelected หรือชื่อเทียบเท่า"
      pattern: "updateFoodEntry + invalidate"
---

<objective>
แยก `ChangeMealBottomSheet` ออกจาก `meal_section.dart`; เพิ่มปุ่มย้ายมื้อบนแถบเลือก `FoodSandbox`; wire `BasicModeTab` ให้อัปเดตหลาย entry และ invalidate providers ตามแพทเทิร์นเดิม

Purpose: ARC2-TIMELINE-03 + D-11–D-12
Output: ไม่มี class `_ChangeMealBottomSheet` เหลือใน meal_section.dart
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-11, D-12, D-14)
@lib/features/health/widgets/meal_section.dart (บรรทัด 295–350, 1256+)
</context>

<tasks>

<task type="auto">
  <name>Task 15-02-01: สร้าง change_meal_bottom_sheet.dart</name>
  <read_first>
    - lib/features/health/widgets/meal_section.dart (ทั้งก้อน `_ChangeMealBottomSheet` ถึงก่อน class ถัดไป)
    - lib/core/constants/date_planning_limits.dart
    - .cursorrules (L10n)
  </read_first>
  <action>
1. สร้าง `lib/features/health/widgets/change_meal_bottom_sheet.dart` ด้วย public `ChangeMealBottomSheet` (หรือชื่อเดียวกัน) ย้ายโค้ดจาก private class เดิม
2. พารามิเตอร์ `bool allowDateChange` (default true เพื่อพฤติกรรมเดิมของ MealSection): เมื่อ `false` **ไม่แสดง** ส่วนเลือกวัน / ไม่เรียก `_pickDate` — สำหรับ batch จาก sandbox (D-12)
3. `_pickDate` / `showDatePicker` ใช้ `lastDate` จาก `DatePlanningLimits` เดียวกับแผน 01
4. Export/import ที่จำเป็น (L10n, AppRadius, MealType, FoodEntry, ฯลฯ)
  </action>
  <acceptance_criteria>
    - ไฟล์ `lib/features/health/widgets/change_meal_bottom_sheet.dart` มี `class ChangeMealBottomSheet`
    - `grep -n "class _ChangeMealBottomSheet" lib/features/health/widgets/meal_section.dart` ไม่พบผลลัพธ์
    - `flutter analyze lib/features/health/widgets/change_meal_bottom_sheet.dart lib/features/health/widgets/meal_section.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-02-02: MealSection ใช้ widget ใหม่ + snackbar L10n</name>
  <read_first>
    - lib/features/health/widgets/meal_section.dart (`_showChangeMealSheet`)
    - lib/l10n/app_en.arb
  </read_first>
  <action>
1. แทนที่ `showModalBottomSheet` ให้ใช้ `ChangeMealBottomSheet` จากไฟล์ใหม่ พร้อม `allowDateChange: true`
2. ข้อความ Snackbar หลังย้ายมื้อ: **ห้าม** hardcode ภาษาอังกฤษ — เพิ่ม key ใน ARB (เช่น `mealMovedToType` พารามิเตอร์ชื่อมื้อที่แปลแล้ว) และใช้ `L10n`
  </action>
  <acceptance_criteria>
    - `grep -n "Moved to" lib/features/health/widgets/meal_section.dart` ไม่พบ
    - `flutter analyze lib/features/health/widgets/meal_section.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-02-03: FoodSandbox + BasicModeTab — batch change meal</name>
  <read_first>
    - lib/features/home/widgets/food_sandbox.dart (`_buildSelectionBar`)
    - lib/features/home/presentation/basic_mode_tab.dart (`_moveEntriesToDate`, providers)
    - lib/features/health/widgets/change_meal_bottom_sheet.dart
  </read_first>
  <action>
1. เพิ่ม optional callback เช่น `Future<void> Function(List<FoodEntry> selected, MealType newType)? onChangeMealForSelected` บน `FoodSandbox` (หรือชื่อเดียวกัน)
2. บน `_buildSelectionBar` เพิ่ม `IconButton` (เช่น `Icons.swap_horiz` หรือ `restaurant`) + `tooltip` จาก L10n (เพิ่ม key ใน ARB ถ้ายังไม่มี — ห้าม hardcode)
3. เมื่อกด: เปิด `showModalBottomSheet` ด้วย `ChangeMealBottomSheet` โหมดหลายรายการ — ส่ง `foods: selectedEntries`, `allowDateChange: false` หรือ API ที่ออกแบบในงาน 15-02-01
4. `BasicModeTab`: implement callback — loop `updateFoodEntry` ตั้ง `mealType` ใหม่ **ไม่เปลี่ยน timestamp**; เรียก `invalidate` ชุดเดียวกับ `_moveEntriesToDate` / meal_section `_refreshProviders` (foodEntriesByDateProvider, healthTimelineProvider, todayCaloriesProvider, todayMacrosProvider)
  </action>
  <acceptance_criteria>
    - `grep -n "onChangeMeal" lib/features/home/widgets/food_sandbox.dart` พบพารามิเตอร์หรือการเรียก callback
    - `grep -n "ChangeMealBottomSheet" lib/features/home/presentation/basic_mode_tab.dart` พบอย่างน้อย 1 บรรทัด หรือ import ผ่าน food_sandbox เท่านั้น — อย่างน้อย `basic_mode_tab` ต้องส่ง handler เข้า `FoodSandbox`
    - `flutter analyze lib/features/home/widgets/food_sandbox.dart lib/features/home/presentation/basic_mode_tab.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-02-04: ChangeMealBottomSheet รองรับหลายรายการ</name>
  <read_first>
    - lib/features/health/widgets/change_meal_bottom_sheet.dart
  </read_first>
  <action>
ถ้ายังไม่ได้ทำใน 15-02-01: รองรับ `List<FoodEntry> entries` หรือ `int count` ใน subtitle — แสดงข้อความจาก L10n เช่น "N รายการ" เมื่อเลือกมากกว่า 1
  </action>
  <acceptance_criteria>
    - `grep -n "List<FoodEntry>\\|entries\\.length" lib/features/health/widgets/change_meal_bottom_sheet.dart` พบอย่างน้อย 1 บรรทัด **หรือ** มีพารามิเตอร์ `batchCount` / `secondaryLabel` ที่ใช้เมื่อเลือกหลายรายการ
    - `flutter gen-l10n` รันแล้วไม่ error
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze` บนไฟล์ที่แก้
- [ ] `rg "_ChangeMealBottomSheet" lib/` ไม่มีผลลัพธ์
</verification>

<success_criteria>
- ROADMAP ข้อ 3 (ย้ายมื้อจาก sandbox) ผ่าน manual
</success_criteria>

<output>
After completion, create `15-02-SUMMARY.md`
</output>
