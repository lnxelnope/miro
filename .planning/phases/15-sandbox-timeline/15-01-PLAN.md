---
phase: 15-sandbox-timeline
plan: 01
type: execute
wave: 1
depends_on:
  - ../14-app-shell-energy/14-02-PLAN.md
files_modified:
  - lib/core/constants/date_planning_limits.dart
  - lib/features/health/widgets/daily_summary_card.dart
  - lib/features/health/widgets/date_navigation_bar.dart
  - lib/features/home/widgets/food_sandbox.dart
  - lib/features/health/widgets/edit_food_bottom_sheet.dart
autonomous: true
requirements:
  - ARC2-TIMELINE-02
user_setup: []
must_haves:
  truths:
    - "มีค่าคงที่ planningHorizonDays และฟังก์ชัน/ getter สำหรับ lastDate ของ date picker และเทียบวันสิ้นสุดการเลื่อนไปข้างหน้า"
    - "DailySummaryCard ลูกศรขวาเปิดไปวันถัดไปได้เมื่อยังไม่ถึงขีดวางแผน (รวมจากวันนี้ไปพรุ่งนี้)"
    - "DateNavigationBar สำหรับ day/week/month สามารถเลื่อนไปข้างหน้าได้จนถึงขีดวางแผน และ picker ใช้ lastDate เดียวกัน"
  artifacts:
    - path: "lib/core/constants/date_planning_limits.dart"
      provides: "แหล่งเดียวสำหรับ horizon (D-06)"
  key_links:
    - from: "date_planning_limits.dart"
      to: "daily_summary_card.dart, date_navigation_bar.dart, food_sandbox.dart, edit_food_bottom_sheet.dart"
      via: "import"
      pattern: "maxPlanningDate / lastDate"
---

<objective>
นิยามขีดวันวางแผนเดียว (365 วัน) และให้ DailySummaryCard, DateNavigationBar, FoodSandbox (ย้ายวันที่), EditFoodBottomSheet ใช้ `lastDate` / ลูกศรสอดคล้องกัน — **ยังไม่** extract change-meal sheet ในแผนนี้

Purpose: ARC2-TIMELINE-02
Output: analyzer ผ่าน + grep ยืนยันไม่ใช้ `lastDate: DateTime.now()` แบบจำกัดแค่เมื่อวาน/วันนี้ในไฟล์เหล่านี้สำหรับ flow เลือกวันมื้อ
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-06–D-10, D-14)
@.planning/phases/15-sandbox-timeline/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 15-01-01: date_planning_limits.dart</name>
  <read_first>
    - .planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-06)
  </read_first>
  <action>
สร้าง `lib/core/constants/date_planning_limits.dart` ที่มีอย่างน้อย:
- `const int planningHorizonDays = 365;`
- `DateTime getMaxPlanningDate()` หรือ `DateTime maxPlanningDate` — นิยามชัดเจนว่าเป็น **วันสุดท้ายที่เลือกได้** (แนะนำ date-only 23:59:59 หรือ startOfDay ของวันที่ N — ใช้แบบเดียวกันทุกที่)
- ฟังก์ชันช่วย `bool isBeforeOrOnPlanningHorizon(DateTime d)` ถ้าจำเป็นสำหรับลูกศร
  </action>
  <acceptance_criteria>
    - ไฟล์ `lib/core/constants/date_planning_limits.dart` มี `planningHorizonDays` และอ้างอิงได้จากไฟล์อื่น
    - `flutter analyze lib/core/constants/date_planning_limits.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-01-02: DailySummaryCard</name>
  <read_first>
    - lib/features/health/widgets/daily_summary_card.dart (ลูกศร, _pickDate, บรรทัด Today)
    - lib/core/constants/date_planning_limits.dart
    - lib/l10n/app_en.arb (ค้นหา today หรือเพิ่ม key ใหม่ เช่น labelToday)
  </read_first>
  <action>
1. `showDatePicker` ใช้ `lastDate` = max ตาม `DatePlanningLimits`
2. ลูกศรขวา: เปิดใช้งานเมื่อ `dateOnly(selected) < dateOnly(maxPlanning)` (ไม่ disable เพราะ isToday อย่างเดียว); disabled เมื่อถึงขีดบน
3. ถ้าแตะไฟล์นี้: แทนที่ `'Today'` ด้วย L10n (เพิ่ม key ใน app_en.arb + app_th.arb + รัน `flutter gen-l10n` ถ้าจำเป็น)
4. วันในสัปดาห์ใน card: ใช้ locale จาก `Localizations.localeOf(context)` แทน `'en'` คงที่ใน `DateFormat` **ถ้า**แก้ในรอบนี้ (ตาม .cursorrules)
  </action>
  <acceptance_criteria>
    - `grep -n "lastDate: DateTime.now()" lib/features/health/widgets/daily_summary_card.dart` ไม่พบ (หรือเหลือเฉพาะที่ไม่ใช่ flow นี้ — ต้องไม่มีใน `_pickDate`)
    - `flutter analyze lib/features/health/widgets/daily_summary_card.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-01-03: DateNavigationBar</name>
  <read_first>
    - lib/features/health/widgets/date_navigation_bar.dart
    - lib/core/constants/date_planning_limits.dart
  </read_first>
  <action>
1. `_canGoForward`: สำหรับ `SummaryPeriod.day` ให้ true เมื่อวันถัดไปของ `selectedDate` ยังไม่เกิน max planning (ไม่ใช้แค่ `!_isCurrentPeriod`); สำหรับ week/month/year คำนวณช่วงถัดไปแล้ว clamp ไม่ให้เกิน horizon ที่สมเหตุสมผล (เช่น สิ้นเดือน/สิ้นปีที่อยู่ใน horizon)
2. `_goForward`: ห้ามส่งวันที่เกิน max planning
3. `_showPicker`: `lastDate` = max planning สำหรับ day และ month picker
4. (ถ้าทำได้ในรอบนี้) แปล tooltips `_prevTooltip` / `_nextTooltip` และข้อความ `helpText` / `All Time` ผ่าน L10n — ถ้า scope กว้างเกินไป ให้เพิ่ม TODO ใน SUMMARY และทำอย่างน้อย 1 key สำหรับ picker helpText ที่แตะแล้ว
  </action>
  <acceptance_criteria>
    - `grep -n "lastDate: DateTime.now()" lib/features/health/widgets/date_navigation_bar.dart` ไม่พบใน `_showPicker`
    - `flutter analyze lib/features/health/widgets/date_navigation_bar.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-01-04: FoodSandbox + EditFoodBottomSheet</name>
  <read_first>
    - lib/features/home/widgets/food_sandbox.dart (`_moveSelectedToDate`)
    - lib/features/health/widgets/edit_food_bottom_sheet.dart (showDatePicker บริเวณ ~2114)
    - lib/core/constants/date_planning_limits.dart
  </read_first>
  <action>
1. `FoodSandbox._moveSelectedToDate`: ตั้ง `lastDate` = max planning; `firstDate` คง 2020 หรือตามเดิม
2. `edit_food_bottom_sheet`: `lastDate` สอดคล้อง horizon เดียวกัน (D-10)
  </action>
  <acceptance_criteria>
    - `grep -n "Duration(days: 1)" lib/features/home/widgets/food_sandbox.dart` ไม่พบใน `_moveSelectedToDate` (ต้องใช้ limits แทน)
    - `flutter analyze lib/features/home/widgets/food_sandbox.dart lib/features/health/widgets/edit_food_bottom_sheet.dart` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze` บนไฟล์ที่แก้ทั้งหมด exit code 0
- [ ] `rg "lastDate: DateTime.now\\(\\)" lib/features/health/widgets/daily_summary_card.dart lib/features/health/widgets/date_navigation_bar.dart` ไม่มีผลลัพธ์
</verification>

<success_criteria>
- เลือกวันในอนาคตได้จาก summary + navigation ตาม ROADMAP ข้อ 2 (ร่วมกับแผน 02–03 สำหรับ sandbox ถ้าจำเป็น)
</success_criteria>

<output>
After completion, create `15-01-SUMMARY.md`
</output>
