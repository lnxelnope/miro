---
phase: 15-sandbox-timeline
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02-PLAN.md
files_modified:
  - lib/features/home/widgets/food_sandbox.dart
  - lib/features/health/utils/meal_type_l10n.dart
  - .planning/phases/15-sandbox-timeline/15-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - ARC2-TIMELINE-01
user_setup: []
must_haves:
  truths:
    - "รายการใน FoodSandbox จัดกลุ่มตามลำดับ breakfast → lunch → dinner → snack; มื้อว่างไม่แสดงหัวข้อ"
    - "หัวข้อมื้อใช้ mealBreakfast / mealLunch / mealDinner / mealSnack จาก L10n"
    - "แต่ละกลุ่มมีกรอบ/พื้นหลังอ่อนครอบ Wrap ของฟอง — ไม่ใช้ MealSection แบบ Pro"
  artifacts:
    - path: "lib/features/home/widgets/food_sandbox.dart"
      provides: "grouped sandbox UI"
  key_links:
    - from: "food_sandbox.dart"
      to: "lib/core/database/app_database.dart"
      via: "MealType.values"
      pattern: "MealType order"
---

<objective>
จัดเรียง `FoodSandbox` เป็น feed เดียวแยกกลุ่มมื้อเบาๆ ตาม D-01–D-05 พร้อมเอกสาร manual checklist ปิด D-14

Purpose: ARC2-TIMELINE-01
Output: UI แสดงหลายมื้อแยกเมื่อมีข้อมูลหลายมื้อในวันเดียวกัน
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-01–D-05)
@lib/features/home/widgets/food_sandbox.dart
</context>

<tasks>

<task type="auto">
  <name>Task 15-03-01: Helper MealType → L10n</name>
  <read_first>
    - lib/l10n/app_en.arb (`mealBreakfast`, `mealLunch`, `mealDinner`, `mealSnack`)
    - lib/core/database/app_database.dart (`enum MealType`)
  </read_first>
  <action>
สร้างฟังก์ชัน top-level หรือ extension เช่น `String mealTypeLabel(MealType t, L10n l10n)` ใน `lib/features/health/utils/meal_type_l10n.dart` (หรือ path ที่สอดคล้องโปรเจกต์) ที่ map แต่ละค่าไป `l10n.mealBreakfast` ฯลฯ — **ห้าม** ใช้ `MealTypeExtension.displayName` ภาษาอังกฤษสำหรับหัวข้อ UI นี้
  </action>
  <acceptance_criteria>
    - ไฟล์ helper มีอยู่และถูก import จาก `food_sandbox.dart`
    - `grep -n "mealBreakfast" lib/features/home/widgets/food_sandbox.dart` หรือ helper มีการอ้างอิง L10n มื้อ
    - `flutter analyze` ผ่าน
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-03-02: โครงสร้างกลุ่มมื้อใน build()</name>
  <read_first>
    - lib/features/home/widgets/food_sandbox.dart (build, sorted, Wrap)
    - lib/core/theme/app_tokens.dart (AppSpacing, AppRadius, สีพื้นผิว)
  </read_first>
  <action>
1. หลัง sort แบบเดิม ให้ **จัดกลุ่ม** `sorted` ตาม `entry.mealType` ตามลำดับ `MealType.values`
2. สำหรับแต่ละมื้อที่มีรายการ: แสดงแถวหัวข้อด้วย `mealTypeLabel` + `Container`/`DecoratedBox` กรอบอ่อน (border หรือ surface สีจาง) ครอบ `Wrap` ของ `_FoodBubble` ของมื้อนั้น
3. มื้อที่ว่าง: ไม่ render ส่วนใดๆ
4. ภายในแต่ละกลุ่ม ลำดับฟอง = **กฎเดิม** (รูปก่อน แล้ว timestamp desc) — ใช้การ sort ย่อยต่อกลุ่มหรือ filter จาก sorted ที่ stable
5. Empty state เมื่อ `sorted.isEmpty` คงเดิม
  </action>
  <acceptance_criteria>
    - `grep -n "MealType.values" lib/features/home/widgets/food_sandbox.dart` พบอย่างน้อย 1 บรรทัด
    - `grep -n "MealSection" lib/features/home/widgets/food_sandbox.dart` ไม่พบ (ห้ามดึง layout Pro)
    - `flutter analyze lib/features/home/widgets/food_sandbox.dart` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-03-03: Optional D-13 drag-and-drop</name>
  <read_first>
    - .planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-13)
  </read_first>
  <action>
ถ้ามีเวลา: สำรวจ `ReorderableWrap` / `DragTarget` สำหรับลากฟองข้ามมื้อ — **ถ้าไม่ทัน** ให้บันทึกใน SUMMARY ว่า deferred และ D-11 ยังครอบคลุม REQ
  </action>
  <acceptance_criteria>
    - `15-03-SUMMARY.md` ระบุชัดว่า D-13 ทำหรือไม่ทำ
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15-03-04: 15-MANUAL-CHECKLIST.md</name>
  <read_first>
    - .planning/phases/15-sandbox-timeline/15-CONTEXT.md (D-14)
  </read_first>
  <action>
สร้าง `.planning/phases/15-sandbox-timeline/15-MANUAL-CHECKLIST.md` รายการตรวจมือ 3 ข้อหลักของ D-14 ภาษาไทย
  </action>
  <acceptance_criteria>
    - ไฟล์มีอยู่และมีคำว่า `กลุ่มมื้อ` หรือ `meal` / `มื้อ` อย่างน้อย 1 ครั้ง
    - `grep -n "อนาคต\\|พรุ่งนี้\\|future" .planning/phases/15-sandbox-timeline/15-MANUAL-CHECKLIST.md` พบอย่างน้อย 1 บรรทัด
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `flutter analyze lib/features/home/widgets/food_sandbox.dart` 
- [ ] Scroll บน Basic home ยังใช้งานได้ (ไม่ทำให้ overflow ผิดปกติ)
</verification>

<success_criteria>
- ROADMAP success ข้อ 1 สำหรับกลุ่มมื้อ
</success_criteria>

<output>
After completion, create `15-03-SUMMARY.md`
</output>
