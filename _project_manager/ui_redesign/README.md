# UI Redesign Tasks - สำหรับ Junior Developer

> **📌 สำคัญมาก:** อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้ง

## 🎯 ภาพรวมโปรเจค

เปลี่ยน UI แอป MIRO ให้สวยขึ้นแบบ Airbnb-inspired โดย **ห้ามแก้ logic** ทั้งหมดต้องทำงานเหมือนเดิม 100%

## 📂 โครงสร้างโฟลเดอร์นี้

```
_project_manager/ui_redesign/
├── README.md                    ← ไฟล์นี้
├── TASK_1_THEME.md              ← Phase 1: เปลี่ยนสีและ theme (ง่าย - 4 ชม)
├── TASK_2_SUMMARY_CARD.md       ← Phase 2: Redesign summary card (ปานกลาง - 1 วัน)
├── TASK_3_HORIZONTAL_TIMELINE.md ← Phase 3: Timeline แนวนอน (ปานกลาง-ยาก - 1.5 วัน)
├── TASK_4_BOTTOM_NAV.md         ← Phase 4: BottomNavBar (Senior ทำ - 1 วัน)
└── TASK_5_POLISH.md             ← Phase 5: ปรับแต่งให้สวย (ง่าย - 4 ชม)
```

## 🚨 กฎเหล็ก - ห้ามฝ่าฝืนเด็ดขาด

### ❌ ไฟล์ที่ห้ามแตะ
- `lib/core/ai/` → AI services
- `lib/core/database/` → Database
- `lib/core/services/` → Services ทั้งหมด
- `lib/features/*/providers/` → **Providers ห้ามแก้แม้แต่ 1 บรรทัด**
- `lib/features/*/models/` → Models
- `lib/features/camera/` → Camera
- `lib/features/scanner/` → Scanner
- `lib/features/chat/` → Chat
- `lib/features/energy/` → Energy system
- `pubspec.yaml` → ห้ามเพิ่ม dependency

### ✅ ที่แก้ได้
- `lib/core/theme/` (2 ไฟล์) → สี + theme
- `lib/features/health/widgets/` (4-5 ไฟล์) → widget ที่แสดงผล
- `lib/features/health/presentation/` (3 ไฟล์) → หน้าจอ
- `lib/features/home/presentation/` (1 ไฟล์) → HomeScreen

## 📋 ลำดับการทำงาน

### แผน A: Junior คนเดียว (ทำตามลำดับ)
1. TASK_1 → TASK_2 → TASK_3 → (รอ Senior ทำ TASK_4) → TASK_5
2. เวลารวม: ~3.5 วัน + 1 วัน (Senior)

### แผน B: Junior 2 คน (ทำ parallel)
- **Junior A:** TASK_1 (0.5 วัน) → TASK_5 (0.5 วัน) = 1 วัน
- **Junior B:** TASK_2 (1 วัน) → TASK_3 (1.5 วัน) = 2.5 วัน
- **Senior:** TASK_4 (1 วัน) หลัง TASK_1+3 เสร็จ
- เวลารวม: 2.5 วัน

## 🔧 การเริ่มต้น

### ขั้นตอนที่ 1: เช็ค branch
```bash
cd c:\aiprogram\miro
git status
# ต้องอยู่ใน branch main หรือ fix/play-console-android15 ที่ merge แล้ว
```

### ขั้นตอนที่ 2: สร้าง branch ใหม่
```bash
git checkout main
git pull
git checkout -b feature/airbnb-redesign
```

### ขั้นตอนที่ 3: เปิดอ่าน Task แรก
เปิด `TASK_1_THEME.md` แล้วทำตามทุกขั้นตอน

## 📝 วิธีทำงานกับแต่ละ Task

แต่ละไฟล์ TASK_X จะมี:
1. **เป้าหมาย** — ทำอะไร
2. **ไฟล์ที่ต้องแก้** — ระบุชัดเจน path + line number
3. **โค้ดแบบ Before/After** — copy-paste ได้เลย
4. **Checklist** — check ✅ เมื่อทำเสร็จ
5. **Testing** — test อะไรบ้าง
6. **Git Commit** — message ที่ใช้

## 🧪 การทดสอบ (ทำหลังทุก Task)

### Test พื้นฐาน
```bash
# 1. Build ต้องผ่าน
flutter clean
flutter pub get
flutter build apk --debug

# 2. Run บนเครื่อง
flutter run
```

### Test ด้วยมือ (ทุก Task)
- [ ] เปิดแอป ไม่ crash
- [ ] เปลี่ยน Dark mode ไม่แตก
- [ ] กดทุกปุ่มในหน้าที่แก้ ทำงานปกติ
- [ ] Scroll ขึ้นลง ไม่ overflow

## 🆘 เมื่อเจอปัญหา

### ถ้า Build ไม่ผ่าน
1. อ่าน error message ให้ดี
2. เช็คว่าลืม import อะไรไหม
3. เช็ค syntax: `}`, `)`, `]` ครบไหม
4. Run `flutter clean` แล้ว `flutter pub get`

### ถ้าไม่แน่ใจ
1. อ่าน comment ในโค้ดที่ให้มา
2. ดู example ใน task file
3. ถาม Senior (แต่ต้องลองทำตาม task ก่อน)

### ถ้าจะแก้อะไรนอกจาก task
**ห้าม** — ทำตาม task เท่านั้น อย่าแก้อะไรเพิ่ม

## 📸 Reference Design

ดูภาพ reference ได้ที่:
```
C:\Users\ASUS\.cursor\projects\c-aiprogram-miro\assets\
c__Users_ASUS_AppData_Roaming_Cursor_User_workspaceStorage_
b66bc50cb66eb56c685c69efd0b27937_images_
image-289f7f95-8e81-443f-a551-d0ec95caadab.png
```

## ✅ เมื่อเสร็จทุก Task

1. Push branch
```bash
git push origin feature/airbnb-redesign
```

2. แจ้ง Senior ตรวจ
3. รอ feedback
4. แก้ตาม feedback (ถ้ามี)

---

**หมายเหตุ:** ถ้ายังไม่มั่นใจ อ่าน `../UI_REDESIGN_PLAN.md` เพิ่มเติม (แต่ละเอียดมาก อ่านตอน need-to-know เท่านั้น)
