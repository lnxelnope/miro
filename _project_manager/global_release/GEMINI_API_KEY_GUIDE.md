# คู่มือการสร้าง Gemini API Key (สำหรับผู้ใช้)

> เอกสารนี้ใช้เป็น reference สำหรับเขียน in-app guide ใน `api_key_screen.dart`
> และอาจใช้เป็นหน้า Help บนเว็บไซต์ด้วย

---

## ทำไมต้องมี API Key?

แอปใช้ **Gemini AI** ของ Google เพื่อวิเคราะห์รูปอาหารและคำนวณแคลอรี่ให้อัตโนมัติ
เพื่อความปลอดภัยและความเป็นส่วนตัว เราให้คุณใช้ **API Key ของคุณเอง** (BYOK — Bring Your Own Key)

**ข้อดี:**
- ข้อมูลของคุณไม่ผ่าน server ของเรา
- คุณควบคุมการใช้งานเอง 100%
- **Gemini API มี Free Tier** — ใช้ฟรีไม่ต้องจ่ายเงิน

---

## ขั้นตอนการสร้าง API Key (5 นาที)

### ขั้นตอนที่ 1: เปิด Google AI Studio

เปิดเว็บเบราว์เซอร์ (Chrome, Safari, Edge) แล้วไปที่:

**https://aistudio.google.com/apikey**

> หรือกดปุ่ม "เปิด Google AI Studio" ในแอปได้เลย

---

### ขั้นตอนที่ 2: ล็อกอิน Google Account

- ใช้ **Gmail** หรือ **Google Account** ที่มีอยู่
- ถ้ายังไม่มี สร้างได้ฟรีที่ accounts.google.com

> *(แสดง screenshot หน้า Google Sign-in)*

---

### ขั้นตอนที่ 3: คลิก "Create API Key"

หลังล็อกอินจะเห็นหน้า API Keys

- คลิกปุ่ม **"Create API Key"** (สีน้ำเงิน)
- ถ้ามี popup ถามให้เลือก Project → เลือก **"Create API key in new project"**

> *(แสดง screenshot ปุ่ม Create API Key)*

---

### ขั้นตอนที่ 4: คัดลอก API Key

จะมี Key ขึ้นมาในกล่องข้อความ หน้าตาประมาณ:
```
AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

- กด **ปุ่มคัดลอก** (Copy) ข้างๆ กล่อง
- หรือ ลากเลือกทั้งหมด → คัดลอก

> *(แสดง screenshot Key ที่สร้างเสร็จ + ปุ่ม Copy)*

---

### ขั้นตอนที่ 5: วาง Key ในแอป

กลับมาที่แอป:
1. ไปที่ **โปรไฟล์ → Gemini API Key**
2. กด **ปุ่มวาง** (📋) หรือ กดค้างในช่อง → Paste
3. กด **"บันทึก API Key"**
4. กด **"ทดสอบการเชื่อมต่อ"** → เห็น "สำเร็จ" ✅

**เสร็จแล้ว!** ตอนนี้คุณใช้ถ่ายรูปอาหาร → AI วิเคราะห์ได้แล้ว

---

## คำถามที่พบบ่อย (FAQ)

### Q: API Key ฟรีจริงไหม?
**ฟรีจริง!** Gemini 2.0 Flash ให้ใช้ฟรี:
- 15 requests/นาที
- 1,500 requests/วัน
- 1,000,000 tokens/นาที

สำหรับการใช้บันทึกอาหาร (ประมาณ 5-15 ครั้ง/วัน) → **ฟรีตลอด**

### Q: ปลอดภัยไหม?
- API Key เก็บใน **Secure Storage** ในเครื่องของคุณเท่านั้น
- แอปไม่ส่ง Key ไปที่ server ของเรา
- รูปอาหารจะถูกส่งไป Google Gemini API โดยตรง (เหมือนคุณใช้ Google เอง)
- Key ของคุณ ≠ รหัสผ่าน Google (ถ้าหลุดก็ลบทิ้งสร้างใหม่ได้)

### Q: ถ้าไม่สร้าง Key ล่ะ?
ยังใช้แอปได้! แต่:
- ❌ ไม่สามารถถ่ายรูป → วิเคราะห์อัตโนมัติ
- ❌ ไม่สามารถค้นหาด้วย Gemini
- ✅ บันทึกอาหารด้วยมือได้
- ✅ Quick Add ได้
- ✅ สร้าง My Meal ได้
- ✅ ดูสรุป kcal/macro ได้

### Q: ถ้าอยากลบ Key?
ไปที่ **โปรไฟล์ → Gemini API Key → กดลบ** (🗑️)
หรือไปลบที่ aistudio.google.com/apikey ได้โดยตรง

### Q: Gemini API จะเสียเงินเมื่อไหร่?
ถ้าใช้เกิน free tier (1,500 ครั้ง/วัน) → Google จะ block request ไม่ให้เรียก
**ไม่มีการเรียกเก็บเงินอัตโนมัติ** ยกเว้นคุณไปเปิด billing เอง
สำหรับการใช้งานปกติ → **ฟรีตลอด ไม่ต้องกังวล**

### Q: ต้องมีบัตรเครดิตไหม?
**ไม่ต้อง** — สร้าง API Key ได้ฟรีโดยไม่ต้องใส่บัตรเครดิต

---

## สำหรับ Developer — Implementation Notes

### In-App Guide UI Spec

ใช้ `Stepper` widget หรือ custom `PageView`:

```
[Step 1] เปิด Google AI Studio     [ปุ่ม: เปิดเว็บไซต์]
    ↓
[Step 2] ล็อกอิน Google Account    [รูปตัวอย่าง]
    ↓
[Step 3] คลิก Create API Key       [รูปตัวอย่าง]
    ↓
[Step 4] คัดลอก Key                [รูปตัวอย่าง]
    ↓
[Step 5] วางในแอป → บันทึก         [ช่องกรอก + ปุ่มวาง]
    ↓
[Step 6] ทดสอบ → สำเร็จ!          [ปุ่มทดสอบ + สถานะ]
```

### Screenshots ที่ต้องทำ (assets/images/guide/)

| ไฟล์ | เนื้อหา |
|------|--------|
| `step1_aistudio.png` | หน้า aistudio.google.com/apikey |
| `step2_login.png` | หน้า Google Sign-in |
| `step3_create_key.png` | ปุ่ม Create API Key |
| `step4_copy_key.png` | Key ที่สร้างเสร็จ + ปุ่ม Copy |

> ถ่าย screenshot จริงจากเบราว์เซอร์ ครอปให้เห็นส่วนสำคัญ
> ขนาดแนะนำ: 720×400 px หรือใกล้เคียง

### Test Connection Implementation

```dart
// api_key_screen.dart → _testConnection()
Future<void> _testConnection() async {
  setState(() => _isLoading = true);
  try {
    final success = await GeminiService.testConnection();
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text(success
            ? '✅ เชื่อมต่อสำเร็จ! พร้อมใช้งาน'
            : '❌ เชื่อมต่อไม่สำเร็จ — ตรวจสอบ API Key'),
          backgroundColor: success ? AppColors.success : AppColors.error,
        ),
      );
    }
  } catch (e) {
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text('❌ เกิดข้อผิดพลาด: $e'),
          backgroundColor: AppColors.error,
        ),
      );
    }
  } finally {
    setState(() => _isLoading = false);
  }
}
```

### No-Key Banner (สำหรับ Timeline)

```dart
// แสดงใน health_timeline_tab.dart ถ้ายังไม่มี Key
FutureBuilder<bool>(
  future: GeminiService.hasApiKey(),
  builder: (context, snapshot) {
    if (snapshot.data == true) return const SizedBox.shrink();
    return Container(
      margin: const EdgeInsets.all(16),
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: AppColors.primaryLight,
        borderRadius: BorderRadius.circular(12),
        border: Border.all(color: AppColors.primary.withOpacity(0.3)),
      ),
      child: Row(
        children: [
          const Icon(Icons.smart_toy, color: AppColors.primary),
          const SizedBox(width: 12),
          const Expanded(
            child: Text('ตั้งค่า Gemini AI\nเพื่อวิเคราะห์อาหารจากรูปถ่าย'),
          ),
          TextButton(
            onPressed: () => Navigator.push(context, ...ApiKeyScreen),
            child: const Text('ตั้งค่า'),
          ),
        ],
      ),
    );
  },
)
```
