# Miro v1.0 Launch Roadmap — แอปบันทึกแคลอรี่อาหาร

> **โมเดลธุรกิจ:** Freemium — โหลดฟรี + In-App Purchase ปลดล็อค Pro (199-299 บาท จ่ายครั้งเดียว)
> **Free tier:** บันทึกอาหารด้วยมือไม่จำกัด + AI วิเคราะห์ 3 ครั้ง/วัน
> **Pro tier:** AI ไม่จำกัด (จ่ายครั้งเดียว ใช้ตลอด)
> **API Model:** BYOK (Bring Your Own Key) — ทั้ง Free และ Pro ต้องใส่ Gemini API Key เอง (ฟรี)
> **ฟีเจอร์ที่เปิด:** เฉพาะบันทึกอาหาร/แคลอรี่ (ซ่อน Finance, Tasks)
> **สถานะ:** ฟีเจอร์หลักพร้อม ~85%
> **รายละเอียด Monetization:** ดู `MONETIZATION_FREEMIUM_PLAN.md`

---

## สารบัญ

1. [Phase 1 — ซ่อนฟีเจอร์ที่ไม่ใช้](#phase-1)
2. [Phase 2 — BYOK: คู่มือ API Key + ปรับ UX ให้ชัดเจน](#phase-2)
3. [Phase 2.5 — Freemium + In-App Purchase](#phase-2-5)
4. [Phase 3 — Onboarding + UX ผู้ใช้ใหม่](#phase-3)
5. [Phase 4 — ลบ Debug Code + Production Hardening](#phase-4)
6. [Phase 5 — Branding (Icon, Splash, ชื่อ)](#phase-5)
7. [Phase 6 — Legal & Privacy Policy](#phase-6)
8. [Phase 7 — Testing & QA](#phase-7)
9. [Phase 8 — Build & Publish to Google Play (FREE + IAP)](#phase-8)
10. [ไฟล์ที่เกี่ยวข้องทั้งหมด](#files)
10. [Timeline ประมาณการ](#timeline)

---

<a id="phase-1"></a>
## Phase 1 — ซ่อนฟีเจอร์ที่ไม่ใช้ (~2-3 ชั่วโมง)

### 1.1 ลบ Bottom Navigation Bar — เหลือ Health เดียว

**ไฟล์:** `lib/features/home/presentation/home_screen.dart`

ปัจจุบัน: 3 Tab → Finance | **Health** | Tasks
เป้าหมาย: **แสดง Health Tab เดียว** (ไม่ต้องมี Bottom Nav)

**สิ่งที่ต้องทำ:**
- [ ] ลบ `_buildBottomNav()` ออกจาก Scaffold
- [ ] เปลี่ยน `body: IndexedStack(...)` → `body: const HealthPage()`
- [ ] ลบ `_currentIndex`, `_pages`, `_pageContextMap`
- [ ] ลบ import `FinancePage`, `TasksPage`
- [ ] ลบปุ่ม Notification ใน AppBar (ยังเป็น "Phase 2" mock)
- [ ] ปรับ `_openMenu()` — ลบ/เปลี่ยนเมนูให้เหมาะสม

### 1.2 ลด Health Sub-Tabs เหลือ 3

**ไฟล์:** `lib/features/health/presentation/health_page.dart`

ปัจจุบัน: 6 Tab → Timeline | Diet | My Meal | ~~Workout~~ | ~~Other~~ | ~~Lab~~
เป้าหมาย: **3 Tab** → Timeline | Diet | My Meal

**สิ่งที่ต้องทำ:**
- [ ] `TabController(length: 6)` → `TabController(length: 3)`
- [ ] ลบ Tab ของ Workout, Other, Lab
- [ ] ลบ import `health_workout_tab.dart`, `health_other_tab.dart`, `health_lab_tab.dart`

### 1.3 Chat Intent — เหลือแค่ food/health

**ไฟล์:** `lib/features/chat/services/intent_handler.dart`

- [ ] ลบ/comment `_handleFinance()`, `_handleTask()`
- [ ] ให้ตอบ "ขออภัย ฟังก์ชันนี้ยังไม่พร้อมในเวอร์ชันนี้" ถ้าผู้ใช้พิมพ์เรื่องอื่น

### 1.4 Profile — ซ่อนเมนูที่ไม่เกี่ยว

**ไฟล์:** `lib/features/profile/presentation/profile_screen.dart`

- [ ] ~~📅 Google Calendar~~ → ซ่อน
- [ ] ~~Health Connect~~ → ซ่อน
- [ ] ~~📊 สรุปสัปดาห์ (Insights)~~ → ซ่อน (ถ้าข้อมูลยังไม่ครบ)
- [ ] ~~📸 การสแกนรูปภาพ~~ → พิจารณา: ถ้าเกี่ยว food scan เก็บไว้, ไม่เกี่ยวลบ

### 1.5 MagicButton Context

**ไฟล์:** `lib/features/home/widgets/magic_button.dart`

- [ ] Fix `activePageContextProvider` ให้เป็น `'health'` เสมอ (เพราะไม่มี tab อื่นแล้ว)

---

<a id="phase-2"></a>
## Phase 2 — BYOK: คู่มือ API Key + ปรับ UX (~3-4 ชั่วโมง)

### โมเดลที่ใช้
```
ผู้ใช้สร้าง Gemini API Key ฟรี → ใส่ในแอป → ใช้ AI วิเคราะห์อาหารได้
ไม่มี Key → ยังบันทึกอาหารด้วยมือ/ค้นจาก DB ได้ (แต่ไม่มี AI)
```

### 2.1 ปรับหน้า API Key ให้เป็นคู่มือ Step-by-Step

**ไฟล์:** `lib/features/profile/presentation/api_key_screen.dart`

ปัจจุบัน: คำแนะนำแค่ 3 บรรทัด → ไม่พอสำหรับผู้ใช้ทั่วไป

**เปลี่ยนเป็นคู่มือแบบ Step-by-Step พร้อมรูป:**

```
Step 1: เปิด Google AI Studio
        → ปุ่ม "เปิดเว็บไซต์" (url_launcher)
        → หมายเหตุ: ต้องมี Google Account (Gmail)

Step 2: ล็อกอิน Google Account
        → รูปตัวอย่างหน้าล็อกอิน

Step 3: คลิก "Get API Key" (มุมซ้ายบน)
        → รูปตัวอย่าง screenshot

Step 4: คลิก "Create API Key"
        → เลือก project ใดก็ได้ หรือสร้างใหม่
        → รูปตัวอย่าง screenshot

Step 5: คัดลอก Key → กลับมาวางในแอป
        → ปุ่ม "วาง (Paste)" อัตโนมัติ

Step 6: กดบันทึก → ทดสอบ → เสร็จ!
```

**สิ่งที่ต้องทำ:**
- [ ] เขียน UI คู่มือ step-by-step (Stepper หรือ PageView)
- [ ] สร้าง screenshot ตัวอย่างแต่ละขั้นตอน → ใส่ใน `assets/images/guide/`
- [ ] ปุ่ม "เปิด Google AI Studio" → `url_launcher` ไปที่ `https://aistudio.google.com/apikey`
- [ ] เน้นว่า **API Key ฟรี** (Gemini 2.0 Flash มี free tier 1,500 req/day)
- [ ] เพิ่มส่วน FAQ: "API Key คืออะไร?", "ปลอดภัยไหม?", "ฟรีจริงไหม?"

### 2.2 Implement ปุ่ม "ทดสอบการเชื่อมต่อ"

**ไฟล์:** `lib/features/profile/presentation/api_key_screen.dart`

ปัจจุบัน: `_testConnection()` ยังเป็น TODO → Coming Soon

**เปลี่ยนเป็น:**
- [ ] เรียก `GeminiService.testConnection()` (มีอยู่แล้วใน gemini_service.dart line 18-29)
- [ ] แสดงผล: สำเร็จ/ไม่สำเร็จ + สาเหตุ (key ไม่ถูก, network error, etc.)

### 2.3 Graceful Degradation — ไม่มี Key ก็ใช้ได้

ทุกจุดที่เรียก Gemini ต้องจัดการกรณีไม่มี Key อย่างดี:

| จุด | ปัจจุบัน | ต้องเปลี่ยนเป็น |
|-----|---------|----------------|
| ถ่ายรูปอาหาร → วิเคราะห์ | Throw error | แสดง dialog แนะนำตั้งค่า API Key |
| แชท → Gemini lookup | Error | ใช้ local DB fallback + แจ้งว่า "ไม่มี AI" |
| กด Gemini ในรายละเอียด | Error | แสดง dialog + ปุ่ม "ไปตั้งค่า API Key" |
| Quick Add | ไม่ต้องใช้ AI | ทำงานปกติ |
| เพิ่มอาหารด้วยมือ | ไม่ต้องใช้ AI | ทำงานปกติ |

**ไฟล์ที่ต้องตรวจ:**
- [ ] `lib/core/ai/gemini_service.dart` — error message ชัดเจน
- [ ] `lib/features/health/widgets/food_detail_bottom_sheet.dart` — กรณีกด Gemini
- [ ] `lib/features/health/widgets/gemini_analysis_sheet.dart` — กรณี no key
- [ ] `lib/features/health/presentation/food_preview_screen.dart` — กรณีถ่ายรูป
- [ ] `lib/features/chat/services/intent_handler.dart` — กรณี chat

### 2.4 เพิ่ม Banner/Hint ใน Timeline ถ้ายังไม่มี Key

เมื่อเปิดแอปครั้งแรก + ยังไม่มี API Key → แสดง banner:
```
🔑 ตั้งค่า Gemini AI เพื่อวิเคราะห์อาหารจากรูปถ่ายอัตโนมัติ
   [ตั้งค่าเลย] [ข้าม]
```

---

<a id="phase-2-5"></a>
## Phase 2.5 — Freemium + In-App Purchase (~1.5-2 วัน)

> **รายละเอียดเต็ม:** ดู `MONETIZATION_FREEMIUM_PLAN.md`

### โมเดล
- **Free:** บันทึกอาหารด้วยมือไม่จำกัด + AI วิเคราะห์ 3 ครั้ง/วัน
- **Pro (199-299 บาท จ่ายครั้งเดียว):** AI ไม่จำกัด

### สิ่งที่ต้องทำ

- [ ] เพิ่ม dependency: `in_app_purchase: ^3.2.0`
- [ ] สร้าง `lib/core/services/usage_limiter.dart` — นับ AI calls/วัน
- [ ] สร้าง `lib/core/services/purchase_service.dart` — จัดการ Google Play IAP
- [ ] เพิ่ม gate check ก่อนทุกจุดที่เรียก Gemini (~6 จุด):
  - `food_preview_screen.dart`
  - `food_detail_bottom_sheet.dart`
  - `gemini_analysis_sheet.dart`
  - `create_meal_sheet.dart`
  - `edit_food_bottom_sheet.dart`
  - `intent_handler.dart`
- [ ] สร้าง Upsell Dialog (แสดงเมื่อใช้ AI ครบ 3 ครั้ง)
- [ ] สร้าง Upsell Banner ใน Timeline (แสดงจำนวนครั้งที่เหลือ)
- [ ] เพิ่มปุ่ม "อัปเกรด Pro" ใน Profile
- [ ] เพิ่มปุ่ม "Restore Purchase" ใน Profile (สำหรับเปลี่ยนเครื่อง)
- [ ] เรียก `PurchaseService.initialize()` ใน `main.dart`
- [ ] ตั้ง Product ID `miro_cal_pro` (Non-consumable) ใน Google Play Console

---

<a id="phase-3"></a>
## Phase 3 — Onboarding + UX ผู้ใช้ใหม่ (~1-2 วัน)

### 3.1 Welcome Screen (สร้างใหม่)

**สร้างไฟล์:** `lib/features/onboarding/presentation/onboarding_screen.dart`

PageView 4 หน้า:

**หน้า 1 — Welcome**
- ชื่อแอป + tagline
- "บันทึกอาหารง่ายๆ ด้วย AI"
- ภาพประกอบสวยๆ

**หน้า 2 — ฟีเจอร์หลัก 3 อย่าง**
- ถ่ายรูป → AI วิเคราะห์อัตโนมัติ
- พิมพ์แชท → บันทึกง่าย
- ดูสรุป kcal / macro ทุกวัน

**หน้า 3 — กรอกข้อมูลพื้นฐาน**
- ชื่อ, เพศ, อายุ, น้ำหนัก, ส่วนสูง
- ระดับกิจกรรม (นั่งทั้งวัน / เบา / ปานกลาง / หนัก)
- → คำนวณ TDEE แนะนำเป้าหมาย kcal

**หน้า 4 — ตั้งค่า API Key (optional)**
- คำอธิบายสั้น: "ตั้งค่า Gemini AI เพื่อวิเคราะห์อาหารจากรูป"
- ปุ่ม "ตั้งค่าเลย" → ไปหน้า API Key
- ปุ่ม "ข้ามไปก่อน" → เข้าแอปเลย

### 3.2 เพิ่ม Fields ใน UserProfile

**ไฟล์:** `lib/features/profile/models/user_profile.dart`

```dart
// เพิ่ม fields ใหม่
String? gender;            // male / female
int? age;
double? weight;            // kg
double? height;            // cm
double? targetWeight;      // kg (optional)
String? activityLevel;     // sedentary / light / moderate / active / very_active
bool onboardingComplete = false;
```

> **สำคัญ:** หลังเพิ่ม field ต้อง run `dart run build_runner build`
> เพื่อ regenerate `user_profile.g.dart`

### 3.3 TDEE Calculator

**สร้างไฟล์:** `lib/core/utils/tdee_calculator.dart`

สูตร Mifflin-St Jeor:
```
ชาย:  BMR = 10 × น้ำหนัก(kg) + 6.25 × ส่วนสูง(cm) - 5 × อายุ + 5
หญิง: BMR = 10 × น้ำหนัก(kg) + 6.25 × ส่วนสูง(cm) - 5 × อายุ - 161

TDEE = BMR × ตัวคูณกิจกรรม:
  นั่งทั้งวัน (sedentary):    × 1.2
  เบา (light):              × 1.375
  ปานกลาง (moderate):       × 1.55
  หนัก (active):            × 1.725
  หนักมาก (very_active):    × 1.9
```

### 3.4 แก้ main.dart — Onboarding Gate

**ไฟล์:** `lib/main.dart`

```dart
home: FutureBuilder<bool>(
  future: _checkOnboardingComplete(),
  builder: (context, snapshot) {
    if (snapshot.connectionState != ConnectionState.done) {
      return const SplashScreen(); // หรือ loading
    }
    if (snapshot.data == true) return const HomeScreen();
    return const OnboardingScreen();
  },
)
```

---

<a id="phase-4"></a>
## Phase 4 — ลบ Debug Code + Production Hardening (~3-4 ชั่วโมง)

### 4.1 สร้าง Logger Utility

**สร้างไฟล์:** `lib/core/utils/logger.dart`

```dart
import 'package:flutter/foundation.dart';

class AppLogger {
  static void info(String message) {
    if (kDebugMode) debugPrint('[INFO] $message');
  }
  static void error(String message) {
    if (kDebugMode) debugPrint('[ERROR] $message');
  }
  static void warn(String message) {
    if (kDebugMode) debugPrint('[WARN] $message');
  }
}
```

### 4.2 Replace debugPrint ทั้งโปรเจค

~300 จุดใน 32 ไฟล์ → Replace `debugPrint(` → `AppLogger.info(`
(หรือ `.error(` / `.warn(` ตามเนื้อหา)

### 4.3 แก้ TODO / Coming Soon ที่ user เห็น

| จุด | ไฟล์ | Action |
|-----|------|--------|
| "ทดสอบการเชื่อมต่อ - Coming Soon" | api_key_screen.dart | **Implement** (Phase 2.2) |
| "ล้างข้อมูลทั้งหมด" TODO | profile_screen.dart | **Implement** ด้วย Isar clear |
| "Coming Soon" Export/Import | profile_screen.dart | **Implement basic export** หรือซ่อน |
| "Notifications - Phase 2" | home_screen.dart | **ลบปุ่ม** (ซ่อนแล้วใน Phase 1) |
| "Coming Soon" Privacy Policy | profile_screen.dart | **Implement** (Phase 6) |

### 4.4 Error Handling ครอบคลุม

ตรวจทุกจุดที่เรียก network / DB:
- [ ] `gemini_service.dart` — timeout, quota exceeded, invalid key
- [ ] `intent_handler.dart` — parse error
- [ ] `database_service.dart` — DB corruption
- [ ] ทุก `showModalBottomSheet` — เช็ค `mounted`

### 4.5 ลบไฟล์ / Dependencies ที่ไม่ใช้

พิจารณาลบจาก `pubspec.yaml`:
- `googleapis` — ถ้าไม่ใช้ Google Calendar แล้ว
- `google_sign_in` / `extension_google_sign_in_as_googleapis_auth` — ถ้าไม่ใช้
- `table_calendar` — ถ้าไม่มี Tasks tab แล้ว

> **ระวัง:** ลบ dependency ต้องลบ import ที่อ้างด้วย

---

<a id="phase-5"></a>
## Phase 5 — Branding & Visual (~1 วัน)

### 5.1 ตั้งชื่อแอปใหม่

ชื่อปัจจุบัน: "Miro Hybrid" → ไม่สื่อถึงอาหาร
แนะนำ: ชื่อที่สื่อถึงอาหาร/สุขภาพ/แคลอรี่
ตัวอย่าง: "Miro Cal", "กินดี", "CalSnap", "FoodLog AI" ฯลฯ

**ไฟล์ที่ต้องแก้:**
- [ ] `android/app/src/main/AndroidManifest.xml` → `android:label`
- [ ] `pubspec.yaml` → `name`
- [ ] `lib/main.dart` → `MaterialApp(title: ...)`
- [ ] AppBar title ใน `home_screen.dart`

### 5.2 App Icon

- [ ] ออกแบบ icon 512×512 (สื่อถึงอาหาร + AI)
- [ ] ใส่ `assets/icon/app_icon.png`
- [ ] ใช้ `flutter_launcher_icons` สร้างทุกขนาด

```yaml
# pubspec.yaml เพิ่ม
dev_dependencies:
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: true
  image_path: "assets/icon/app_icon.png"
```

Run: `dart run flutter_launcher_icons`

### 5.3 Splash Screen

- [ ] ใช้ `flutter_native_splash`
- [ ] สร้าง logo สำหรับ splash

```yaml
dev_dependencies:
  flutter_native_splash: ^2.4.0

flutter_native_splash:
  color: "#4CAF50"
  image: "assets/icon/splash_logo.png"
  android: true
```

Run: `dart run flutter_native_splash:create`

### 5.4 Theme / Font ภาษาไทย

- [ ] พิจารณาใช้ Google Fonts (Kanit, Prompt, Sarabun)
- [ ] ตรวจ Dark Mode ทำงานถูกต้องทุกหน้า
- [ ] ตรวจ font size ภาษาไทยอ่านง่าย

---

<a id="phase-6"></a>
## Phase 6 — Legal & Privacy Policy (~2-3 ชั่วโมง)

### 6.1 Privacy Policy (จำเป็น 100% สำหรับ Play Store)

**เนื้อหาที่ต้องมี:**
- ข้อมูลที่แอปเก็บ: รูปอาหาร, ข้อมูลโภชนาการ, เป้าหมายสุขภาพ, ข้อมูลส่วนตัวพื้นฐาน
- การส่งข้อมูลไปภายนอก: เฉพาะรูปอาหาร → Google Gemini API (**ด้วย API Key ของผู้ใช้เอง**)
- การเก็บรักษา: **Offline-first** ข้อมูลอยู่ในเครื่องเท่านั้น ไม่มี server ของเรา
- เราไม่เก็บ/ส่ง API Key ของผู้ใช้ไปที่ไหน (เก็บใน Secure Storage ในเครื่อง)
- สิทธิ์ผู้ใช้: ลบข้อมูลทั้งหมดได้ทุกเมื่อ
- Permissions ที่ขอ: กล้อง (ถ่ายรูปอาหาร), แกลเลอรี (เลือกรูป)

**วิธีจัดทำ:**
- [ ] เขียน Privacy Policy ภาษาไทย (และภาษาอังกฤษถ้าจำเป็น)
- [ ] Host บน GitHub Pages, Google Sites, หรือ Notion (ฟรี)
- [ ] ได้ URL สำหรับใส่ใน Play Store

### 6.2 Terms of Service

**เนื้อหาสำคัญ:**
- Disclaimer: ข้อมูลโภชนาการจาก AI **อาจไม่ถูกต้อง 100%**
- **ไม่ใช่คำแนะนำทางการแพทย์** — ปรึกษาแพทย์/นักโภชนาการ
- ผู้ใช้รับผิดชอบ API Key และ cost ของ Google API ด้วยตัวเอง
- ผู้ใช้รับผิดชอบข้อมูลของตัวเอง (แอปไม่มี backup บน cloud)

### 6.3 Link ใน App

**ไฟล์:** `lib/features/profile/presentation/profile_screen.dart`

- [ ] "นโยบายความเป็นส่วนตัว" → `url_launcher` ไปที่ Privacy Policy URL
- [ ] "เงื่อนไขการใช้งาน" → `url_launcher` ไปที่ Terms URL

---

<a id="phase-7"></a>
## Phase 7 — Testing & QA (~1-2 วัน)

### 7.1 Manual Testing Checklist — Happy Path

- [ ] เปิดแอปครั้งแรก → Onboarding → ตั้งเป้าหมาย → เข้าหน้าหลัก
- [ ] ตั้งค่า API Key ตามคู่มือ → ทดสอบการเชื่อมต่อ → สำเร็จ
- [ ] ถ่ายรูปอาหาร → Gemini วิเคราะห์ → ยืนยัน → บันทึก → เห็นใน Timeline
- [ ] เลือกรูปจากแกลเลอรี → วิเคราะห์ → บันทึก
- [ ] แชท "วันนี้กินข้าวผัด" → สร้างรายการ → เห็นใน Timeline + Diet
- [ ] แชท "ทานกระเพราะ ชาเย็น ขนมครก" → สร้าง 3 รายการ
- [ ] Quick Add → เลือก → บันทึก
- [ ] Diet Tab → เพิ่มอาหารด้วยมือ → บันทึก
- [ ] แก้ไขรายการ → เปลี่ยนปริมาณ → kcal เปลี่ยนตาม
- [ ] แก้ไขรายการ → เปลี่ยนวันที่/เวลา → บันทึก
- [ ] ลบรายการอาหาร → หายไป
- [ ] เปลี่ยนวันที่ → สรุปเปลี่ยนตาม (ทั้ง Timeline + Diet)
- [ ] สร้าง My Meal → เพิ่ม ingredients → Gemini ค้นหา → บันทึก
- [ ] ใช้ My Meal → Log จาก Meal → เห็นใน Timeline
- [ ] ตั้งเป้าหมาย kcal + macro % → บันทึก → สรุปเปลี่ยน
- [ ] ดูสรุปวันนี้ (Daily Summary Card) → ตรงกับที่บันทึก

### 7.2 Manual Testing — Edge Cases / No API Key

- [ ] **ไม่มี API Key** → ถ่ายรูปอาหาร → แสดง dialog แนะนำตั้งค่า (ไม่ crash)
- [ ] **ไม่มี API Key** → แชท → ใช้ local DB ค้นหา (graceful)
- [ ] **ไม่มี API Key** → เพิ่มอาหารด้วยมือ → ทำงานปกติ
- [ ] **API Key ผิด** → ทดสอบ → แจ้ง "Key ไม่ถูกต้อง"
- [ ] ไม่อนุญาต permission กล้อง → แอปไม่ crash
- [ ] ไม่มี internet → ยังบันทึกด้วยมือ/Quick Add ได้
- [ ] ข้อมูลเยอะ (100+ entries) → ไม่ช้า
- [ ] Dark Mode → UI ไม่แตก
- [ ] หน้าจอเล็ก (5") → UI ไม่ล้น
- [ ] Android 10, 11, 12, 13, 14 → ทำงานได้

### 7.3 Performance Check

- [ ] Cold start < 3 วินาที
- [ ] APK size < 50MB
- [ ] ไม่มี memory leak ที่ชัดเจน

---

<a id="phase-8"></a>
## Phase 8 — Build & Publish to Google Play (~3-4 ชั่วโมง + รอ review)

### 8.1 สร้าง Signing Key

```bash
keytool -genkey -v -keystore miro-release-key.jks ^
  -keyalg RSA -keysize 2048 -validity 10000 ^
  -alias miro
```

> **สำคัญมาก:** เก็บ keystore file และ password ให้ดี!
> ถ้าหาย จะ update แอปบน Play Store ไม่ได้อีกเลย

สร้าง `android/key.properties`:
```properties
storePassword=รหัสผ่านที่ตั้ง
keyPassword=รหัสผ่านที่ตั้ง
keyAlias=miro
storeFile=path/to/miro-release-key.jks
```

> เพิ่ม `key.properties` ใน `.gitignore` ด้วย!

### 8.2 แก้ android/app/build.gradle.kts

```kotlin
android {
    namespace = "com.yourname.mirocalorie"  // ชื่อ package unique
    
    defaultConfig {
        applicationId = "com.yourname.mirocalorie"
        minSdk = 23        // Android 6.0+
        targetSdk = 34     // Android 14
        versionCode = 1
        versionName = "1.0.0"
    }
    
    signingConfigs {
        create("release") {
            // อ่านจาก key.properties
        }
    }
    
    buildTypes {
        release {
            isMinifyEnabled = true    // R8/ProGuard
            isShrinkResources = true  // ลบ resource ที่ไม่ใช้
            signingConfig = signingConfigs.getByName("release")
        }
    }
}
```

### 8.3 Build Release

```bash
flutter build appbundle --release
```

Output: `build/app/outputs/bundle/release/app-release.aab`

### 8.4 สมัคร Google Play Console

1. สมัคร Google Play Developer Account ($25 one-time)
2. สร้าง App ใหม่
3. เลือก **Free** app (Freemium model)
4. ตั้ง In-App Product:
   - Product ID: `miro_cal_pro`
   - Type: Non-consumable (managed product)
   - ราคา: 199-299 THB / $4.99-6.99 USD

### 8.5 กรอก Store Listing

| รายการ | รายละเอียด |
|--------|-----------|
| **App name** | ชื่อแอป (max 30 ตัวอักษร) |
| **Short description** | เช่น "บันทึกอาหาร นับแคลอรี่ ด้วย AI วิเคราะห์รูปถ่ายอัตโนมัติ" (max 80) |
| **Full description** | รายละเอียดฟีเจอร์ทั้งหมด + วิธีใช้ + บอกว่าต้องมี Gemini API Key (max 4000) |
| **Category** | Health & Fitness |
| **Content Rating** | ทำแบบสอบถาม IARC |
| **Privacy Policy URL** | ลิงก์ที่เตรียมไว้ |
| **Target audience** | 13+ (ไม่ใช่เด็ก) |

### 8.6 เตรียม Assets สำหรับ Store

| Asset | ขนาด | จำนวน |
|-------|------|--------|
| App Icon (Hi-res) | 512×512 | 1 |
| Feature Graphic | 1024×500 | 1 |
| Phone Screenshots | 1080×1920 | 4-8 |
| Tablet Screenshots (optional) | 1200×1920 | 1-4 |

**Screenshot แนะนำ:**
1. หน้า Timeline + สรุปวัน
2. ถ่ายรูปอาหาร → AI วิเคราะห์
3. แชทบันทึกอาหาร
4. รายละเอียดโภชนาการ
5. My Meal / สร้างสูตร
6. ตั้งเป้าหมาย kcal/macro

### 8.7 Full Description ตัวอย่าง (สำหรับ Play Store)

```
🍽️ บันทึกอาหาร นับแคลอรี่ ด้วย AI อัจฉริยะ

ถ่ายรูปอาหาร → AI วิเคราะห์ให้อัตโนมัติ!
ไม่ต้องค้นหาเอง ไม่ต้องกรอกเอง แค่ถ่ายรูปหรือพิมพ์บอก

✨ ฟีเจอร์หลัก:
• ถ่ายรูปอาหาร → AI วิเคราะห์ kcal, โปรตีน, คาร์บ, ไขมัน อัตโนมัติ
• พิมพ์แชท เช่น "กินข้าวผัด" → บันทึกให้เลย
• พิมพ์หลายเมนูพร้อมกัน เช่น "กินกระเพราะ ชาเย็น ขนมครก"
• Quick Add — เลือกจากเมนูยอดนิยม 1 แตะ
• สร้างสูตรอาหาร (My Meal) เก็บไว้ใช้ซ้ำ
• สรุป kcal / macro ทุกวัน
• ตั้งเป้าหมายสุขภาพ (kcal, โปรตีน, คาร์บ, ไขมัน, น้ำ)
• แก้ไขวัตถุดิบที่ AI วิเคราะห์ได้
• รองรับฐานข้อมูลอาหารไทย

🔒 ข้อมูลของคุณปลอดภัย:
• ข้อมูลทั้งหมดเก็บในเครื่องของคุณ (Offline-first)
• ไม่มี server ของเรา ไม่เก็บข้อมูลของคุณ
• ใช้ Gemini API Key ของคุณเอง (ฟรีจาก Google)

⚙️ หมายเหตุ:
• การวิเคราะห์อาหารด้วย AI ต้องใช้ Gemini API Key (สร้างฟรีที่ aistudio.google.com)
• มีคู่มือการตั้งค่าในแอป
• ไม่มี API Key ก็บันทึกอาหารด้วยมือได้
• ข้อมูลโภชนาการจาก AI อาจไม่แม่นยำ 100%
```

### 8.8 Upload & Submit

1. Upload `.aab`
2. สร้าง Release (Production หรือ Open Testing)
3. Submit for review
4. รอ ~1-7 วัน (ครั้งแรกอาจนานกว่า)

---

<a id="files"></a>
## ไฟล์ที่เกี่ยวข้องทั้งหมด

### ต้องแก้ไข (Modify)
| ไฟล์ | Phase | ทำอะไร |
|------|-------|--------|
| `lib/features/home/presentation/home_screen.dart` | 1 | ลบ Bottom Nav, ลบ Notification |
| `lib/features/health/presentation/health_page.dart` | 1 | ลด Tab เหลือ 3 |
| `lib/features/home/widgets/magic_button.dart` | 1 | Fix context |
| `lib/features/chat/services/intent_handler.dart` | 1 | ลบ finance/task handler |
| `lib/features/profile/presentation/profile_screen.dart` | 1,4,6 | ซ่อนเมนู + implement ล้างข้อมูล + link policy |
| `lib/features/profile/presentation/api_key_screen.dart` | 2 | เขียนคู่มือ step-by-step + implement test |
| `lib/core/ai/gemini_service.dart` | 2 | ปรับ error message ให้ชัด |
| `lib/features/health/widgets/food_detail_bottom_sheet.dart` | 2 | Graceful no-key handling |
| `lib/features/health/presentation/food_preview_screen.dart` | 2 | Graceful no-key handling |
| `lib/features/profile/models/user_profile.dart` | 3 | เพิ่ม fields |
| `lib/main.dart` | 3 | Onboarding gate |
| ทุกไฟล์ .dart (~32 ไฟล์) | 4 | Replace debugPrint |
| `android/app/src/main/AndroidManifest.xml` | 5 | เปลี่ยนชื่อ |
| `pubspec.yaml` | 5 | เพิ่ม launcher_icons, splash, ลบ deps ไม่ใช้ |
| `android/app/build.gradle.kts` | 8 | applicationId, signing, minify |

### ต้องสร้างใหม่ (Create)
| ไฟล์ | Phase | ทำอะไร |
|------|-------|--------|
| `lib/features/onboarding/presentation/onboarding_screen.dart` | 3 | Welcome + ตั้งค่า |
| `lib/core/utils/tdee_calculator.dart` | 3 | คำนวณ TDEE |
| `lib/core/utils/logger.dart` | 4 | Logger utility |
| `assets/icon/app_icon.png` | 5 | App icon |
| `assets/icon/splash_logo.png` | 5 | Splash logo |
| `assets/images/guide/step_1.png` ถึง `step_6.png` | 2 | Screenshot คู่มือ API Key |
| `android/key.properties` | 8 | Signing config |
| Privacy Policy (host ภายนอก) | 6 | Legal |
| Terms of Service (host ภายนอก) | 6 | Legal |

---

<a id="timeline"></a>
## Timeline ประมาณการ

| Phase | งาน | เวลา | วันที่ (ถ้าเริ่มวันนี้) |
|-------|------|------|----------------------|
| **1** | ซ่อนฟีเจอร์ | 2-3 ชม. | วันที่ 1 เช้า |
| **2** | BYOK + คู่มือ API Key | 3-4 ชม. | วันที่ 1 บ่าย |
| **2.5** | **Freemium + IAP** | **1.5-2 วัน** | **วันที่ 2-3** |
| **3** | Onboarding + TDEE | 1-2 วัน | วันที่ 3-4 |
| **4** | Debug cleanup + hardening | 3-4 ชม. | วันที่ 4 บ่าย |
| **5** | Icon + Splash + ชื่อ | 1 วัน | วันที่ 5 |
| **6** | Privacy Policy + Terms | 2-3 ชม. | วันที่ 5 บ่าย |
| **7** | Testing ทั้งหมด | 1-2 วัน | วันที่ 6-7 |
| **8** | Build + Upload (Free + IAP) | 3-4 ชม. | วันที่ 7 |

### **รวม: ~7-8 วันทำงาน**

---

## เตือนความจำสำคัญ

### ก่อนทำ Phase ใดๆ
- [x] Backup โปรเจคทั้งหมดแล้ว

### ก่อน Build Release (Phase 8)
- [ ] ตรวจว่าไม่มี `.env` / API Key หลุดไปใน APK
- [ ] `.gitignore` มี `.env`, `key.properties`
- [ ] ลบ `debugShowCheckedModeBanner: false` ออก (ใน release จะไม่แสดง banner อยู่แล้ว แต่ลบให้ clean)
- [ ] ตรวจ ProGuard rules ไม่ strip code ที่จำเป็น (Isar, JSON serialization)

### หลัง Publish
- [ ] ทดสอบซื้อ + ดาวน์โหลดจาก Play Store จริง
- [ ] ตอบ review ของผู้ใช้
- [ ] วางแผน v1.1 (bug fixes + ฟีเจอร์เสริม)
