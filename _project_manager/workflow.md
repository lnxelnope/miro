# 🔄 Sentina Development Workflow

## 📋 Overview: วิธีการทำงานหลัก

การพัฒนา Sentina ใช้ระบบ **"Two-Model Workflow"** โดยแบ่งหน้าที่ชัดเจน:

```
┌─────────────────────────────────────────────────────────────────┐
│                    TWO-MODEL WORKFLOW                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🧠 PLANNER (Senior Coder)           🔧 CODER (Junior Coder)     │
│  ─────────────────────               ──────────────────────────  │
│  • วางแผน + สร้าง MD file            • รับ MD file ไปทำตาม       │
│  • ❌ ไม่ลงมือ code เอง              • ✅ เขียน code ตาม step    │
│  • Output = Task MD เท่านั้น         • ถามน้อย ทำตาม MD          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    DEVELOPMENT WORKFLOW                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐     │
│   │  STEP 1 │───▶│  STEP 2 │───▶│  STEP 3 │───▶│  STEP 4 │     │
│   │  CHECK  │    │  PLAN   │    │ EXECUTE │    │ DEPLOY  │     │
│   │ ROADMAP │    │   MD    │    │   CODE  │    │ & TEST  │     │
│   └─────────┘    └─────────┘    └─────────┘    └─────────┘     │
│                                                                  │
│   ดู Global     🧠 PLANNER     🔧 New Chat    Model ฉลาด        │
│   Roadmap ว่า   สร้าง Task     + CODER        สำหรับ Deploy      │
│   ถึงไหนแล้ว    MD เท่านั้น!   ทำตาม MD       & ตรวจสอบ          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## ⚠️ IMPORTANT: Model Roles

```
┌─────────────────────────────────────────────────────────────────┐
│  🧠 PLANNER (Senior Coder) - Model ที่วางแผน                    │
├─────────────────────────────────────────────────────────────────┤
│  ✅ DO:                                                          │
│  • วิเคราะห์ requirement                                        │
│  • สร้างไฟล์ MD แบบ step-by-step                                │
│  • ระบุ files ที่ต้องสร้าง/แก้ไข                                 │
│  • เขียน code snippets ใน MD                                    │
│  • อธิบาย logic และ verification                                │
│  • 🔍 ตรวจงาน + Cleanup เมื่องานเสร็จ (ดู STEP 0)               │
│                                                                  │
│  ❌ DO NOT:                                                      │
│  • ลงมือ create/edit CODE files โดยตรง                          │
│  • Deploy functions                                             │
│  • ทำงานแทน Coder ในส่วน coding                                 │
│                                                                  │
│  Output = ไฟล์ .md ใน _task_from_user/ + อัพเดท Roadmap        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  🔧 CODER (Junior Coder) - Model ที่เขียน Code                  │
├─────────────────────────────────────────────────────────────────┤
│  ✅ DO:                                                          │
│  • อ่าน MD file ที่ได้รับ                                       │
│  • ทำตาม step-by-step ไม่ต้องคิดเอง                             │
│  • สร้าง/แก้ไขไฟล์ตาม instructions                              │
│  • ถามน้อย ทำมาก                                                │
│                                                                  │
│  ❌ DO NOT:                                                      │
│  • คิดเองนอกเหนือจาก MD                                         │
│  • ข้าม steps                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 STEP 0: Review Existing Task (ตรวจงาน)

### เมื่อไหร่ใช้ STEP 0?
เมื่อ User สั่ง **"ตรวจงาน"** หรือ **"review task"** ให้ Planner ทำตามนี้:

```
┌─────────────────────────────────────────────────────────────────┐
│  🔍 REVIEW WORKFLOW                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. อ่าน Task MD File ที่ต้องตรวจ                               │
│  2. ตรวจสอบ Definition of Done ทุกข้อ                           │
│  3. ตรวจสอบว่า files ถูกสร้าง/แก้ไขครบหรือไม่                   │
│  4. ตรวจ linter errors (ถ้ามี)                                   │
│                                                                  │
│  ผลลัพธ์:                                                        │
│  ├── ✅ เสร็จครบ → ทำ CLEANUP ทันที (ไม่ต้องถาม)                │
│  └── ❌ ไม่ครบ → รายงานสิ่งที่ขาด + สร้าง Task MD เพิ่มเติม    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### CLEANUP Actions (ทำทันทีเมื่องานเสร็จ):

```
เมื่อตรวจแล้วพบว่างานเสร็จ Planner ต้องทำ:

1. ✅ อัพเดท global_roadmap.md
   - Mark [x] ที่ items ที่ทำเสร็จ
   - เปลี่ยน status เป็น ✅ COMPLETED ถ้าจบ sub-section
   - ย้าย ⚡ CURRENT ไปยัง task ถัดไป

2. 🗑️ ลบ Task MD File ที่เสร็จแล้ว
   - ป้องกัน Coder สับสนกับ task เก่า
   - ประหยัด token

3. 📋 สร้าง Task MD ใหม่ (ถ้า Phase ถัดไปพร้อม)
   - ดู roadmap ว่า task ถัดไปคืออะไร
   - สร้าง MD file พร้อมใช้งาน

4. 📊 สรุปรายงาน
   - บอก User ว่าทำอะไรไปบ้าง
   - แนะนำ next steps
```

### ตัวอย่างการตรวจงาน:

```
User: "@workflow.md ใช้ตรวจงาน @phase2_dm_system.md"

Planner ทำ:
1. อ่าน phase2_dm_system.md
2. ตรวจ Definition of Done ทุกข้อ
3. ตรวจว่าไฟล์สร้างครบไหม
4. ถ้าเสร็จ:
   - อัพเดท global_roadmap.md (mark Phase 2.4 ✅)
   - ลบ phase2_dm_system.md
   - สร้าง phase3_content_types.md (task ถัดไป)
5. รายงานผล
```

---

## 📌 STEP 1: Check Global Roadmap (ดู Roadmap ว่าถึงไหน)

### วิธีการ:
1. เปิดไฟล์ `_task_from_user/project_guideline/global_roadmap.md`
2. ดูที่ section "Development Phases" ว่า Phase ไหนมี ⚡ CURRENT
3. ดู checklist `[ ]` (ยังไม่ทำ) vs `[x]` (ทำเสร็จแล้ว)

### สถานะปัจจุบัน:
```
📍 Current Phase: Phase 3 - Content & Engagement ⚡

Phase 2 - Social Foundation ✅ COMPLETED

Phase 3 Checklist:
├── 3.1 Content Types ✅ COMPLETED
│   ├── [x] Insight Posts (Text + Image)
│   ├── [x] Projects (Goal → Public)
│   └── [x] Milestones & Progress
│
├── 3.2 Feed System ⚡ CURRENT
│   ├── [ ] Home Feed (Following)
│   ├── [ ] Discover Feed
│   └── [ ] Profile Feed
│
├── 3.3 Engagement
│   ├── [ ] Like
│   ├── [ ] Comment
│   ├── [ ] Share/Repost
│   └── [ ] Save/Bookmark
│
└── 3.4 Privacy Controls
    ├── [ ] Post-level privacy
    └── [ ] Project visibility
```

### อัพเดท Roadmap:
หลังทำ task เสร็จ ให้ mark `[x]` ใน global_roadmap.md

---

## 📝 STEP 2: Create Task MD File (🧠 PLANNER ทำขั้นตอนนี้)

### ⚠️ CRITICAL RULE:
```
🧠 PLANNER ทำแค่ขั้นตอนนี้เท่านั้น!
   ├── สร้างไฟล์ .md ใน _task_from_user/
   ├── เขียน step-by-step ละเอียด
   └── ❌ หยุด! ไม่ทำ STEP 3-4 เอง

📤 Output ของ PLANNER = ไฟล์ MD 1 ไฟล์
   User จะเอาไปใช้กับ Coder ใน New Chat
```

### หลักการ:
- สร้างไฟล์ `.md` ใน folder `_task_from_user/`
- เขียนแบบ **Step-by-step** ละเอียดมาก
- Coder จะอ่านและทำตามได้โดยไม่ต้องถามเพิ่ม

### Template สำหรับ Task File:

```markdown
# 📋 Task: [ชื่อ Task]

## 🎯 เป้าหมาย
[อธิบายสั้นๆ ว่าต้องการอะไร]

## 📂 ไฟล์ที่เกี่ยวข้อง
- `path/to/file1.tsx` - คำอธิบาย
- `path/to/file2.ts` - คำอธิบาย

## 🔧 ขั้นตอนการทำงาน

### Step 1: [ชื่อขั้นตอน]
**ไฟล์:** `path/to/file.tsx`
**Action:** [create/edit/delete]

ทำอะไร:
- รายละเอียด 1
- รายละเอียด 2

Code example (ถ้ามี):
```typescript
// ตัวอย่าง code
```

### Step 2: [ชื่อขั้นตอน]
...

## ⚠️ ข้อควรระวัง
- [สิ่งที่ต้องระวัง]

## ✅ Definition of Done
- [ ] Checkbox 1
- [ ] Checkbox 2

## 🚀 ต้อง Deploy หรือไม่?
- [ ] ไม่ต้อง Deploy (Frontend only)
- [x] ต้อง Deploy Firebase Functions
- [ ] ต้อง Deploy Firestore Rules
- [ ] ต้องทำอย่างอื่น: [อธิบาย]
```

### ตัวอย่าง Task Files ที่มีอยู่:
```
_task_from_user/
├── assets_and_subtasks_integration.md
├── financial_intents_system.md
├── future_development.md
├── life_dreams_dashboard.md
├── sentina_activation_onboarding.md
├── smart_tagging_system.md
├── task_modal_improvements.md
├── unified_dashboard.md
└── project_guideline/
    ├── global_roadmap.md
    ├── technical_blueprint.md
    └── workflow.md (ไฟล์นี้)
```

---

## 🖥️ STEP 3: Execute with Fresh Context (New Chat + Coder)

### ขั้นตอน:
1. **เปิด New Chat** ใน Cursor เพื่อล้าง context เก่า
2. **Attach Task File** โดยใช้ `@_task_from_user/[task_name].md`
3. **พิมพ์คำสั่ง** เช่น:
   ```
   @_task_from_user/task_modal_improvements.md 
   ช่วยทำตาม task นี้ให้หน่อย
   ```

### Tips สำหรับ Prompt ที่ดี:
```
✅ Good Prompts:
- "ทำตาม task นี้ทั้งหมด"
- "ทำเฉพาะ Step 1-3 ก่อน"
- "ตรวจสอบว่า Step 1 ทำถูกต้องไหม"

❌ Bad Prompts:
- "ทำอะไรสักอย่าง" (ไม่ชัดเจน)
- "แก้ bug" (ไม่บอกว่า bug อะไร)
```

### Context Management:
```
┌─────────────────────────────────────────────────────────────────┐
│  💡 เมื่อไหร่ควรเปิด New Chat?                                  │
├─────────────────────────────────────────────────────────────────┤
│  ✅ เริ่ม Task ใหม่                                             │
│  ✅ Context ยาวมาก (> 100 messages)                             │
│  ✅ เปลี่ยน topic หลัก                                          │
│  ✅ AI ตอบผิดซ้ำๆ (อาจสับสนจาก context เก่า)                     │
│                                                                  │
│  ❌ ไม่ต้องเปิดใหม่ถ้า:                                         │
│  - ยังทำ task เดิมอยู่                                          │
│  - ต้องการให้ AI จำ context ก่อนหน้า                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 STEP 4: Deploy & Review (สำหรับงานยาก)

### เมื่อไหร่ต้องเปลี่ยน Model?

```
┌─────────────────────────────────────────────────────────────────┐
│  🧠 Model Selection Guide                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📝 Regular Coding Tasks:                                        │
│     → ใช้ Default Model (Claude Sonnet / GPT-4o-mini)           │
│     → UI components, simple logic, refactoring                   │
│                                                                  │
│  🔥 Complex Tasks (ต้องการ "ความคิด"):                           │
│     → ใช้ Smart Model (เช่น o1 / o3 / Gemini 2.5 ฯลฯ)           │
│     → Deploy, Debug ยาก, Architecture decisions                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### งานที่ต้องใช้ Smart Model:

| Task Type | ทำไมต้อง Smart Model | Model แนะนำ |
|-----------|----------------------|-------------|
| 🚀 **Firebase Deploy** | ต้องเข้าใจ config, แก้ error หลายชั้น | Smart Model |
| 🔐 **Firestore Rules** | Security rules ซับซ้อน, ต้องคิดหลาย case | Smart Model |
| 🐛 **Debug Production** | หา root cause จาก logs, trace back | o1 / o3 |
| 🏗️ **Architecture Change** | ต้องเข้าใจ impact กว้าง, trade-offs | Smart Model |
| 🔄 **Large Refactor** | แก้หลายไฟล์, ต้องไม่ break อะไร | Smart Model |
| 📊 **Complex Algorithm** | คิดวิธีคำนวณ, optimize performance | o1 / o3 |

### Deploy Checklist:

```markdown
## 🚀 Firebase Deploy Checklist

### Pre-Deploy:
- [ ] ตรวจสอบ code ไม่มี error ใน local
- [ ] รัน `npm run lint` ผ่าน
- [ ] ตรวจสอบ environment variables

### Deploy Commands:
```bash
# Deploy Cloud Functions เท่านั้น
firebase deploy --only functions

# Deploy Firestore Rules เท่านั้น  
firebase deploy --only firestore:rules

# Deploy ทุกอย่าง
firebase deploy

# Deploy specific function
firebase deploy --only functions:functionName
```

### Post-Deploy:
- [ ] ตรวจสอบ Firebase Console ว่า deploy สำเร็จ
- [ ] Test function ใน production
- [ ] ตรวจสอบ logs ว่าไม่มี error
```

### Prompt Template สำหรับ Deploy:
```
ช่วย deploy Firebase Functions ให้หน่อย

สิ่งที่ต้องทำ:
1. ตรวจสอบ code ว่าพร้อม deploy
2. รัน deploy command
3. ตรวจสอบ logs ว่าสำเร็จ
4. Test function ว่าทำงานได้

ถ้ามี error ช่วย debug และแก้ไขด้วย
```

---

## 📁 File Structure Reference

```
Sentina/
├── _task_from_user/           # 📋 Task files อยู่ที่นี่
│   ├── project_guideline/
│   │   ├── global_roadmap.md   # 🗺️ ดู Phase & Progress
│   │   ├── technical_blueprint.md  # 📐 Schema & API Reference
│   │   └── workflow.md         # 📖 คู่มือนี้
│   └── *.md                    # Task files แต่ละงาน
│
├── app/                        # 📱 Expo Router pages
│   ├── (auth)/                 # Auth screens
│   ├── (main)/                 # Main app screens
│   └── onboarding/             # Onboarding flow
│
├── components/                 # 🧩 React components
│   ├── chat/                   # Chat-related components
│   ├── dashboard/              # Dashboard components
│   ├── tasks/                  # Task management
│   └── vault/                  # Vault (Finance, etc.)
│
├── hooks/                      # 🪝 Custom React hooks
├── lib/                        # 📚 Utilities & API calls
├── functions/                  # ☁️ Firebase Cloud Functions (Python)
├── types/                      # 📝 TypeScript types
└── context/                    # 🔄 React Context providers
```

---

## 🔧 Quick Reference Commands

### Development:
```bash
# Start development server
npm start

# Start web version
npm run web

# Type check
npx tsc --noEmit
```

### Firebase:
```bash
# Login to Firebase
firebase login

# Deploy functions
firebase deploy --only functions

# View function logs
firebase functions:log

# Emulator for local testing
firebase emulators:start
```

### Git:
```bash
# Check status
git status

# Stage all changes
git add .

# Commit
git commit -m "message"

# Push
git push origin main
```

---

## 💡 Best Practices

### 1. Task Granularity
```
❌ Bad: "สร้างระบบ Social Media ทั้งหมด"
✅ Good: "สร้าง Follow Button component"
```

### 2. Clear Success Criteria
```
❌ Bad: "ทำให้ดีขึ้น"
✅ Good: "กดปุ่ม Follow แล้ว status เปลี่ยนเป็น Following"
```

### 3. Reference Technical Blueprint
```
เมื่อสร้าง feature ใหม่ ให้ดู technical_blueprint.md สำหรับ:
- Database schema
- API endpoints
- UI component structure
```

### 4. Mark Progress & Cleanup
```
หลังทำ task เสร็จ:
1. Mark [x] ใน global_roadmap.md
2. ลบไฟล์ task .md ที่ทำเสร็จแล้ว (ป้องกัน confusion และประหยัด token)
3. Commit code พร้อม message ที่ชัดเจน

⚠️ สำคัญ: ลบ task file เมื่อตรวจงานเสร็จ!
   - ป้องกัน Coder สับสนกับ task เก่า
   - ประหยัด token ไม่ต้องอ่านซ้ำ
   - เก็บแค่ task ที่กำลังทำอยู่
```

---

## 🎯 Workflow Summary

```
┌──────────────────────────────────────────────────────────────────┐
│  📍 START                                                         │
│     │                                                             │
│     ├── มี Task MD อยู่แล้ว? ────────────────────────────────┐   │
│     │                                                         │   │
│     ▼                                                         ▼   │
│  ┌────────────────────────────────┐   ┌────────────────────────┐ │
│  │ 1️⃣ CHECK ROADMAP               │   │ 0️⃣ REVIEW TASK (ตรวจงาน)│ │
│  │    ดู Phase ไหน                │   │    ตรวจ Definition of  │ │
│  └────────────┬───────────────────┘   │    Done ทุกข้อ         │ │
│               │                       └───────────┬────────────┘ │
│               ▼                                   │              │
│  ┌────────────────────────────────┐               │              │
│  │ 2️⃣ CREATE TASK MD              │   ┌───────────▼────────────┐ │
│  │    เขียน step-by-step          │   │ ✅ เสร็จ → CLEANUP     │ │
│  └────────────┬───────────────────┘   │ • อัพเดท roadmap       │ │
│               │                       │ • ลบ task file เก่า    │ │
│               ▼                       │ • สร้าง task ใหม่      │ │
│  ┌────────────────────────────────┐   └────────────────────────┘ │
│  │ 3️⃣ NEW CHAT + EXECUTE          │                              │
│  │    Fresh context → Go!         │                              │
│  └────────────┬───────────────────┘                              │
│               │                                                   │
│               ▼                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 4️⃣ NEED DEPLOY?                                             │  │
│  │    ├── NO  → ไปขั้นตอนถัดไป                                 │  │
│  │    └── YES → Smart Model                                   │  │
│  └────────────┬───────────────────────────────────────────────┘  │
│               │                                                   │
│               ▼                                                   │
│  ┌────────────────────────────────┐                              │
│  │ 5️⃣ COMMIT & NEXT               │                              │
│  │    git commit → Next task      │                              │
│  └────────────────────────────────┘                              │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

*Last updated: February 2026*
