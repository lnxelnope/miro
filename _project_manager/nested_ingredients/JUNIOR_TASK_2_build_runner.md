# 🔧 JUNIOR TASK 2: รัน Build Runner

> **ระดับความยาก:** 🟢 Junior (ง่ายมาก)  
> **เวลาประมาณ:** 5-10 นาที  
> **ความรู้ที่ต้องมี:** รันคำสั่งใน terminal

---

## 🎯 เป้าหมาย

รัน `build_runner` เพื่อ generate code สำหรับ Isar database ให้รู้จัก fields ใหม่ที่เราเพิ่มไป

---

## ⚠️ ก่อนเริ่ม

**ต้องทำ JUNIOR_TASK_1 เสร็จก่อน!**

ถ้ายังไม่ทำ ให้กลับไปทำก่อน

---

## 📋 ขั้นตอนการทำ (ทำตามลำดับ)

### ✅ Step 1: เปิด Terminal

**ใน VS Code:**

1. กด `` Ctrl+` `` (backtick) หรือ
2. ไปที่ Menu `View` → `Terminal`

Terminal จะเปิดขึ้นมาด้านล่าง

---

### ✅ Step 2: ตรวจสอบว่าอยู่ใน project folder

พิมพ์คำสั่ง:

```bash
pwd
```

(ใน Windows PowerShell ให้ใช้ `cd` แทน)

**ผลลัพธ์ที่ต้องการ:**
```
C:\aiprogram\miro
```

หรือ path ที่มี `miro` อยู่ในนั้น

**ถ้าไม่ใช่:**

```bash
cd C:\aiprogram\miro
```

(แก้ path ให้ตรงกับ project ของคุณ)

---

### ✅ Step 3: รันคำสั่ง build_runner

**คัดลอกคำสั่งนี้แล้ววางใน Terminal:**

```bash
dart run build_runner build --delete-conflicting-outputs
```

กด Enter

---

### ✅ Step 4: รอให้มันทำงานเสร็จ

**คุณจะเห็น:**

```
[INFO] Generating build script completed, took 459ms
[INFO] Reading cached asset graph completed, took 147ms
[INFO] Checking for updates since last build completed, took 612ms
[INFO] Running build completed, took 8.2s
[INFO] Caching finalized dependency graph completed, took 45ms
[INFO] Succeeded after 8.2s with 2 outputs (4 actions)
```

**ข้อความสำคัญ:** `Succeeded`

**เวลาที่ใช้:** ประมาณ 5-15 วินาที (ขึ้นอยู่กับเครื่อง)

---

### ✅ Step 5: ตรวจสอบว่าสำเร็จ

**ถ้าเห็นคำว่า `Succeeded` แสดงว่าสำเร็จแล้ว!**

คุณจะเห็นว่ามีไฟล์ใหม่ถูกสร้างขึ้นมา:
- `lib/features/health/models/my_meal_ingredient.g.dart`

(ไฟล์นี้ถูก auto-generate ห้ามแก้เอง!)

---

## ✅ การตรวจสอบว่าทำถูกต้อง

### 1. เปิดไฟล์ `my_meal_ingredient.g.dart`

**ไฟล์:** `lib/features/health/models/my_meal_ingredient.g.dart`

ค้นหา `parentId`, `depth`, `isComposite`, `detail`

**ถ้าเจอ → ✅ สำเร็จ!**

ตัวอย่างโค้ดที่ควรเจอ:

```dart
void _myMealIngredientPut(...) {
  writer.writeInt(object.parentId);
  writer.writeInt(object.depth);
  writer.writeBool(object.isComposite);
  writer.writeString(object.detail);
  // ...
}
```

### 2. ไม่มี error ใน Terminal

ถ้า build_runner รันเสร็จแล้วไม่มี error (ไม่มีสีแดงๆ) แสดงว่าถูกต้อง

---

## ⚠️ ถ้าเจอ Error

### ❌ Error: "Please make sure build_runner is installed"

**วิธีแก้:**

```bash
dart pub get
```

แล้วลองรันคำสั่ง build_runner อีกครั้ง

---

### ❌ Error: "Conflicting outputs"

**วิธีแก้:**

ลบ build cache แล้วรันใหม่:

```bash
dart run build_runner clean
dart run build_runner build --delete-conflicting-outputs
```

---

### ❌ Error สีแดงๆ เกี่ยวกับ "Missing required parameters"

**สาเหตุ:** JUNIOR_TASK_1 ทำไม่ครบ

**วิธีแก้:**
1. กลับไปดูไฟล์ `my_meal_ingredient.dart`
2. ตรวจสอบว่าเพิ่ม fields ครบหรือยัง
3. ตรวจสอบว่า fields มี default value (`= 0`, `= false`) หรือ nullable (`?`)

---

## 🎉 เมื่อทำเสร็จ

**คุณทำเสร็จแล้ว!**

**ไฟล์ที่เปลี่ยน:**
- ✅ `my_meal_ingredient.g.dart` (auto-generated)

**สถานะ:**
- ✅ Isar database รู้จัก fields ใหม่แล้ว
- ✅ App สามารถคอมไพล์ได้ปกติ

---

## 🔜 ขั้นตอนถัดไป

**รอ Senior ทำ:**
- SENIOR_TASK_1: แก้ AI prompts
- SENIOR_TASK_2: แก้ provider logic

**เมื่อ Senior ทำเสร็จแล้ว ให้ทำ:**
- `JUNIOR_TASK_3_ui_ingredient_card.md`

---

## 🆘 ถ้าติดปัญหา

1. ตรวจสอบว่า JUNIOR_TASK_1 ทำเสร็จจริงๆ หรือยัง
2. ลอง restart VS Code แล้วรัน build_runner ใหม่
3. ถ้ายังไม่ได้ → copy error ทั้งหมดมาถามพี่
4. แนบ screenshot ของ Terminal มาด้วย

---

**หมายเหตุ:** คำสั่ง build_runner จะรันบ่อยมากในโปรเจค Flutter/Dart ทำความคุ้นเคยกับมันไว้!
