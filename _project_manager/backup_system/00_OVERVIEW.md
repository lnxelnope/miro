# 📚 คู่มือพัฒนา Backup & Transfer System

## เอกสารฉบับนี้คืออะไร?

คู่มือนี้สร้างขึ้นเพื่อให้ Junior Developer สามารถพัฒนาระบบ Backup & Transfer ได้โดย**ไม่ต้องคิดเอง**  
📌 **อ่านตามลำดับ ทำตามทุกขั้นตอน คัดลอกโค้ดได้เลย**

---

## 📂 โครงสร้างเอกสาร

| ไฟล์ | เนื้อหา | เวลาโดยประมาณ |
|------|---------|----------------|
| `00_OVERVIEW.md` | 👈 คุณอยู่ที่นี่ (ภาพรวม) | - |
| `01_BACKEND_SETUP.md` | สร้าง Cloud Functions | 3-4 ชม. |
| `02_CLIENT_SERVICE.md` | สร้าง BackupService (Flutter) | 3-4 ชม. |
| `03_UI_IMPLEMENTATION.md` | สร้าง UI สำหรับ Backup/Restore | 2-3 ชม. |
| `04_TESTING_GUIDE.md` | วิธีทดสอบระบบ | 2-3 ชม. |
| `05_TERMS_UPDATE.md` | อัปเดต Terms of Service | 1 ชม. |
| `06_ERROR_HANDLING.md` | จัดการ Error Cases ทั้งหมด | 2-3 ชม. |
| `CHECKLIST.md` | Checklist สำหรับตรวจสอบงาน | - |

---

## 🎯 สิ่งที่จะได้หลังทำเสร็จ

### ✅ ฝั่ง Backend (Firebase Cloud Functions)
- สร้าง Transfer Key ที่ใช้ได้ครั้งเดียว (Generate)
- ยืนยันและโอน Energy ไปเครื่องใหม่ (Redeem)
- Firestore Collection: `transfer_keys`

### ✅ ฝั่ง Frontend (Flutter)
- กด "Backup Data" → ได้ไฟล์ `.json` (มี Energy + Food History)
- กด "Restore from Backup" → เลือกไฟล์ → Energy + ข้อมูลอาหารกลับมา
- UI แสดง Preview และ Warning ก่อน Restore

### ✅ ระบบความปลอดภัย
- Transfer Key หมดอายุใน 30 วัน
- ใช้ได้ครั้งเดียว
- ไม่สามารถโอนให้เครื่องเดิมได้

---

## 📋 สิ่งที่ต้องรู้ก่อนเริ่ม

### ความรู้พื้นฐานที่ต้องมี:
- [x] พื้นฐาน Flutter (Dart)
- [x] พื้นฐาน Firebase Cloud Functions (TypeScript)
- [x] เคยใช้ Firestore มาบ้าง
- [x] พื้นฐาน JSON format

### ไม่ต้องรู้ก็ได้ (เราจะสอนในคู่มือ):
- การออกแบบ Backup System
- การจัดการ Transfer Key Security
- การ Merge ข้อมูล (Duplicate Check)

---

## 🛠 Tools ที่ต้องติดตั้ง

### ฝั่ง Backend
```bash
# ติดตั้ง Firebase CLI (ถ้ายังไม่มี)
npm install -g firebase-tools

# เข้าไปที่โฟลเดอร์ functions
cd functions

# ติดตั้ง dependencies
npm install
```

### ฝั่ง Frontend
```bash
# ติดตั้ง dependencies ใหม่ที่ต้องใช้
flutter pub add file_picker
flutter pub add path_provider
```

---

## 🚦 ลำดับการทำงาน (ห้ามข้าม!)

```
1️⃣ Backend (Cloud Functions)
   ↓
2️⃣ Client Service (BackupService)
   ↓
3️⃣ UI Implementation
   ↓
4️⃣ Testing
   ↓
5️⃣ Terms Update
   ↓
6️⃣ Error Handling Polish
```

### ❗ ทำไมต้องเป็นลำดับนี้?
- **Backend ต้องทำก่อน** → เพราะ Frontend ต้องเรียกใช้ Cloud Functions
- **Service ต้องทำก่อน UI** → เพราะ UI ต้องใช้ BackupService
- **Testing ทำหลัง UI** → เพื่อทดสอบ Flow ทั้งหมด
- **Error Handling ทำทีหลัง** → หลังจากระบบหลักใช้งานได้แล้ว

---

## 📌 หลักการสำคัญ

### 1. **ห้ามข้ามขั้นตอน**
   - ทำตามลำดับในคู่มือ
   - อย่าทำหลายไฟล์พร้อมกัน

### 2. **คัดลอกโค้ดได้เลย**
   - โค้ดในคู่มือนี้ใช้งานได้จริง
   - ไม่ต้องแก้อะไร (เว้นแต่จะระบุไว้)

### 3. **ทดสอบทุกขั้นตอน**
   - แต่ละขั้นตอนจะมีวิธีทดสอบ
   - ต้องผ่านถึงจะไปต่อ

### 4. **อ่าน Error Message**
   - ถ้า Error → อ่านข้อความ
   - เช็คใน `06_ERROR_HANDLING.md`

### 5. **ถามได้เสมอ**
   - ถ้าติดปัญหา → บอก Error Message พร้อมบรรทัดที่ผิด
   - แนบ Code ที่กำลังเขียน

---

## 🔥 Quick Start

### เริ่มเลย!
```bash
# 1. เปิดไฟล์แรก
code _project_manager/backup_system/01_BACKEND_SETUP.md

# 2. อ่านและทำตามทุกขั้นตอน
```

---

## 📝 Checklist ก่อนเริ่ม

ตรวจสอบให้แน่ใจว่า:
- [ ] อ่าน `docs/PLAN_backup_transfer.md` แล้ว (เพื่อเข้าใจภาพรวม)
- [ ] มี Firebase Project แล้ว
- [ ] ติดตั้ง Firebase CLI แล้ว (`firebase --version`)
- [ ] ติดตั้ง Flutter SDK แล้ว (`flutter --version`)
- [ ] เปิด VS Code / Android Studio แล้ว
- [ ] มีเวลาว่างอย่างน้อย 3-4 ชั่วโมงต่อเนื่อง (สำหรับ Phase แรก)

---

## 🆘 หากมีปัญหา

### 1. ตรวจสอบ Checklist
   - ดูใน `CHECKLIST.md`
   - ตรวจว่าขั้นตอนก่อนหน้าผ่านหรือไม่

### 2. อ่าน Error Handling Guide
   - เปิด `06_ERROR_HANDLING.md`
   - ค้นหา Error Message ที่ได้รับ

### 3. ตรวจสอบไฟล์ที่สร้างแล้ว
   ```bash
   # Backend
   functions/src/transferKey.ts  → มีหรือยัง?
   functions/src/index.ts        → export แล้วหรือยัง?
   
   # Frontend
   lib/core/services/backup_service.dart → มีหรือยัง?
   ```

### 4. Restart Development Server
   ```bash
   # Backend
   cd functions
   npm run serve  # หยุดแล้วรันใหม่
   
   # Frontend
   flutter run    # หยุดแล้วรันใหม่
   ```

---

## 💡 Tips สำหรับ Junior Developer

### 🟢 Good Practices
- ✅ Commit หลังแต่ละ Phase สำเร็จ
- ✅ เขียน TODO comment เมื่อไม่แน่ใจ
- ✅ ทดสอบในโหมด Debug ก่อน (จะเห็น Log)
- ✅ เก็บ Backup ไฟล์เดิมก่อนแก้

### 🔴 Bad Practices
- ❌ ข้ามขั้นตอนเพราะคิดว่าไม่จำเป็น
- ❌ แก้หลายไฟล์พร้อมกัน (จะหาจุด Error ยาก)
- ❌ ลบ Comment ทิ้ง (อาจต้องกลับมาอ่าน)
- ❌ Deploy ขึ้น Production ก่อนทดสอบ

---

## 🎯 เป้าหมายสุดท้าย

เมื่อทำตามคู่มือนี้เสร็จ คุณจะได้:
1. ระบบ Backup & Restore ที่ใช้งานได้จริง
2. ความรู้เรื่อง Transfer Key Security
3. ประสบการณ์การ Integrate Backend + Frontend
4. ความมั่นใจในการพัฒนา Feature ขนาดกลาง

---

## 📍 พร้อมแล้ว? เริ่มเลย!

➡️ **[ไปที่ 01_BACKEND_SETUP.md](./01_BACKEND_SETUP.md)**

---

*เอกสารนี้สร้างจาก: `docs/PLAN_backup_transfer.md`*  
*อัปเดตล่าสุด: 15 ก.ค. 2026*
